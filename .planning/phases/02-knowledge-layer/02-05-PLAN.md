---
phase: 02-knowledge-layer
plan: 05
type: execute
mode: mvp
wave: 5
week: 5
status: ready
authored: 2026-05-30
review_history:
  - cycle: 1
    reviewed_at: 2026-05-30
    reviewers: [codex]
    reviews_file: .planning/phases/02-knowledge-layer/02-REVIEWS.md
    findings: { critical: 0, high: 4, medium: 2, low: 1 }
    addressed_in_replan: true   # cycle-2 replan applied 2026-05-31: all 4 HIGH + 2 MEDIUM addressed in this doc; C1-H4/OD-1 resolved by founder ruling to DEFER T04 (→ FOLLOWUP-SANITIZER-EVAL-01).
    summary: "C1-H1 skip fail-open on LIVE runs → added EVAL_LIVE_REQUIRED=1 (a 'skip' in a live-required context becomes a FAILURE / exit 1); C1-H2 OD-3 read path wrong (client.ts writes cache metrics to TRACE metadata, creates NO GENERATION observations) → switched cache-hit to fetchTraces({ name, fromTimestamp, fields:'core,io', limit }) reading metadata.cacheRead/inputTokens; C1-H3 invalid Actions YAML (if: under env:) → split into PR step (no secrets) + LIVE step (if: event != pull_request; sets EVAL_LIVE_REQUIRED + secrets) + PR-comment step (if: always() && pull_request); C1-H4 T04 fixtures mis-wired → DEFERRED per founder ruling 2026-05-31, T04 removed entirely, captured as FOLLOWUP-SANITIZER-EVAL-01; C1-M1 PENDING_ALLOWED → new Set<string>(['qa-grounding']); C1-M2 countPopulatedFields corrected to its ACTUAL shallow (Object.keys(group).length>0) semantics. Plan is now 3 tasks (T01–T03), all CORE."
  - cycle: 2
    reviewed_at: 2026-05-31
    reviewers: [codex]
    reviews_file: .planning/phases/02-knowledge-layer/02-REVIEWS.md
    findings: { critical: 0, high: 0, medium: 1, low: 1 }   # plan-level; raw code-vs-plan count was 3 (framing corrected in REVIEWS.md)
    addressed_in_replan: true
    summary: "All cycle-1 findings confirmed resolved at plan level (raw HIGHs were code-state observations on a planning-only review). 2 NEW plan findings folded in-cycle: C2-NEW-MED PR eval step scoped to `if: github.event_name=='pull_request'` (no redundant skip-pass on schedule/dispatch); C2-NEW-LOW fields:'core,io' relaxed from 'required' to 'the chosen restricted whitelist'."
  - cycle: 3
    reviewed_at: 2026-05-31
    reviewers: [codex]
    reviews_file: .planning/phases/02-knowledge-layer/02-REVIEWS.md
    findings: { critical: 0, high: 0, medium: 1, low: 0 }   # C3-H1 refuted as a type-read error; C3-M1 fixed → CONVERGED
    addressed_in_replan: true
    summary: "CONVERGED. C3-H1 (fromTimestamp string|null → Date would fail tsc) REFUTED via type-def evidence: fetchTraces takes FixTypes-wrapped GetLangfuseTracesQuery where fromTimestamp IS Date (langfuse-core d.ts 7013 + 7104-7108); Codex read the unwrapped openapi type. C3-M1 (threat row T-02-05-04 inaccurate 'no checkout before it' claim) FIXED to the accurate fork-PR token model. Genuine plan-level HIGH = 0; 3 tasks; execution-ready pending founder go."
depends_on: [02-01, 02-02, 02-03, 02-04]
predecessor_commits:
  - "3301aa9"   # HEAD of phase-2-knowledge-layer at start of this plan author (02-04 close tail; T08 triage)
  - "29228e8"   # schema-lock baseline — git diff --quiet 29228e8 -- src/db/schema/ must stay exit 0
strategic_tickets: [EVAL-01b]
requirements: []   # No canonical Phase-2 requirement is DELIVERED here — this plan builds prerequisite eval-harness infrastructure. Verified against REQUIREMENTS.md: phase-02 req IDs are KNW-01..05/KNW-08/XC-08 (no eval requirement). The cache-hit check is the verification surface for XC-06 (prompt-cache hit rate instrumented in Langfuse — already Complete) + FND-07 (Langfuse cache/token/cost tracing); extraction-floor guards the Phase-2 extraction quality bar. PITCH-04 (Phase 3 — Pitch Lab) is the canonical eval requirement that LATER reuses this shared machinery — it is NOT delivered by this plan (plan-checker Finding 1). See strategic_tickets: [EVAL-01b].
notes: "Prerequisite eval-harness infrastructure (stub-to-real flip), 3 tasks (T01–T03), all CORE. Delivers no new REQUIREMENTS.md ID; exercises XC-06/FND-07 instrumentation already shipped in Phase 0/1; unblocks PITCH-04's Phase-3 deck-reviewer eval by making the shared harness real. The live-Sonnet sanitizer FP/FN check was DEFERRED per founder ruling 2026-05-31 (C1-H4/OD-1) → FOLLOWUP-SANITIZER-EVAL-01 (a DIRECT eval over promptInjectionSanitizer + redactUnrelatedPartyPII, NOT via the paste path)."
autonomous: true   # 3 autonomous tasks; no human-action checkpoints
security_gate: cso
codex_review_required: true
files_modified:
  # Modified (eval checks — flip 'pending' → dynamic pass/fail/skip)
  - src/ai/eval/checks/extraction-floor.ts        # CORE — flip to real: 5 paste fixtures → extractFromPaste → mean field count ≥ 8
  - src/ai/eval/checks/cache-hit.ts               # CORE — flip to real: Langfuse fetchTraces({name,fromTimestamp,fields:'core,io',limit}) → metadata.cacheRead/inputTokens ratio > 0
  # src/ai/eval/checks/qa-grounding.ts            # UNCHANGED — STAYS 'pending' (depends on Plan 02-07 qa-rag agent). DO NOT flip.
  # Modified (eval core — runtime gate + skip status + EVAL_LIVE_REQUIRED)
  - src/ai/eval/types.ts                          # add 'skip' to EvalStatus union ('pending'|'pass'|'fail'|'skip') per OD-A
  - src/ai/eval/runner.ts                         # PENDING_ALLOWED = new Set<string>(['qa-grounding']) runtime gate (C1-M1); 'skip' non-failing UNLESS EVAL_LIVE_REQUIRED==='1' (then 'skip' → FAILURE, C1-H1); badge() handles 'skip' (FOLLOWUP-EVAL-PENDING-RUNTIME-GATE-01)
  # Created (fixture re-export seam — zero body duplication)
  - src/ai/eval/fixtures/index.ts                 # NEW — re-exports the 5 paste fixture PATHS + loadFixtureText from tests/ai/fixtures/ so checks import one place; no fixture bodies live here
  - src/ai/eval/fixtures/README.md                # update: fixtures now reuse tests/ai/fixtures/* (re-export module, no body duplication)
  # Modified (CI workflow — two-step PR/LIVE split + PR-comment step + permission)
  - .github/workflows/eval.yml                    # re-add `pull-requests: write`; split into a PR eval step (no secrets, EVAL_LIVE_REQUIRED unset) + a LIVE eval step (if: event != pull_request; sets EVAL_LIVE_REQUIRED=1 + ANTHROPIC_API_KEY/Langfuse secrets) + an actions/github-script PR-comment step (if: always() && pull_request) (FOLLOWUP-EVAL-PR-COMMENT-01, C1-H3, OD-2)
  # Modified (tests)
  - tests/ai/eval/runner.test.ts                  # keep 5 existing cases green; ADD Case 6 (non-qa-grounding 'pending' → exit 1) + Case 7 ('skip' → exit 0 when EVAL_LIVE_REQUIRED unset) + Case 8 ('skip' + EVAL_LIVE_REQUIRED=1 → exit 1, C1-H1)
  # Created (tests)
  - tests/ai/eval/checks/extraction-floor.test.ts # NEW — field-count logic + env-skip branch (mock extractFromPaste, no live calls in unit test)
  - tests/ai/eval/checks/cache-hit.test.ts        # NEW — ratio logic + env-skip branch (mock getLangfuseClient/fetchTraces)
  # Modified (docs)
  - tasks/lessons.md                              # capture the 'skip' vs 'pending' vs EVAL_LIVE_REQUIRED status-model lesson + Langfuse fetchTraces signature pin (metadata is in the 'io' field group)
mandatory_skills:
  - agent-evaluation                    # eval check semantics: pending (not-impl) vs skip (env-unavail) vs pass/fail; EVAL_LIVE_REQUIRED gate
  - advanced-evaluation                 # threshold pinning, eval-as-CI-gate, live-required-fails-on-skip
  - claude-api                          # extractFromPaste routes through runAgent (Anthropic chokepoint); cache metrics written to TRACE metadata
  - langfuse                            # fetchTraces read path; metadata.cacheRead/inputTokens (NOT GENERATION observations — client.ts emits none)
  - privacy-by-design                   # NEVER log/PR-comment chunk_text, paste content, PII, query content, creds, or trace input/output bodies
  - llm-structured-output              # extractFromPaste returns a Zod-validated BusinessMemoryDraft; field-count reuses countPopulatedFields
mandatory_mcp_servers:
  - context7                            # langfuse fetchTraces signature; vitest 4.1.6 flag surface; @anthropic-ai/sdk usage shape
  - github                              # .github/workflows/eval.yml two-step PR/LIVE split + PR-comment step (actions/github-script) authoring

must_haves:
  truths:
    - "extraction-floor.ts computes status dynamically: it loads the 5 paste fixtures from tests/ai/fixtures/paste-*.txt (via the re-export seam, NOT duplicated), runs the REAL extractFromPaste agent on each, counts populated top-level field groups on the returned draft by calling the EXISTING countPopulatedFields helper, and returns status='pass' when the MEAN field count across the 5 fixtures is ≥ 8, else status='fail'; metric = mean field count, threshold = 8"
    - "cache-hit.ts computes status dynamically: it queries Langfuse via getLangfuseClient()!.fetchTraces({ name, fromTimestamp: <Date now-minus-window>, fields: 'core,io', limit }) over a bounded window, sums metadata.cacheRead vs metadata.inputTokens across the returned agent:* traces (the exact keys client.ts writes via trace.update), and returns status='pass' when the ratio > 0, else status='fail'; metric = ratio, threshold = 0; Anthropic-only (Voyage exposes no prompt cache — stated in the docstring)"
    - "qa-grounding.ts is BYTE-UNCHANGED at plan close — it still returns the literal status: 'pending'; it is the ONLY file under src/ai/eval/checks/ containing the literal `status: 'pending'`"
    - "A new EvalStatus member 'skip' exists ('pending'|'pass'|'fail'|'skip'); 'skip' means env-unavailable (fail-OPEN in NON-live contexts, never blocks PR/local CI); 'pending' means not-yet-implemented; the two are distinct and the distinction is documented on the type"
    - "When ANTHROPIC_API_KEY is absent, extraction-floor returns status='skip' (NOT 'pending', NOT 'fail') with a reason that names the missing env var; when Langfuse is unconfigured (isLangfuseConfigured() === false), cache-hit returns status='skip' with a reason that names the unconfigured client"
    - "The runner enforces a LIVE-required gate: when process.env.EVAL_LIVE_REQUIRED === '1', ANY check that returns 'skip' is treated as a FAILURE (exitCode 1) — a live run that cannot reach its dependency is a RED gate, not a silent pass (C1-H1). When EVAL_LIVE_REQUIRED is unset (the PR/local default), 'skip' is non-failing. This is documented on the status model."
    - "runner.ts holds a runtime allowlist `const PENDING_ALLOWED = new Set<string>(['qa-grounding'])` (a Set<string> so PENDING_ALLOWED.has(r.id) typechecks against r.id: string — C1-M1); a 'pending' status from any check whose id is NOT in PENDING_ALLOWED forces exitCode = 1 (promotes the 02-04 process-gate to a runtime gate, FOLLOWUP-EVAL-PENDING-RUNTIME-GATE-01)"
    - "The runner's fail-CLOSED contract is preserved: any status='fail' → exitCode 1; the existing 5 runner.test.ts cases stay green; a 6th case pins (non-qa-grounding 'pending' → exit 1), a 7th pins ('skip' + EVAL_LIVE_REQUIRED unset → exit 0), and an 8th pins ('skip' + EVAL_LIVE_REQUIRED=1 → exit 1)"
    - "grep -rn \"status: 'pending'\" src/ai/eval/checks/ returns ≤ 1 hit AND that single hit is qa-grounding.ts (the flipped checks compute status from a variable and never contain the literal 'pending' string)"
    - "Zero schema changes: git diff --quiet 29228e8 -- src/db/schema/ exits 0 at plan close; zero new migrations under src/db/migrations/**; zero ALTER statements"
    - "Zero new npm dependencies: package.json dependencies + devDependencies are byte-identical to the 3301aa9 baseline EXCEPT a possible scripts-only addition; the Langfuse read uses the already-installed langfuse SDK's fetchTraces (no raw HTTP, no new client construction outside src/lib/langfuse.ts)"
    - "No eval file imports @anthropic-ai/sdk: extraction-floor exercises Anthropic ONLY via extractFromPaste → runAgent → src/ai/client.ts; the ESLint @anthropic-ai/sdk boundary stays green"
    - "Langfuse read whitelist holds: cache-hit reads ONLY metadata.cacheRead/metadata.inputTokens token counts from the returned traces; it NEVER logs, returns in `reason`, or PR-comments trace.input / trace.output bodies, chunk_text, paste content, PII, founder query content, or Langfuse credentials. (NOTE: fields:'core,io' also returns input/output on the trace object — the executor MUST read ONLY metadata.* and never touch .input/.output.)"
    - ".github/workflows/eval.yml has `pull-requests: write` re-added AND a two-step PR/LIVE structure (C1-H3) AND an actions/github-script PR-comment step (FOLLOWUP-EVAL-PR-COMMENT-01); the comment body NEVER contains secrets or trace/paste/PII content (only the check id, status badge, sanitized reason, metric, threshold)"
    - "Live-AI checks (extraction-floor, cache-hit) truly execute on `schedule` + `workflow_dispatch` only (the LIVE step sets EVAL_LIVE_REQUIRED=1 + the secrets); on `pull_request` events the PR step runs them WITHOUT secrets and WITHOUT EVAL_LIVE_REQUIRED, so they env-skip (return 'skip', non-failing) — PRs stay fast + free and never false-fail; the PR-comment step posts the latest result or a 'live checks run nightly' note on PR events (OD-2)"
    - "npm run eval:run still exits per contract (0 on all-pass/skip + qa-grounding-pending when EVAL_LIVE_REQUIRED unset; 1 on any fail, any non-allowlisted pending, or any 'skip' when EVAL_LIVE_REQUIRED=1); the script already exists in package.json — verified, not re-added"
    - "Operator voice held — eval check `reason` strings + PR-comment copy + code comments use 'scores / counts / reads / asserts / pins / skips' verbs; never 'detects / handles / sorry / want / help / feel'; zero banned compliance strings (npm run check:banned green)"
  artifacts:
    - path: "src/ai/eval/checks/extraction-floor.ts"
      provides: "extractionFloor: EvalCheck; run() loads 5 paste fixtures via the fixtures re-export seam, env-checks ANTHROPIC_API_KEY (absent → 'skip'), runs extractFromPaste({ accountId: <eval-fixed>, paste, founderEmail: '' }) per fixture, counts populated top-level field groups on the returned draft by calling the EXISTING countPopulatedFields(draft) from @/ai/schemas/business-memory.zod (shallow Object.keys(group).length>0 semantics — C1-M2; do NOT re-implement, do NOT harden here), computes mean across 5, returns { status: mean >= 8 ? 'pass' : 'fail', metric: mean, threshold: 8, reason }"
    - path: "src/ai/eval/checks/cache-hit.ts"
      provides: "cacheHit: EvalCheck; run() env-checks isLangfuseConfigured() (false → 'skip'), calls getLangfuseClient()!.fetchTraces({ name: 'agent:<taskClass>' OR filter by name-prefix 'agent:' in code, fromTimestamp: <Date window-start>, fields: 'core,io', limit: <N> }), sums metadata.cacheRead + metadata.inputTokens across the returned traces (the exact keys client.ts writes via trace.update — there are NO GENERATION observations, C1-H2), computes ratio = cacheRead / max(inputTokens, 1), returns { status: ratio > 0 ? 'pass' : 'fail', metric: ratio, threshold: 0, reason }. Docstring states Anthropic-only (Voyage has no prompt cache). NO credential construction outside src/lib/langfuse.ts. Reads ONLY metadata.* — never trace .input/.output."
    - path: "src/ai/eval/types.ts"
      provides: "EvalStatus = 'pending' | 'pass' | 'fail' | 'skip'; 'skip' documented as env-unavailable (fail-open in non-live contexts; FAILS when EVAL_LIVE_REQUIRED==='1'), distinct from 'pending' (not-yet-implemented). EvalCheckResult/EvalCheck/EvalSuiteResult shapes otherwise unchanged (metric?/threshold? already present)."
    - path: "src/ai/eval/runner.ts"
      provides: "runEvalSuite(): adds `const PENDING_ALLOWED = new Set<string>(['qa-grounding'])` (C1-M1); exitCode = 1 if any check status='fail' OR any check status='pending' with id NOT in PENDING_ALLOWED OR (process.env.EVAL_LIVE_REQUIRED==='1' AND any check status='skip') (C1-H1); 'skip' is otherwise non-failing; badge() renders 'skip' (⏭️ skip) and takes EvalStatus (Note B); CLI entrypoint unchanged (ESM isCliEntry)."
    - path: "src/ai/eval/fixtures/index.ts"
      provides: "Re-export seam: exports PASTE_FIXTURE_PATHS (5 absolute paths under tests/ai/fixtures/paste-*.txt) + loadFixtureText(path) helper. ZERO fixture bodies live here — paths point at the canonical tests/ai/fixtures/ files. This is the 'import or re-export, do NOT duplicate' contract from the README. (The INJECTION_FIXTURE_PATH / PII_FIXTURE_PATH exports are DROPPED — they were only for the deferred T04; see FOLLOWUP-SANITIZER-EVAL-01.)"
    - path: ".github/workflows/eval.yml"
      provides: "permissions: { contents: read, pull-requests: write }; a PR eval step `if: ${{ github.event_name == 'pull_request' }}` (no secrets, EVAL_LIVE_REQUIRED unset → live checks env-skip; scoped to PR per C2 NEW-MED so it never double-runs on schedule/dispatch) + a LIVE eval step `if: ${{ github.event_name != 'pull_request' }}` setting EVAL_LIVE_REQUIRED: '1' + ANTHROPIC_API_KEY/Langfuse secrets in its env: (C1-H3); a final actions/github-script@v7 step `if: ${{ always() && github.event_name == 'pull_request' }}` that reads eval-report.json and posts/updates a PR comment with the sanitized Markdown summary (no secrets, no trace/paste/PII content) using the default GITHUB_TOKEN."
    - path: "tests/ai/eval/runner.test.ts"
      provides: "8 cases: the 5 existing (Case 1 baseline, any-fail→1, all-pass→0, all ids present, summary shape) PLUS Case 6 (extraction-floor 'pending' → exit 1, pins the runtime gate) + Case 7 (extraction-floor 'skip' + EVAL_LIVE_REQUIRED unset → exit 0) + Case 8 (extraction-floor 'skip' + EVAL_LIVE_REQUIRED='1' → exit 1, pins C1-H1). Case 1 updated: with the runtime gate live, mock the live checks to 'skip' (their real env-absent behavior in credential-less CI, EVAL_LIVE_REQUIRED unset) to assert the all-skip + qa-grounding-pending baseline → exit 0. Case 4's canonical id list is unconditionally `['cache-hit', 'extraction-floor', 'qa-grounding']`."
    - path: "tests/ai/eval/checks/extraction-floor.test.ts"
      provides: "Unit tests for the field-count + skip logic with extractFromPaste MOCKED (vi.mock) — NO live Anthropic calls: (a) mean ≥ 8 → 'pass' with metric = mean; (b) mean < 8 → 'fail'; (c) ANTHROPIC_API_KEY absent → 'skip' (reason names the var, extractFromPaste NOT called); (d) fixture count is exactly 5 (cross-check vs implementation per Gate 6)."
    - path: "tests/ai/eval/checks/cache-hit.test.ts"
      provides: "Unit tests for the ratio + skip logic with getLangfuseClient MOCKED: (a) metadata.cacheRead>0 → ratio>0 → 'pass'; (b) cacheRead=0 → ratio=0 → 'fail'; (c) isLangfuseConfigured()===false → 'skip', fetchTraces NOT called; (d) the mocked fetchTraces is called with fields including 'io' and a fromTimestamp Date (signature pin)."
  key_links:
    - from: "src/ai/eval/checks/extraction-floor.ts"
      to: "src/ai/agents/extract-from-paste.agent.ts"
      via: "import { extractFromPaste } and call per fixture"
      pattern: "extractFromPaste\\("
    - from: "src/ai/eval/checks/extraction-floor.ts"
      to: "src/ai/eval/fixtures/index.ts"
      via: "import fixture paths (no body duplication)"
      pattern: "PASTE_FIXTURE_PATHS"
    - from: "src/ai/eval/checks/extraction-floor.ts"
      to: "src/ai/schemas/business-memory.zod.ts"
      via: "reuse the existing countPopulatedFields helper"
      pattern: "countPopulatedFields\\("
    - from: "src/ai/eval/checks/cache-hit.ts"
      to: "src/lib/langfuse.ts"
      via: "getLangfuseClient()/isLangfuseConfigured()"
      pattern: "getLangfuseClient\\(\\)|isLangfuseConfigured\\(\\)"
    - from: "src/ai/eval/checks/cache-hit.ts"
      to: "langfuse SDK fetchTraces"
      via: "client.fetchTraces({ name, fromTimestamp, fields: 'core,io', limit })"
      pattern: "fetchTraces\\("
    - from: "src/ai/eval/runner.ts"
      to: "the runtime pending gate"
      via: "PENDING_ALLOWED allowlist (Set<string>)"
      pattern: "PENDING_ALLOWED"
    - from: "src/ai/eval/runner.ts"
      to: "the live-required gate"
      via: "EVAL_LIVE_REQUIRED env signal"
      pattern: "EVAL_LIVE_REQUIRED"
    - from: ".github/workflows/eval.yml"
      to: "PR comment surface"
      via: "actions/github-script reading eval-report.json"
      pattern: "github-script"
---

## Open Decisions for Convergence

Three architectural decisions + one scope flag were surfaced at cycle 1 for the cross-AI convergence cycle and founder adjudication. All four are now RESOLVED below (cycle-2 replan, 2026-05-31). Resolutions are reflected in the tasks.

| ID | Decision | Resolution (implemented below) | Trade-off / why |
|---|---|---|---|
| **OD-1** | Include the 4th "live-Sonnet sanitizer FP/FN" check (T04) now, or defer it? | **DEFERRED per founder ruling 2026-05-31 — T04 removed from 02-05; captured as FOLLOWUP-SANITIZER-EVAL-01.** Codex C1-H4 showed the T04 wiring could not run as written (injection fixtures are short strings that fail `extractFromPaste`'s MIN-length check before the sanitizer ever runs; the PII fixtures are `draftInput` objects for `redactUnrelatedPartyPII`, not pastes). The correct surface is a DIRECT eval over `promptInjectionSanitizer` + `redactUnrelatedPartyPII` — re-authored in a dedicated plan with all 6 standing gates active. | Keeps 02-05 surgical (extraction-floor + cache-hit only, the two checks the founder named for THIS plan). The Phase-3 exit gate (constraints.md: "Langfuse eval harness FP rate <25%") is owned by FOLLOWUP-SANITIZER-EVAL-01 and evaluated before Phase 3 closes. 27 injection + 17 PII fixtures already committed at tests/ai/fixtures/. |
| **OD-2** | Where do the live-AI checks run (every PR vs nightly only) + how is "must run live here" signalled? | **Nightly + manual only, gated by EVAL_LIVE_REQUIRED (refined per C1-H1+H3).** extraction-floor + cache-hit env-skip on `pull_request` (the PR step passes NO secrets and does NOT set EVAL_LIVE_REQUIRED → checks return 'skip', non-failing). They truly execute on `schedule` (UTC 04:00) + `workflow_dispatch` via a separate LIVE step that sets `EVAL_LIVE_REQUIRED: '1'` + the secrets — and in that live-required context a 'skip' is a FAILURE (exit 1), so a creds-misconfigured nightly run is a RED gate, not a silent pass. PR runs post a comment with the latest result or a "live checks run nightly" note. | Running real Sonnet extraction on every PR costs tokens + flakes on 5xx/rate limits. Nightly-only keeps PRs fast + free + deterministic while EVAL_LIVE_REQUIRED closes the fail-open hole C1-H1 found. Cost of the default: a regression merged same-day isn't caught until the next nightly — acceptable for a solo-founder cadence. |
| **OD-3** | How does cache-hit read cache metrics from Langfuse? | **`getLangfuseClient()!.fetchTraces({ name, fromTimestamp, fields: 'core,io', limit })` reading `metadata.cacheRead` / `metadata.inputTokens` (CORRECTED per C1-H2).** The cycle-1 default (`fetchObservations({type:'GENERATION'})`) was WRONG: `src/ai/client.ts:107-156` creates the trace as `name: \`agent:${opts.taskClass}\`` and writes cache metrics via `trace.update({ metadata: { cacheRead: res.usage.cache_read_input_tokens, inputTokens: res.usage.input_tokens, ... } })` — it creates NO GENERATION observations, so the observations query returned no Trochia cache data. `fetchTraces` reads the TRACE metadata bag where the metrics actually live. | Signature pinned against the installed `langfuse@^3.38.20` type defs: `fetchTraces(query?: GetLangfuseTracesQuery)` at `node_modules/langfuse-core/lib/index.d.ts:7364`; the query supports `name`, `fromTimestamp` (typed `Date` after FixTypes), `limit`, `fields` (`trace_list` query, lines 6804-6827). `metadata` is part of the `'io'` field group (the d.ts comment lists `'io' (input, output, metadata)`); omitting `fields` returns all fields including metadata, and a restricted `fields` whitelist must include `'io'` to receive metadata — `fields: 'core,io'` is the chosen restricted value (C2 NEW-LOW). **CRITICAL whitelist:** `io` also returns trace `.input`/`.output` bodies — the executor reads ONLY `metadata.cacheRead`/`metadata.inputTokens` and NEVER touches `.input`/`.output` (G-LANGFUSE-WHITELIST). Fallback: a raw `fetch` to `/api/public/traces` routed through src/lib/langfuse.ts using the existing credentials if the SDK method shape drifts. |
| **OD-A** | env-skip vs the PENDING_ALLOWED runtime gate, and what happens on a LIVE-required run with missing creds. | **Distinct `'skip'` EvalStatus + EVAL_LIVE_REQUIRED live-required gate (refined per C1-H1).** `'skip'` = env-unavailable. It is fail-OPEN (non-failing) ONLY in non-live contexts (PR / local / no creds expected). When `process.env.EVAL_LIVE_REQUIRED === '1'` (set by the nightly/manual LIVE step), a 'skip' becomes a FAILURE (exit 1) — a live run that can't reach its dependency is a red gate. `'pending'` = not-yet-implemented; only `qa-grounding` may be 'pending'. The flipped checks compute status from a variable and never contain the literal `status: 'pending'`, so G-EVAL-1 still returns ≤1 hit (qa-grounding.ts). | Without EVAL_LIVE_REQUIRED, a creds-missing scheduled run would silently exit 0 (the C1-H1 fail-open hole). The signal cleanly separates "can't run here, that's fine" (PR) from "this run was SUPPOSED to reach the dependency and didn't" (nightly). Cost: one env read + one runner branch + one test case. This is the load-bearing decision the other three depend on. |

---

<objective>
Flip the two Phase-2 eval checks the 02-04 scaffold left as `'pending'` stubs into real, dynamically-computed evals, and harden the runner from a process-gate into a runtime-gate with a live-required signal.

- **extraction-floor** scores the real `extractFromPaste` agent against the 5 committed paste fixtures and asserts a mean populated-field floor of ≥ 8 (counted via the existing shallow `countPopulatedFields` helper).
- **cache-hit** reads Anthropic prompt-cache token ratios from Langfuse TRACE metadata (via `fetchTraces`, since `client.ts` writes metrics to trace metadata and creates no GENERATION observations) and asserts the ratio is non-zero.
- **qa-grounding** STAYS `'pending'` — it depends on Plan 02-07's qa-rag agent.
- **FOLLOWUP-EVAL-PENDING-RUNTIME-GATE-01** lands: a runtime allowlist (`PENDING_ALLOWED = new Set<string>(['qa-grounding'])`) so any other check left `'pending'` fails the run.
- **FOLLOWUP-EVAL-PR-COMMENT-01** lands: the PR-comment step the 02-04 plan promised, with `pull-requests: write` re-added, via a two-step PR/LIVE workflow split.
- A new `'skip'` EvalStatus separates env-unavailable (fail-open in non-live contexts) from not-yet-implemented (`'pending'`); an `EVAL_LIVE_REQUIRED` signal turns a 'skip' into a FAILURE on scheduled/manual live runs (OD-A + C1-H1), so live-AI checks degrade cleanly on PRs but cannot silently fail-open on nightly.

The live-Sonnet sanitizer FP/FN check is DEFERRED (founder ruling 2026-05-31, C1-H4/OD-1) → FOLLOWUP-SANITIZER-EVAL-01.

Purpose: turn the 02-04 fail-open scaffold into a real CI exit gate for Phase 2's extraction-floor + cache-hit exit criteria, closing the two FOLLOWUP hardening items the predecessor explicitly routed here.

Output: 2 real eval checks + a runtime-gated, live-required-aware runner + a 'skip' status model + a two-step CI workflow with a PR-comment step + tests. ZERO schema changes, ZERO new npm deps, Anthropic touched only through the chokepoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@tasks/constraints.md
@docs/BRAND.md

# Predecessor close + hand-off (authoritative scope source)
@.planning/phases/02-knowledge-layer/02-04-SUMMARY.md
@docs/Trochia_AI_Handoff_2026-05-30.md

# Standing gates + bounded-bypass + founder-gated review flow (apply at AUTHORING time)
@tasks/lessons.md

# The scaffold being flipped (read in full before editing)
@src/ai/eval/runner.ts
@src/ai/eval/types.ts
@src/ai/eval/checks/extraction-floor.ts
@src/ai/eval/checks/cache-hit.ts
@src/ai/eval/checks/qa-grounding.ts
@src/ai/eval/fixtures/README.md
@tests/ai/eval/runner.test.ts
@.github/workflows/eval.yml

# Code surfaces the real checks call
@src/ai/agents/extract-from-paste.agent.ts
@src/lib/langfuse.ts
@src/ai/client.ts
@src/ai/schemas/business-memory.zod.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + node_modules. Use directly — no exploration. -->

From src/ai/eval/types.ts (current — extend with 'skip'):
```
export type EvalStatus = 'pending' | 'pass' | 'fail';   // → add | 'skip'
export type EvalCheckResult = { id; description; status: EvalStatus; reason: string; metric?: number; threshold?: number };
export type EvalCheck = { id; description; run: () => Promise<EvalCheckResult> };
export type EvalSuiteResult = { checks: EvalCheckResult[]; exitCode: 0 | 1; summary: string };
```

From src/ai/agents/extract-from-paste.agent.ts (the agent extraction-floor calls):
```
export async function extractFromPaste(input: ExtractFromPasteInput): Promise<ExtractFromPasteResult>;
interface ExtractFromPasteInput { accountId: string; paste: string; founderEmail: string }
interface ExtractFromPasteResult { draft: BusinessMemoryDraft; langfuseTraceId: string | null; latencyMs: number; injectionScreen: InjectionSanitizerResult; pii: { redactionsApplied: number; byType: Record<...> } }
// draft top-level slots: companyName, oneLiner, sector, stage, geography, incorporationStatus, foundingDate, team, traction, narrative
// extractFromPaste routes through runAgent (src/ai/client.ts) — the Anthropic chokepoint. DO NOT import @anthropic-ai/sdk in any eval file.
// Paste fixtures are 1,575–2,328 words each; all 5 exceed MIN_PASTE_CHARS (500) and are under MAX_PASTE_CHARS (40,000).
```

From src/ai/schemas/business-memory.zod.ts (REUSE this — do NOT re-implement, do NOT harden, per plan-checker Finding 2 + C1-M2):
```
export function countPopulatedFields(draft: BusinessMemoryDraft): number;
// ACTUAL (shallow) semantics, confirmed by Codex C1-M2: counts the structured groups by `Object.keys(group).length > 0` ONLY.
// It does NOT check that leaves are non-empty — an empty string / empty array inside team/traction/narrative still counts the GROUP as populated.
// extraction-floor's metric is this shallow count averaged over 5 fixtures. Deeper leaf-emptiness checking is OUT OF SCOPE here
// (tracked as a possible future hardening — NOT done in this plan).
```

From src/lib/langfuse.ts (the cache-hit read seam):
```
export function isLangfuseConfigured(): boolean;          // true iff PUBLIC+SECRET+HOST set
export function getLangfuseClient(): Langfuse | null;     // memoized singleton or null
// DO NOT construct Langfuse credentials anywhere else — mirror the chokepoint discipline.
```

From src/ai/client.ts (where cache metrics are emitted — confirms what cache-hit reads; C1-H2):
```
const trace = langfuse?.trace({ name: `agent:${opts.taskClass}`, metadata: { model } }) ?? NOOP_TRACE;   // line 107-110
trace.update({ metadata: { cacheWrite: res.usage.cache_creation_input_tokens, cacheRead: res.usage.cache_read_input_tokens, inputTokens: res.usage.input_tokens, outputTokens: res.usage.output_tokens, model } });   // line 148-156 (and again at 174-183 on repair)
// CONFIRMED: cache metrics are written to TRACE metadata. There are NO GENERATION observations created by runAgent.
// => cache-hit MUST read trace metadata via fetchTraces, NOT fetchObservations({type:'GENERATION'}).
```

From node_modules/langfuse-core/lib/index.d.ts (Gate 4 signature pin — verified at replan time):
```
fetchTraces(query?: GetLangfuseTracesQuery): Promise<GetLangfuseTracesResponse>;              // line 7364
// GetLangfuseTracesQuery (the `trace_list` GET /api/public/traces query, lines 6804-6827) supports:
//   page?, limit?, userId?, name?, sessionId?, fromTimestamp? (Date after FixTypes), toTimestamp?, orderBy?, tags?, version?, release?, environment?, fields?
//   `fields` is a comma-separated list; groups: 'core' (always included), 'io' (input, output, metadata), 'scores', 'observations', 'metrics'.
//   => to RECEIVE trace.metadata you MUST pass fields including 'io' (e.g. 'core,io'). metadata is NOT a standalone group.
// Response: GetLangfuseTracesResponse.data: TraceWithDetails[]; each trace has `metadata?: unknown` (line 961) — read metadata.cacheRead / metadata.inputTokens.
// WHITELIST: 'io' also returns trace.input / trace.output — read ONLY metadata.*; NEVER .input/.output.
```

Fixture inventory (counts pinned at replan time for Gate 6):
```
tests/ai/fixtures/paste-acme-fintech.txt       (1,575 words)
tests/ai/fixtures/paste-helix-saas.txt         (1,706 words)
tests/ai/fixtures/paste-mosaic-marketplace.txt (1,880 words)
tests/ai/fixtures/paste-tributary-healthtech.txt (2,328 words)
tests/ai/fixtures/paste-vanta-hardware.txt     (1,629 words)
→ exactly 5 paste fixtures. extraction-floor mean is over these 5.
tests/ai/fixtures/injection-payloads.json       (27 entries)   ← DEFERRED → FOLLOWUP-SANITIZER-EVAL-01 (NOT used in this plan)
tests/ai/fixtures/pii-fixtures.json             (17 entries)   ← DEFERRED → FOLLOWUP-SANITIZER-EVAL-01 (NOT used in this plan)
```

Installed versions (Gate 1 + Gate 2):
```
vitest ^4.1.6   → NO --repeat flag (use vitest.config `repeats` or serial in-test loop; lessons.md 2026-05-29)
langfuse ^3.38.20
npm script `eval:run` ALREADY EXISTS: "tsx src/ai/eval/runner.ts" (verify, do NOT re-add)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'skip' EvalStatus + PENDING_ALLOWED runtime gate + EVAL_LIVE_REQUIRED live gate to the runner; extend tests (OD-A, C1-H1, C1-M1, FOLLOWUP-EVAL-PENDING-RUNTIME-GATE-01)</name>
  <files>
    - src/ai/eval/types.ts (MODIFY — add 'skip' to EvalStatus)
    - src/ai/eval/runner.ts (MODIFY — PENDING_ALLOWED Set gate; 'skip' non-failing unless EVAL_LIVE_REQUIRED==='1'; badge('skip'))
    - tests/ai/eval/runner.test.ts (MODIFY — keep 5 green; add Case 6 + Case 7 + Case 8; update Case 1 for the live gate)
  </files>
  <read_first>
    - src/ai/eval/types.ts (full — current EvalStatus union)
    - src/ai/eval/runner.ts (full — exitCode logic at lines 31-34, badge() at 60-62, CLI entry 68-81)
    - tests/ai/eval/runner.test.ts (full — the 5 existing cases + the vi.spyOn stub pattern)
    - tasks/lessons.md "Gate 6 — Implementation-vs-assertion cross-check" + the vitest 4.1.6 --repeat entry
  </read_first>
  <action>
    Extend `EvalStatus` to `'pending' | 'pass' | 'fail' | 'skip'`. Document inline that 'skip' = env-unavailable (fail-OPEN in non-live contexts, never blocks PR/local; becomes a FAILURE when EVAL_LIVE_REQUIRED==='1') and 'pending' = not-yet-implemented (only qa-grounding may carry it). This is the OD-A status model + the C1-H1 live-required refinement.

    In `runner.ts`: declare `const PENDING_ALLOWED = new Set<string>(['qa-grounding'])` (a `Set<string>` — NOT `['qa-grounding'] as const` — so `PENDING_ALLOWED.has(r.id)` typechecks against `r.id: string`; this is the C1-M1 fix). Read the live signal once: `const liveRequired = process.env.EVAL_LIVE_REQUIRED === '1'`. Change the exit logic so `exitCode = 1` when ANY check has `status === 'fail'`, OR any check has `status === 'pending'` AND `!PENDING_ALLOWED.has(check.id)`, OR (`liveRequired` AND any check has `status === 'skip'`). When `liveRequired` is false, a `'skip'` is non-failing. Extend `badge()` to render `'skip'` (use a skip glyph + label, e.g. `⏭️ skip`) AND widen its parameter type from the current literal union to `EvalStatus` (plan-checker Note B — so the compiler flags any future status member the badge forgets to handle). Keep the existing JSON-to-stdout, GITHUB_STEP_SUMMARY append, `eval-report.json` write, and the ESM `isCliEntry` entrypoint exactly as-is. Do NOT change the `CHECKS` array in this task (the live checks register their real bodies in Tasks 2-3; the array membership is unchanged — exactly the three checks cache-hit, extraction-floor, qa-grounding).

    Update `tests/ai/eval/runner.test.ts`:
    - Keep Cases 2 (any-fail→1), 3 (all-pass→0), 4 (ids present), 5 (summary shape) green. Case 4's canonical id list is unconditionally `['cache-hit', 'extraction-floor', 'qa-grounding']` (sorted).
    - **Update Case 1**: with the runtime gate live, "all-pending" is no longer the real baseline — extraction-floor + cache-hit will be 'skip' (env-absent) in credential-less CI while qa-grounding stays 'pending'. Rewrite Case 1 to mock extractionFloor + cacheHit `run()` → `'skip'`, ensure `EVAL_LIVE_REQUIRED` is unset for this case, and assert exitCode 0 with qa-grounding 'pending' allowed (the real PR/local CI baseline).
    - **Add Case 6** (pins the pending runtime gate): `vi.spyOn(extractionFloor, 'run').mockResolvedValueOnce({ ..., status: 'pending' })` → assert `exitCode === 1` (a non-allowlisted 'pending' fails).
    - **Add Case 7** (pins 'skip' is non-failing on PR/local): mock extractionFloor → `'skip'`, EVAL_LIVE_REQUIRED unset → assert `exitCode === 0`.
    - **Add Case 8** (pins the C1-H1 live-required gate): mock extractionFloor → `'skip'`, set `process.env.EVAL_LIVE_REQUIRED = '1'` (restore in afterEach) → assert `exitCode === 1` (a skip on a live-required run is a RED gate).
    Gate 6 cross-check: the assertion "non-qa-grounding 'pending' → 1" in Case 6 must match the `PENDING_ALLOWED` membership in the implementation (Set contains exactly `'qa-grounding'`); the Case 8 assertion must match the `liveRequired && status==='skip'` branch.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/eval/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `EvalStatus` union includes `'skip'`; the not-impl-vs-env-unavailable-vs-live-required distinction is documented on the type
    - `runner.ts` contains `const PENDING_ALLOWED = new Set<string>(['qa-grounding'])` (a Set, so `.has(r.id)` typechecks against `r.id: string` — C1-M1)
    - A 'pending' from any check whose id ∉ PENDING_ALLOWED → exitCode 1; a 'skip' is non-failing UNLESS `process.env.EVAL_LIVE_REQUIRED === '1'`, in which case any 'skip' → exitCode 1 (C1-H1)
    - badge() takes `EvalStatus` and renders all four statuses
    - 8/8 runner.test.ts cases pass; Case 6 pins the pending runtime gate, Case 7 pins skip-is-non-failing (EVAL_LIVE_REQUIRED unset), Case 8 pins skip-fails-on-live (EVAL_LIVE_REQUIRED='1')
    - Run `npx vitest run tests/ai/eval/runner.test.ts` (mock-driven, deterministic) for this task's gate, NOT `eval:run` (at this task's close extraction-floor + cache-hit are still 'pending' stubs, so `eval:run` would exit 1 here — expected and correct mid-plan; it goes green again at Task 3 close when the live checks return 'skip'/'pass')
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip extraction-floor.ts to a real eval + fixtures re-export seam (CORE)</name>
  <files>
    - src/ai/eval/fixtures/index.ts (NEW — re-export the 5 paste fixture paths/loader; zero body duplication)
    - src/ai/eval/fixtures/README.md (MODIFY — reflect the re-export seam)
    - src/ai/eval/checks/extraction-floor.ts (MODIFY — flip 'pending' → dynamic pass/fail/skip)
    - tests/ai/eval/checks/extraction-floor.test.ts (NEW — field-count + skip logic, extractFromPaste mocked)
  </files>
  <read_first>
    - src/ai/eval/checks/extraction-floor.ts (full — the docstring codifies the ≥8-field target + threshold 8)
    - src/ai/agents/extract-from-paste.agent.ts (the exported extractFromPaste signature + ExtractFromPasteResult.draft shape + the 10 top-level slots + MIN/MAX paste char bounds)
    - src/ai/schemas/business-memory.zod.ts (the EXISTING `countPopulatedFields(draft)` — REUSE this; do NOT re-implement, do NOT harden; its semantics are shallow `Object.keys(group).length > 0` per C1-M2)
    - src/ai/eval/fixtures/README.md (the "reuse, do NOT duplicate" contract)
    - src/ai/eval/types.ts (post-Task-1, includes 'skip')
    - tasks/lessons.md Gate 6 (implementation-vs-assertion cross-check: fixture count 5, threshold 8)
  </read_first>
  <behavior>
    - Test: with extractFromPaste mocked so countPopulatedFields returns 9 for all 5 fixtures → status 'pass', metric 9, threshold 8.
    - Test: with the mock yielding a count of 6 each → status 'fail', metric 6.
    - Test: mean rounds correctly across mixed fixtures (counts [10,9,8,7,6] → mean 8.0 → 'pass' at the boundary; [9,8,7,7,6] → mean 7.4 → 'fail').
    - Test: ANTHROPIC_API_KEY absent (delete from process.env in the test) → status 'skip', reason names ANTHROPIC_API_KEY, extractFromPaste NOT called.
    - Test: the loader resolves exactly 5 paste fixture paths (Gate 6 count pin).
  </behavior>
  <action>
    Create `src/ai/eval/fixtures/index.ts` as the re-export seam: export `PASTE_FIXTURE_PATHS` (an array of 5 absolute paths to `tests/ai/fixtures/paste-*.txt`, resolved via `node:path` + `import.meta.url` or `process.cwd()` — pick the resolution that works under both `tsx` CLI and vitest), plus `loadFixtureText(path: string): string` using `node:fs`. NO fixture text is copied into this file — it points at the canonical `tests/ai/fixtures/` files (the README's "import or re-export, do NOT duplicate" contract). Do NOT export `INJECTION_FIXTURE_PATH` / `PII_FIXTURE_PATH` — those were only for the deferred T04; add a one-line comment noting the sanitizer eval is deferred to FOLLOWUP-SANITIZER-EVAL-01.

    Update `src/ai/eval/fixtures/README.md`: replace the "placeholder, lands in 02-05" framing with the shipped reality — fixtures are reused from `tests/ai/fixtures/` via `src/ai/eval/fixtures/index.ts`; nothing is duplicated; the directory holds the seam, not data.

    Flip `src/ai/eval/checks/extraction-floor.ts`: keep `id: 'extraction-floor'` and the description. In `run()`:
    1. Env-gate: if `!process.env.ANTHROPIC_API_KEY` → return `{ status: 'skip', reason: 'ANTHROPIC_API_KEY not set — live extraction skipped (env-unavailable, non-blocking)' }` and DO NOT call the agent. (This is the OD-A skip path + the OD-2 PR-event behavior; on a live-required run the runner promotes this 'skip' to a failure.)
    2. Load the 5 paste fixtures via `PASTE_FIXTURE_PATHS` + `loadFixtureText`.
    3. For each, call `extractFromPaste({ accountId: <a fixed eval account id constant>, paste, founderEmail: '' })`. (`founderEmail: ''` is the conservative-default per the agent contract — every email gets redacted; acceptable for eval.)
    4. Count populated top-level field groups on `result.draft` by calling the EXISTING `countPopulatedFields(result.draft)` exported from `@/ai/schemas/business-memory.zod` (plan-checker Finding 2 + C1-M2 — reuse, do NOT re-implement, do NOT harden). NOTE the ACTUAL semantics: that helper counts each structured group by `Object.keys(group).length > 0` ONLY — it is SHALLOW and does not verify leaves are non-empty. The metric is this shallow count averaged over the 5 fixtures. Deeper leaf-emptiness checking is OUT OF SCOPE here.
    5. Compute `mean` = total counted fields / 5. Return `{ status: mean >= 8 ? 'pass' : 'fail', metric: mean, threshold: 8, reason: <names the mean + per-fixture counts, NO draft content> }`.
    NEVER put draft content, paste content, or source_snippet text into `reason` or any log — only counts. Do NOT import `@anthropic-ai/sdk` (the agent already routes through the chokepoint). NEVER inline a fenced implementation in this plan — this is directive prose.

    Author `tests/ai/eval/checks/extraction-floor.test.ts` per `<behavior>` with `vi.mock('@/ai/agents/extract-from-paste.agent', ...)` so NO live Anthropic call fires in the unit test.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/eval/checks/extraction-floor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/eval/fixtures/index.ts` exports exactly 5 paste fixture paths; zero fixture bodies are duplicated into src/ai/eval/fixtures/; no INJECTION_FIXTURE_PATH / PII_FIXTURE_PATH exports (deferred to FOLLOWUP-SANITIZER-EVAL-01)
    - extraction-floor.ts contains NO literal `status: 'pending'`; status is computed from `mean >= 8`
    - the field count is produced by reusing `countPopulatedFields` (grep-verifiable); the plan/code wording reflects its ACTUAL shallow `Object.keys(group).length>0` semantics (C1-M2); threshold is 8 and metric is the mean; both match the test assertions (Gate 6)
    - ANTHROPIC_API_KEY-absent path returns 'skip' and never calls extractFromPaste (asserted with the mock's call count = 0)
    - No `@anthropic-ai/sdk` import in extraction-floor.ts (grep clean)
    - Unit test green with extractFromPaste mocked (no live calls)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip cache-hit.ts to a real eval via Langfuse fetchTraces + wire the two-step CI workflow + PR-comment step (FOLLOWUP-EVAL-PR-COMMENT-01, OD-2, OD-3, C1-H2, C1-H3)</name>
  <files>
    - src/ai/eval/checks/cache-hit.ts (MODIFY — flip 'pending' → dynamic pass/fail/skip via fetchTraces)
    - tests/ai/eval/checks/cache-hit.test.ts (NEW — ratio + skip logic, getLangfuseClient/fetchTraces mocked)
    - .github/workflows/eval.yml (MODIFY — re-add pull-requests: write; split PR step / LIVE step; add github-script PR-comment step)
  </files>
  <read_first>
    - src/ai/eval/checks/cache-hit.ts (full — docstring codifies ratio>0 target, Anthropic-only)
    - src/lib/langfuse.ts (full — getLangfuseClient/isLangfuseConfigured contract; the only client-construction site)
    - src/ai/client.ts lines 107-156 (the trace creation `name: agent:${taskClass}` + trace.update cacheRead/inputTokens metadata emission — confirms what to read; there are NO GENERATION observations, C1-H2)
    - node_modules/langfuse-core/lib/index.d.ts:7364 (fetchTraces signature) + 6804-6827 (the trace_list query params incl. name, fromTimestamp, limit, fields) + 961 (trace.metadata) — Gate 4 pin
    - .github/workflows/eval.yml (full — current permissions block + the dropped pull-requests:write comment)
    - tasks/lessons.md "Founder-gated review + deploy flow" (the executor does not trigger /codex,/cso, or deploy)
  </read_first>
  <behavior>
    - Test: getLangfuseClient mocked to return a client whose fetchTraces resolves traces whose metadata sums cacheRead=1200, inputTokens=4000 → ratio 0.3 → 'pass', threshold 0.
    - Test: cacheRead=0 across all traces → ratio 0 → 'fail'.
    - Test: isLangfuseConfigured() === false → 'skip', reason names the unconfigured client, fetchTraces NOT called.
    - Test: fetchTraces is invoked with a query whose `fields` includes 'io' and whose `fromTimestamp` is a Date (signature/intent pin — Gate 4).
  </behavior>
  <action>
    Flip `src/ai/eval/checks/cache-hit.ts`: keep `id: 'cache-hit'` + description. Docstring states Anthropic-only (Voyage exposes no prompt cache). In `run()`:
    1. Env-gate: if `!isLangfuseConfigured()` → return `{ status: 'skip', reason: 'Langfuse not configured — cache-hit read skipped (env-unavailable, non-blocking)' }`; do NOT call fetchTraces.
    2. `const client = getLangfuseClient()` (null-guard; if somehow null after the configured check, also 'skip').
    3. Call `client.fetchTraces({ name: 'agent:<the cached production taskClass>', fromTimestamp: <a Date of now minus a bounded window, e.g. 7 days>, fields: 'core,io', limit: <N, e.g. 100> })`. `client.ts` names traces `agent:${opts.taskClass}` (line 109). EITHER filter by the specific `agent:<taskClass>` the cached production path uses (preferred — pass `name` directly), OR fetch recent `agent:*` traces with no `name` filter and filter in code by the `'agent:'` name-prefix; state which in a code comment. `metadata` is returned by default when `fields` is OMITTED; only when restricting `fields` to a whitelist must that whitelist include `'io'` (the d.ts groups input/output/metadata under `'io'`), so `fields: 'core,io'` is the correct restricted value to still receive `metadata` (C2 NEW-LOW — `'io'` is not unconditionally required, only required under a restricted whitelist). This corrects the cycle-1 `fetchObservations({type:'GENERATION'})` default, which returned no Trochia cache data because `client.ts` writes metrics to TRACE metadata and creates NO GENERATION observations (C1-H2).
    4. Sum `metadata.cacheRead` (cache_read_input_tokens) and `metadata.inputTokens` across the returned traces (the exact keys client.ts writes via `trace.update`). `ratio = cacheRead / Math.max(inputTokens, 1)`.
    5. Return `{ status: ratio > 0 ? 'pass' : 'fail', metric: ratio, threshold: 0, reason: <names the ratio + trace count, NO trace input/output bodies> }`.
    WHITELIST (G-LANGFUSE-WHITELIST): `fields: 'core,io'` ALSO returns each trace's `.input`/`.output` bodies — read ONLY `metadata.cacheRead`/`metadata.inputTokens`; NEVER read or surface trace `.input`/`.output`, chunk_text, paste content, PII, query content, or credentials. NEVER construct Langfuse credentials outside `src/lib/langfuse.ts`. NO new npm dep — use the installed `langfuse` SDK. Fallback (note only): a raw `fetch` to `/api/public/traces` routed through src/lib/langfuse.ts if the SDK method shape drifts.

    Author `tests/ai/eval/checks/cache-hit.test.ts` per `<behavior>` with `vi.mock('@/lib/langfuse', ...)` providing a mock client whose `fetchTraces` returns traces with `metadata` bags.

    Edit `.github/workflows/eval.yml` (C1-H3 — `if:` under `env:` is INVALID Actions YAML; use a two-step structure instead):
    - Re-add `pull-requests: write` under `permissions` (FOLLOWUP-EVAL-PR-COMMENT-01); keep `contents: read`; update the comment that previously explained the removal.
    - **PR eval step** `if: ${{ github.event_name == 'pull_request' }}` (C2 NEW-MED — scope it to PR so scheduled/manual runs do NOT execute a redundant non-live skip-pass before the LIVE pass): runs `npm run eval:run` with NO ANTHROPIC_API_KEY / Langfuse secrets in its `env:` and WITHOUT setting `EVAL_LIVE_REQUIRED`. The live checks env-skip (return 'skip', non-failing). This is the fast/free PR path.
    - **LIVE eval step** `if: ${{ github.event_name != 'pull_request' }}`: runs `npm run eval:run` with `env:` setting `EVAL_LIVE_REQUIRED: '1'` + `ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}` + the Langfuse secrets. On schedule + workflow_dispatch the live checks truly run; a 'skip' here is a FAILURE (C1-H1 / OD-A). Document the nightly cadence in a workflow comment.
    - **PR-comment step** `if: ${{ always() && github.event_name == 'pull_request' }}` using `actions/github-script@v7` with the default `GITHUB_TOKEN` (the `pull-requests: write` permission): read `eval-report.json` and post (or update, by a marker string, to avoid comment spam) a PR comment containing ONLY the eval summary table (each check id, status badge, sanitized `reason`, `metric`, `threshold`, exit code) and a "live checks run nightly" note. The comment body MUST NEVER echo secrets, env values, or trace/paste/PII content. Add a guard comment in the workflow naming this whitelist.
    - DO NOT trigger /codex, /cso, or any deploy — that is founder-owned (lessons.md founder-gated flow). Leave the branch review-ready.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/eval/checks/cache-hit.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cache-hit.ts contains NO literal `status: 'pending'`; status is computed from `ratio > 0`
    - cache-hit reads via `fetchTraces({ ..., fields: 'core,io' })` and sums `metadata.cacheRead`/`metadata.inputTokens` (NOT fetchObservations — C1-H2); threshold is 0 and metric is the ratio (Gate 6 cross-check vs test)
    - isLangfuseConfigured()===false → 'skip', fetchTraces not called (mock call count 0)
    - fetchTraces is called with `fields` including 'io' and a `fromTimestamp` Date (signature pin asserted)
    - cache-hit reads ONLY metadata.* — no access to trace .input/.output (grep clean); no `@anthropic-ai/sdk` import; no Langfuse client constructed outside src/lib/langfuse.ts (grep clean)
    - eval.yml has `pull-requests: write` AND a two-step PR/LIVE split (PR step `if: github.event_name == 'pull_request'`: no secrets, no EVAL_LIVE_REQUIRED; LIVE step `if: event != pull_request` sets EVAL_LIVE_REQUIRED=1 + secrets — the two `if:` conditions are mutually exclusive so exactly one eval pass runs per event, C2 NEW-MED) AND a github-script PR-comment step `if: always() && pull_request`; the comment whitelist excludes secrets + trace/paste/PII content; the YAML is valid (no `if:` under `env:`)
    - `npx vitest run tests/ai/eval/checks/cache-hit.test.ts` green; `tsc --noEmit` clean
    - `grep -rn "status: 'pending'" src/ai/eval/checks/` returns exactly 1 hit (qa-grounding.ts) at this task's close
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| `.github/workflows/eval.yml` → repo secrets | The LIVE eval step receives ANTHROPIC_API_KEY + Langfuse creds ONLY when `github.event_name != 'pull_request'` (schedule + workflow_dispatch, OD-2); the PR eval step gets NO secrets; never echoed to logs or PR comments |
| eval check → extractFromPaste → runAgent → Anthropic | Live Sonnet calls (extraction-floor); routed through the chokepoint; no direct SDK import; paste fixtures are public-safe synthetic content |
| cache-hit → src/lib/langfuse.ts → Langfuse public API | Read-only fetchTraces; reads ONLY metadata.cacheRead/inputTokens token counts; never reads/surfaces trace input/output bodies (even though fields:'core,io' returns them); credentials stay inside src/lib/langfuse.ts |
| eval runner → eval-report.json → PR comment | The github-script step reads eval-report.json and posts a comment; the report + comment carry only check id/status/sanitized-reason/metric/threshold — no secrets, no trace/paste/PII content |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-02-05-01 | Information Disclosure | PR comment echoes a secret, env value, or trace/paste/PII content | mitigate | The github-script comment body is constructed from a hard whitelist (id, status badge, sanitized reason, metric, threshold, exitCode) plus a "live checks run nightly" note; `reason` strings are authored to carry only counts/ratios; the PR-comment step uses only the default GITHUB_TOKEN and never reads secrets; a workflow comment names the whitelist; /cso pins this at close (founder-triggered) |
| T-02-05-02 | Information Disclosure | cache-hit reads a Langfuse trace `input`/`output` body containing chunk_text or founder query content (fields:'core,io' DOES return them) | mitigate | cache-hit reads ONLY `metadata.cacheRead`/`metadata.inputTokens` off each trace; it never accesses `.input`/`.output`; the docstring + a code comment pin the read whitelist; a grep gate asserts no `.input`/`.output` access; carries forward the 02-04 Langfuse whitelist invariant |
| T-02-05-03 | Information Disclosure | Langfuse credentials leak via a second construction site | mitigate | getLangfuseClient()/isLangfuseConfigured() from src/lib/langfuse.ts are the ONLY access path; no `new Langfuse(...)` and no raw-fetch credential assembly in any eval file; grep gate G-LANGFUSE-WHITELIST |
| T-02-05-04 | Elevation of Privilege | `pull-requests: write` re-added — a malicious fork PR abuses the token to write to the repo or exfiltrate secrets (C3-MEDIUM corrected the earlier inaccurate "no checkout before it" claim — the PR eval step DOES run PR-branch repo code in the same job before the comment step) | mitigate | Layered, accurate to the single-job structure: (1) GitHub's fork-PR model makes `GITHUB_TOKEN` READ-ONLY on `pull_request` from a fork REGARDLESS of the `permissions:` block (only `pull_request_target` — which this workflow does NOT use — would grant write in fork context); same-repo PRs get the `pull-requests: write` to post the comment. (2) The PR eval step runs with NO secrets in its `env:` and does NOT set EVAL_LIVE_REQUIRED, so even malicious PR-branch code has no ANTHROPIC_API_KEY / Langfuse creds to exfiltrate; the LIVE step that holds secrets is `if: github.event_name != 'pull_request'` and never runs on PRs (OD-2). (3) The comment step reads ONLY the whitelisted fields of eval-report.json (id/status/sanitized-reason/metric/threshold), never secrets or trace/paste/PII content. Residual risk (a fork PR posting a comment on its own same-repo PR is not possible; on a fork the token is read-only) is accepted. If stricter isolation is ever wanted, split the comment into a separate no-checkout job — noted, not required here. /cso pins this at close (founder-triggered) |
| T-02-05-05 | Denial of Service | extraction-floor (5 Sonnet calls) + cache-hit run on every PR, burning tokens / hitting rate limits | mitigate | OD-2: live-AI checks env-skip on pull_request (the PR step passes no secrets + no EVAL_LIVE_REQUIRED) and run only on schedule (nightly) + workflow_dispatch; PR runs are free + fast; the per-user $5/day cost cap (PULL-OBS-COST-01-FORWARD, Plan 02-07) is the production throttle; eval is CI-account scoped, not founder-tenant scoped |
| T-02-05-06 | Tampering | A future check is left `'pending'` and silently fails open in CI | mitigate | FOLLOWUP-EVAL-PENDING-RUNTIME-GATE-01 lands: PENDING_ALLOWED runtime gate (Set<string>) forces exit 1 on any non-qa-grounding 'pending'; runner.test.ts Case 6 pins it; the G-EVAL-1 grep gate pins ≤1 'pending' literal |
| T-02-05-07 | Spoofing | Eval reports a false 'pass' on a scheduled run because a live check silently env-skipped (creds misconfigured) and the skip was fail-open | mitigate | C1-H1: the LIVE step sets EVAL_LIVE_REQUIRED=1; the runner promotes any 'skip' to a FAILURE (exit 1) when EVAL_LIVE_REQUIRED==='1', so a creds-misconfigured nightly run is a RED gate, not a silent pass; runner.test.ts Case 8 pins it. Each check's env-skip is explicit (returns 'skip', not 'pass'); a thrown error inside run() propagates rather than defaulting to pass; the fail-CLOSED contract (any 'fail' → exit 1) is preserved |
| T-02-05-SC | Tampering | npm supply-chain risk on new deps | accept | This plan adds ZERO new npm dependencies. The Langfuse read uses the already-installed `langfuse@^3.38.20` `fetchTraces`; the extractor + fixtures already exist; fixture loading via `node:fs`. If a new dep IS proposed at any point, the Package Legitimacy Gate applies and the close surfaces it explicitly. Block-on: high. |

Block-on: high (security_gate: cso; /codex + /cso are founder-triggered at close per lessons.md founder-gated flow).

</threat_model>

<deferred_items>

Explicit list of items NOT in scope of this plan — captured for SUMMARY hand-off.

## Deferred per founder ruling 2026-05-31 (C1-H4 / OD-1)

1. **FOLLOWUP-SANITIZER-EVAL-01** — a DIRECT eval over `promptInjectionSanitizer` + `redactUnrelatedPartyPII` (NOT via the paste path). Codex C1-H4 proved the originally-planned T04 wiring could not run: the 27 injection fixtures are short strings that fail `extractFromPaste`'s MIN_PASTE_CHARS (500) length check BEFORE the sanitizer runs, and the 17 PII fixtures are `draftInput` objects for `redactUnrelatedPartyPII`, not pastes. The correct check calls the sanitizer + redactor functions directly, using each fixture's `expected*` oracle fields, asserting `max(FP, FN) < 0.25`. This FOLLOWUP OWNS the Phase-3 exit gate "Langfuse eval harness FP rate <25%" (tasks/constraints.md) and must be evaluated before Phase 3 closes. 27 injection + 17 PII fixtures already committed at `tests/ai/fixtures/`. Re-author in a dedicated plan with all 6 standing gates active from word one. (Net effect on 02-05: T04 removed; `sanitizer-fp-fn.ts` + its test are NOT created; `index.ts` drops the INJECTION_FIXTURE_PATH / PII_FIXTURE_PATH exports.)

## Deferred to Plan 02-07 (Q&A sidebar + qa-rag + cost cap — the joint-merge plan)

1. Real implementation of `qa-grounding.ts` eval check — STAYS `'pending'` here; depends on Plan 02-07's qa-rag agent landing. It is the single permitted `'pending'` (PENDING_ALLOWED). When qa-rag ships, 02-07 flips it AND removes `'qa-grounding'` from PENDING_ALLOWED so the allowlist empties.
2. **PULL-OBS-COST-01-FORWARD** — OBS-COST-01 ($5/user/day cap, Anthropic + Voyage) ships in the same merge that takes the write path to prod. Not closed here.

## Deferred to Plan 02-06 (RAG retrieve — hybrid pgvector + FTS)

1. `src/ai/rag/retrieve.ts` hybrid retriever; the FTS GENERATED column + GIN index migration (the one schema change Phase 2 sees post-02-01). NOT in this plan — this plan adds ZERO migrations.

## Still-open follow-ups this plan does NOT close (carry forward)

- **FOLLOWUP-NODE-VERSION-SKEW-01** — Vercel Node 22.x vs CI Node 24; align before first prod merge. (eval.yml stays on Node 24; flag carries.)
- **FOLLOWUP-SOURCEMAP-PROD-PUBLIC-HARDENING-01** — confirm prod source maps go to Sentry, not public, before first prod merge. (This plan adds no source-map surface — eval code is CI/server-side.)
- **FOLLOWUP-CORPUS-SYNC-01** — corpus-sync Inngest function; own plan; all 6 gates at authoring time.
- **FOLLOWUP-CORPUS-01** — expand curated corpus 5 → 50 docs.
- **FOLLOWUP-DRIZZLE-TYPE-ANNOTATION-01** — `.$type<T>()` on jsonb columns post-schema-lock-release. (N/A here — no schema/jsonb-read code in this plan; Gate 5 is N/A, stated in the gate list.)
- **FOLLOWUP-VOYAGE-TIMEOUT-TEST** — real-timer abort test when voyage.adapter.ts is next edited. (Not edited here.)
- **FOLLOWUP-DBDIFF-01** — reusable `db:diff` in the first plan that legitimately modifies schema. (Not this one.)
- **FOLLOWUP-HARDCODED-DOMAIN-REGEX-01** — `trochia.asranest` regex over-fire; route through CCO before tightening. (Bounded-bypass D4 carve-out still covers these two pre-existing test failures if they surface in the pre-push gate.)
- **Tokenizer swap** — replace heuristic char chunker with `@anthropic-ai/tokenizer` if eval shows drift. (extraction-floor's field-count is independent of token counting, so it does not force this swap; if cache-hit ratios or extraction quality reveal drift, log it for the swap decision.)
- **P4.5-POLISH-07/08/09**, **Test-user-mint helper** — Phase 4.5 backlog.

## Out of scope of Phase 2 entirely

- EU residency / multi-region embeddings — V2.
- Deck-reviewer-specific PITCH-04 thresholds (median review <90s, 5–15 issues, zero fabricated slide refs) — Phase 3. This plan flips only the two shared Phase-2 checks (extraction-floor + cache-hit) on the same harness.

</deferred_items>

<success_criteria>

Plan 02-05 ships when ALL of the following are TRUE:

1. All 3 tasks complete (T01-T03, all CORE) with per-task `<acceptance_criteria>` satisfied
2. All 17 `must_haves.truths` verified
3. All `must_haves.artifacts` present at documented paths with documented exports
4. All 8 `must_haves.key_links` grep-verifiable in source
5. G-EVAL-1: `grep -rn "status: 'pending'" src/ai/eval/checks/` returns ≤ 1 hit AND it is qa-grounding.ts
6. G-SCHEMA-LOCK: `git diff --quiet 29228e8 -- src/db/schema/` exits 0; zero migrations
7. G-NO-NEW-DEPS: package.json deps unchanged (scripts-only at most); langfuse read uses installed SDK
8. G-CHOKEPOINT: no eval file imports `@anthropic-ai/sdk`; ESLint boundary green
9. G-BANNED-STRINGS: `npm run check:banned` green; operator voice held
10. G-RUNNER-CONTRACT: `npm run eval:run` exits per contract (incl. the EVAL_LIVE_REQUIRED live-required gate); `npx vitest run tests/ai/eval/` green
11. /codex verdict: APPROVED (founder-triggered at close)
12. /cso verdict: APPROVED (founder-triggered at close)
13. CI green on PR #7 at HEAD (both the PR step and, on a manual dispatch, the LIVE step)
14. `02-05-SUMMARY.md` written with hand-off to Plan 02-06 + Plan 02-07 + the OD adjudications recorded (incl. the OD-1 defer → FOLLOWUP-SANITIZER-EVAL-01)
15. `tasks/lessons.md` appended (the 'skip' vs 'pending' vs EVAL_LIVE_REQUIRED status model + the Langfuse fetchTraces signature pin — metadata lives in the 'io' field group, client.ts emits no GENERATION observations)

</success_criteria>

<plan_checker_pre_dispatch>

Run `/plan-checker` against this plan-doc before dispatch. Mandatory gates:

| ID | Check | Expected |
|---|---|---|
| **G-EVAL-1** (MANDATORY, from handoff) | After 02-05 ships, `grep -rn "status: 'pending'" src/ai/eval/checks/` returns ≤ 1 hit AND that single hit is `qa-grounding.ts`. The plan must show extraction-floor + cache-hit compute status from a variable and contain no literal `status: 'pending'`. | PASS |
| **G-SCHEMA-LOCK** | `git diff --quiet 29228e8 -- src/db/schema/` exits 0; no new migration filename; no ALTER; zero schema files in files_modified. | PASS |
| **G-CHOKEPOINT** | No eval file imports `@anthropic-ai/sdk`. extraction-floor exercises Anthropic ONLY via `extractFromPaste` → `runAgent` → `src/ai/client.ts`. Voyage not involved. `grep -rn "@anthropic-ai/sdk" src/ai/eval/` returns 0 hits. | PASS |
| **G-LANGFUSE-WHITELIST** | cache-hit's Langfuse read NEVER logs/returns/PR-comments chunk_text, paste content, PII, founder query content, or trace input/output bodies — only `metadata.cacheRead`/`metadata.inputTokens` token counts. It reads via `fetchTraces({fields:'core,io'})` and accesses ONLY `metadata.*`, never `.input`/`.output` (even though 'io' returns them). No `new Langfuse(...)` or raw-fetch credential assembly outside `src/lib/langfuse.ts`. | PASS |
| **G-NO-NEW-DEPS** | No new npm dependency. package.json diff is scripts-only at most (eval:run already exists — not re-added). The Langfuse read uses the installed `langfuse@^3.38.20` `fetchTraces`. | PASS |
| **G-BANNED-STRINGS** | `npm run check:banned` passes; Trochia operator voice held in plan + code comments + reason strings + PR-comment copy (no feel/help/want/sorry/detects). | PASS |
| **G-RUNNER-CONTRACT** | `npm run eval:run` exits per contract (0 on all-pass/skip + qa-grounding-pending when EVAL_LIVE_REQUIRED unset; 1 on any fail, any non-allowlisted pending, OR any 'skip' when EVAL_LIVE_REQUIRED=1); runner.test.ts stays green + Case 6 (non-qa-grounding 'pending' → exit 1) + Case 7 ('skip' + EVAL_LIVE_REQUIRED unset → exit 0) + Case 8 ('skip' + EVAL_LIVE_REQUIRED=1 → exit 1). | PASS |
| **Gate 1 — npm-script existence** (lessons.md) | `eval:run` resolves in package.json (it does — `"tsx src/ai/eval/runner.ts"`). No new npm script invented without adding it. | PASS |
| **Gate 2 — CLI-flag existence in installed versions** (lessons.md) | vitest is `^4.1.6` — NO `--repeat` flag. No task uses `--repeat`; any determinism loop is serial in-test or `vitest.config` `repeats`. All verify commands use plain `npx vitest run <path>`. | PASS |
| **Gate 4 — external-library SIGNATURE consistency** (lessons.md) | Langfuse `fetchTraces(query?: GetLangfuseTracesQuery)` verified at `node_modules/langfuse-core/lib/index.d.ts:7364`; the trace_list query (lines 6804-6827) supports `name`, `fromTimestamp` (Date after FixTypes), `limit`, `fields`; `metadata` requires `fields` to include `'io'`; the cache-hit call shape (`{ name, fromTimestamp: <Date>, fields: 'core,io', limit }`) matches. `extractFromPaste(input: ExtractFromPasteInput)` single-arg shape matches `src/ai/agents/extract-from-paste.agent.ts`. The cycle-1 `fetchObservations({type:'GENERATION'})` shape is REMOVED (C1-H2 — client.ts emits no GENERATION observations). | PASS |
| **Gate 5 — Drizzle column-type-annotation completeness** (lessons.md) | N/A — this plan touches NO Drizzle schema and reads NO jsonb columns (eval reads agent return values + Langfuse trace metadata, not DB rows). Stated explicitly. | N/A |
| **Gate 6 — implementation-vs-assertion cross-check** (lessons.md) | Every counted invariant matches in BOTH implementation and test: extraction-floor fixture count = 5, threshold = 8, metric via reused `countPopulatedFields` (shallow); cache-hit threshold = 0; PENDING_ALLOWED is a `Set<string>` containing exactly `'qa-grounding'`; EVAL_LIVE_REQUIRED='1' → skip→fail. | PASS |
| **G-OD-PINNED** | The four Open Decisions are recorded at top-of-file with their RESOLUTIONS: OD-1 (T04 DEFERRED → FOLLOWUP-SANITIZER-EVAL-01), OD-2 (nightly-only live checks via EVAL_LIVE_REQUIRED + two-step workflow), OD-3 (fetchTraces reading metadata.* — corrected from fetchObservations), OD-A ('skip' status model + live-required-fails-on-skip). | PASS |

Only after `/plan-checker` returns APPROVED, dispatch T01.

</plan_checker_pre_dispatch>

<output>

Create `.planning/phases/02-knowledge-layer/02-05-SUMMARY.md` when Plan 02-05 is closed; use `$HOME/.claude/get-shit-done/templates/summary.md` (this $HOME path is correct + expected for the Claude Code executor — the GSD summary template legitimately lives there; C1-L1 flagged it only as an artifact of Codex's own review-time filesystem boundary, no change required). Record the OD-1/OD-2/OD-3/OD-A adjudications (what the founder + convergence cycle settled, incl. the T04 defer → FOLLOWUP-SANITIZER-EVAL-01), the hand-off to Plan 02-06 + Plan 02-07 (qa-grounding flip + PENDING_ALLOWED emptying when qa-rag lands), and this `<deferred_items>` block.

</output>
