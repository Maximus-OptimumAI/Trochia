---
phase: 02-knowledge-layer
plan: 01
type: execute
wave: 1
week: 1
depends_on: []
files_modified:
  - src/db/schema/memory.ts
  - src/db/schema/timeline.ts
  - src/db/schema/embeddings.ts
  - src/db/schema/index.ts
  - src/db/rls.ts
  - src/db/migrations/*
  - drizzle.config.ts
  - tests/rls/schema-scan.test.ts
  - tests/rls/two-user-isolation.test.ts
  - tests/integration/rls-memory.test.ts
autonomous: false
requirements: [KNW-01, KNW-02, KNW-03, KNW-04, KNW-05, KNW-08, XC-08]
strategic_tickets: [KNW-01a, KNW-01b, XC-08a]
must_haves:
  truths:
    - "Drizzle schema files exist for memory (business_memory + pipeline_entry + interaction), timeline (timeline_event), and embeddings (embeddings + HNSW index)"
    - "Every new tenant-scoped table has RLS enabled with default-deny + tenant_isolation policy co-located in the schema file"
    - "src/db/rls.ts continues to be the single source of truth for tenant_isolation policy helpers; no inline policy logic in schema files"
    - "schema-scan CI test catches all five new tables and fails on any future table lacking RLS+policy (using the Phase 1 NON_TENANT_TABLES allowlist constant — no new entries needed; embeddings is tenant-scoped)"
    - "two-user-isolation test extended to cover all five new tables — tenant A reads zero of tenant B's rows in every new table"
    - "Provenance JSON columns on business_memory carry source_snippet + confidence + extracted_at + last_updated + snooze_until — the schema is locked here so later weeks add NO new columns, only update provenance values"
    - "embeddings table has the HNSW index on embedding vector(1024) with cosine operator class; vector dim matches Voyage voyage-3-large output"
    - "embedding_model_version is a NOT NULL text column on embeddings; tenant_id + source_type + source_id is uniquely indexed for idempotent upserts"
    - "timeline_event has source_module enum (memory|pipeline|deck|brief|safe|cap_table|legal — pre-seeded for downstream phases) + source_id + event_type text + event_at + founder_visible bool"
    - "Migration produced by drizzle-kit generate runs against staging Supabase; npm run db:push succeeds (autonomous: false — runs against live DB)"
    - "npm run lint + npm run typecheck + npm run check:banned + npm run build all exit 0"
  artifacts:
    - path: "src/db/schema/memory.ts"
      provides: "business_memory (tenant-scoped, jsonb provenance), pipeline_entry (tenant-scoped, surface shape only — Phase 4 fills CRUD), interaction (tenant-scoped, captures Q&A queries + answers + citations for audit); RLS policies co-located"
    - path: "src/db/schema/timeline.ts"
      provides: "timeline_event (tenant-scoped, source_module enum, source_id, event_type, event_at, founder_visible); RLS policy co-located"
    - path: "src/db/schema/embeddings.ts"
      provides: "embeddings (tenant-scoped, source_type enum [memory|corpus|interaction], source_id, chunk_text, chunk_idx, embedding vector(1024), embedding_model_version, embedded_at); HNSW index on embedding with cosine operator class; tenant_id + source_type + source_id + chunk_idx unique index; RLS policy co-located"
    - path: "src/db/schema/index.ts"
      provides: "barrel re-exports the three new schema files alongside Phase 1's tenancy/billing/jobs/legal"
    - path: "tests/integration/rls-memory.test.ts"
      provides: "two-user isolation integration test covering business_memory, pipeline_entry, interaction, timeline_event, embeddings — extends the Phase 1 two-user pattern from tests/rls/two-user-isolation.test.ts to the new tables"
    - path: "src/db/migrations/0XXX_phase2_memory.sql"
      provides: "single migration that CREATE TABLE + ENABLE ROW LEVEL SECURITY + CREATE POLICY tenant_isolation_X + CREATE INDEX (HNSW on embeddings) + grant tables-to-authenticated; reviewed manually before db:push"
  key_links:
    - from: "src/db/schema/memory.ts"
      to: "src/db/rls.ts"
      via: "import { tenantIsolationPolicy }"
      pattern: "tenantIsolationPolicy"
    - from: "src/db/schema/timeline.ts"
      to: "src/db/rls.ts"
      via: "import { tenantIsolationPolicy }"
      pattern: "tenantIsolationPolicy"
    - from: "src/db/schema/embeddings.ts"
      to: "src/db/rls.ts"
      via: "import { tenantIsolationPolicy }"
      pattern: "tenantIsolationPolicy"
    - from: "src/db/migrations/0XXX_phase2_memory.sql"
      to: "Postgres pgvector"
      via: "USING hnsw (embedding vector_cosine_ops)"
      pattern: "USING hnsw"
    - from: "tests/integration/rls-memory.test.ts"
      to: "src/db/schema/*"
      via: "iterates new tenant-scoped tables"
      pattern: "business_memory|pipeline_entry|interaction|timeline_event|embeddings"
---

<objective>
Lay the schema spine for Phase 2: five new tenant-scoped tables (`business_memory`, `pipeline_entry`, `interaction`, `timeline_event`, `embeddings`) with RLS default-deny, provenance JSON columns on `business_memory`, the HNSW index on `embeddings`, and an integration test that proves tenant isolation across all five. **Schema is locked at the end of this plan** — later weeks add NO new columns to these tables, only provenance value mutations (Pitfall 11 mitigation from PLAN v1 §6).

This plan delivers strategic tickets **KNW-01a + KNW-01b + XC-08a** consolidated (PLAN v1 §7 Week 1). Canonical requirement IDs supported: foundation for **KNW-01, KNW-04, KNW-05, KNW-08, XC-08** (each filled in later weeks).

`autonomous: false` because `db:push` runs against the live staging Supabase project and Drizzle migrations sometimes prompt for destructive-change confirmation.

Output: schema files exist, RLS is enforced and CI-tested, the migration has been reviewed and pushed, two-user isolation is green on all five new tables. No application code consumes these tables yet — that lands in Week 2+.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-layer/02-CONTEXT.md
@.planning/phases/02-knowledge-layer/02-PLAN.md
@docs/Trochia_AI_Phase_2_PLAN_v1.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-03-PLAN.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@tasks/constraints.md
@tasks/lessons.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 shipped these. DO NOT reshape them:
- src/db/schema/{tenancy,billing,jobs,legal}.ts — existing tables
- src/db/schema/index.ts — barrel; add 3 new export lines
- src/db/rls.ts — exports tenantIsolationPolicy + NON_TENANT_TABLES constant (the schema-scan allowlist)
- src/db/client.ts + src/db/region.ts — DB client factories (don't touch)
- tests/rls/schema-scan.test.ts — already iterates schema; will auto-pick up the new tables; assert they pass
- tests/rls/two-user-isolation.test.ts — Phase 1 two-user pattern; this plan EXTENDS to cover new tables in tests/integration/rls-memory.test.ts (separate file to keep Phase 1 test untouched)
- drizzle.config.ts — already points at src/db/schema/; no changes
- pgvector extension — already enabled on Supabase in Phase 1 (per 01-03-SUMMARY.md)

Voyage embedding dim = 1024 for voyage-3-large (per CONTEXT.md decisions). The vector(1024) column MUST match this. If a later week changes embedding provider, that is a SCHEMA CHANGE = new plan = new migration. Do not parameterize dim now.

source_module enum on timeline_event pre-seeds future phase modules to avoid an ALTER TYPE migration in every later phase. The full enum: ['memory', 'pipeline', 'deck', 'brief', 'safe', 'cap_table', 'legal']. Phase 2 only writes 'memory' values; later phases write the others without schema changes.

source_type enum on embeddings: ['memory', 'corpus', 'interaction']. Phase 2 only writes 'memory' and 'corpus'; 'interaction' is reserved for Phase 4.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/db/schema/memory.ts (business_memory + pipeline_entry + interaction)</name>
  <files>src/db/schema/memory.ts</files>
  <read_first>
    - .planning/phases/02-knowledge-layer/02-CONTEXT.md (provenance JSON shape; Sonnet 4.6 + Zod contract; Pitfall 11)
    - docs/Trochia_AI_Phase_2_PLAN_v1.md (§5 tech decisions; §7 Week 1 acceptance)
    - src/db/schema/tenancy.ts (existing tenant-scoped table pattern; tenantIsolationPolicy usage)
    - src/db/schema/billing.ts (existing tenant-scoped table pattern with enums)
    - src/db/rls.ts (tenantIsolationPolicy signature)
    - .planning/intel/Trochia_AI_PRD_v2.docx (§6.1 Business Memory schema fields — read via Read tool with limit; pull the canonical field list)
  </read_first>
  <action>
    Create `src/db/schema/memory.ts` with three pgTable declarations:

    1. **`business_memory`** — one row per `account_id` (tenant). Columns:
       - `id uuid primaryKey defaultRandom()`
       - `accountId uuid notNull references(() => accounts.id, { onDelete: 'cascade' })` — tenant scope (FK to Phase 1's `accounts`)
       - `companyName text`, `oneLiner text`, `sector text`, `stage text`, `geography text`, `incorporationStatus text`, `foundingDate timestamp({ withTimezone: true })`
       - `team jsonb` (founder profiles + roles), `traction jsonb` (mrr, arr, customers, growth), `narrative jsonb` (problem, solution, why-now, why-us)
       - `provenance jsonb notNull default('{}')` — keyed by field name → `{ source_snippet: string, confidence: number, extracted_at: timestamp, last_updated: timestamp, snooze_until: timestamp | null }`
       - `extractedAt timestamp({ withTimezone: true }).notNull().defaultNow()`
       - `confirmedAt timestamp({ withTimezone: true })` (null until founder confirms)
       - `lastUpdatedAt timestamp({ withTimezone: true }).notNull().defaultNow()`
       - `createdAt timestamp({ withTimezone: true }).notNull().defaultNow()`
       - `updatedAt timestamp({ withTimezone: true }).notNull().defaultNow()`
       - Unique index on `accountId` (one row per tenant)
       - Co-located RLS: `tenantIsolationPolicy(t.accountId)` from `src/db/rls.ts`

    2. **`pipeline_entry`** — Phase 4 fills CRUD; this plan ships the surface shape only. Columns:
       - `id uuid primaryKey defaultRandom()`
       - `accountId uuid notNull references accounts.id`
       - `investorName text notNull`
       - `investorType pgEnum('pipeline_investor_type_t', ['vc', 'angel', 'accelerator', 'family_office', 'crowd', 'other'])`
       - `stage pgEnum('pipeline_stage_t', ['identified', 'researching', 'intro_pending', 'pitched', 'in_diligence', 'soft_circle', 'committed', 'wired', 'declined', 'ghosted'])`
       - `checkSize text`, `sector text`, `geo text`, `notes text`
       - `firstContactAt timestamp({ withTimezone: true })`
       - `lastContactAt timestamp({ withTimezone: true })`
       - `createdAt`, `updatedAt` standard
       - Index on (`accountId`, `stage`)
       - Co-located RLS: `tenantIsolationPolicy(t.accountId)`

    3. **`interaction`** — captures every Q&A query for audit + future eval. Columns:
       - `id uuid primaryKey defaultRandom()`
       - `accountId uuid notNull references accounts.id`
       - `userId uuid notNull references users.id` (which user asked)
       - `kind pgEnum('interaction_kind_t', ['qa_sidebar', 'paste_extract', 'file_import', 'staleness_nudge'])`
       - `query text` (the user's question — null for non-query kinds)
       - `answer text` (the model's answer — null for non-qa kinds)
       - `citations jsonb` (array of `{ source_type, source_id, snippet }`)
       - `model text` (e.g., 'claude-opus-4-7')
       - `langfuseTraceId text`
       - `latencyMs integer`
       - `createdAt timestamp({ withTimezone: true }).notNull().defaultNow()`
       - Index on (`accountId`, `createdAt desc`)
       - Co-located RLS: `tenantIsolationPolicy(t.accountId)`

    All three tables export their column types via `$inferSelect` / `$inferInsert` named exports (e.g., `BusinessMemoryRow`, `BusinessMemoryInsert`) so application code is strongly typed.

    Use the **exact** import + RLS pattern from `src/db/schema/tenancy.ts` and `src/db/schema/billing.ts`. Do NOT inline policy logic — call `tenantIsolationPolicy(t.accountId)` only.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/memory.ts` exports `businessMemory`, `pipelineEntry`, `interaction` pgTable objects
    - Each table calls `tenantIsolationPolicy(t.accountId)` from `@/db/rls`
    - `business_memory` has `provenance jsonb notNull default('{}')` and is the ONLY catch-all column for source/confidence/snooze metadata (no schema changes after this plan)
    - `interaction.citations` is `jsonb` (array shape enforced at app layer via Zod, not in SQL)
    - `pipeline_entry` has the 10-value stage enum exactly: identified, researching, intro_pending, pitched, in_diligence, soft_circle, committed, wired, declined, ghosted
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - No string literal "investment advice", "legal advice" (without "not" prefix), "rolling fund", "fund manager", "adviser" in any code comment or default value
    - File contains zero raw `console.*` calls (ESLint rule from Phase 1)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create src/db/schema/timeline.ts (timeline_event)</name>
  <files>src/db/schema/timeline.ts</files>
  <read_first>
    - .planning/phases/02-knowledge-layer/02-CONTEXT.md (XC-08 unified timeline scope)
    - .planning/phases/02-knowledge-layer/02-PLAN.md (§10 must_haves for timeline_event)
    - src/db/schema/tenancy.ts (pattern; pgEnum usage)
    - src/db/rls.ts
  </read_first>
  <action>
    Create `src/db/schema/timeline.ts` with one pgTable:

    **`timeline_event`** — chronological cross-module event view. Columns:
    - `id uuid primaryKey defaultRandom()`
    - `accountId uuid notNull references accounts.id`
    - `sourceModule pgEnum('timeline_source_module_t', ['memory', 'pipeline', 'deck', 'brief', 'safe', 'cap_table', 'legal'])` — pre-seeded for downstream phases; Phase 2 only writes `'memory'`
    - `sourceId uuid notNull` — foreign key by convention only (cross-module; not a SQL FK)
    - `eventType text notNull` — free-form, e.g., 'memory_confirmed', 'memory_field_updated', 'memory_staleness_dismissed'
    - `summary text notNull` — short founder-facing summary (≤140 chars by convention)
    - `payload jsonb default('{}')` — module-specific event detail
    - `investorId uuid` (nullable — filled when event is investor-scoped; FK convention only, no SQL FK)
    - `founderVisible boolean notNull default(true)` — when false, hidden from UI but kept for audit
    - `eventAt timestamp({ withTimezone: true }).notNull().defaultNow()`
    - `createdAt timestamp({ withTimezone: true }).notNull().defaultNow()`

    Indexes:
    - `(accountId, eventAt desc)` — primary read path (chronological timeline per tenant)
    - `(accountId, sourceModule)` — module filter
    - `(accountId, investorId)` partial index `where investor_id is not null` — investor filter

    Co-located RLS: `tenantIsolationPolicy(t.accountId)`.

    Export `TimelineEventRow`, `TimelineEventInsert` types via `$inferSelect`/`$inferInsert`.

    Pre-seeding the 7-value enum here means later phases (3 deck, 4 pipeline, 7 data room, 9 SAFE, 9 cap table, 8 legal) do NOT need an ALTER TYPE migration to write their events — this is intentional schema lock per Pitfall 11.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/timeline.ts` exports `timelineEvent` pgTable
    - `sourceModule` enum has exactly these 7 values: memory, pipeline, deck, brief, safe, cap_table, legal
    - `eventAt` defaults to `now()`; `(accountId, eventAt desc)` index exists
    - RLS applied via `tenantIsolationPolicy(t.accountId)`
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create src/db/schema/embeddings.ts (embeddings + HNSW index)</name>
  <files>src/db/schema/embeddings.ts</files>
  <read_first>
    - .planning/phases/02-knowledge-layer/02-CONTEXT.md (Voyage voyage-3-large 1024-dim; HNSW cosine; idempotency rule)
    - .planning/phases/02-knowledge-layer/02-PLAN.md (§4 Week 4 KNW-04a wiring; §10 must_haves)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (pgvector extension already enabled in Phase 1)
    - src/db/schema/tenancy.ts
    - src/db/rls.ts
    - **MCP context7**: lookup `pgvector` HNSW operator class syntax + drizzle-orm `vector` column type (this is the first vector column in the repo — verify current Drizzle syntax before writing)
  </read_first>
  <action>
    Create `src/db/schema/embeddings.ts` with one pgTable using `drizzle-orm`'s `vector` type:

    ```ts
    import { vector } from 'drizzle-orm/pg-core';
    // (context7 lookup may reveal a different import path on the current drizzle version; use whatever context7 returns)
    ```

    **`embeddings`** — Columns:
    - `id uuid primaryKey defaultRandom()`
    - `accountId uuid notNull references accounts.id` — tenant scope
    - `sourceType pgEnum('embedding_source_type_t', ['memory', 'corpus', 'interaction'])` — Phase 2 writes memory + corpus; interaction reserved Phase 4
    - `sourceId uuid notNull` — FK convention only (cross-table)
    - `chunkText text notNull`
    - `chunkIdx integer notNull` — 0-indexed chunk position within source
    - `embedding vector('embedding', { dimensions: 1024 }).notNull()` — Voyage voyage-3-large dim
    - `embeddingModelVersion text notNull` — e.g., `'voyage-3-large'`; future re-embeds use a different value here, never overwrite
    - `tokenCount integer` — chunk token count (informational, for cost tuning)
    - `embeddedAt timestamp({ withTimezone: true }).notNull().defaultNow()`
    - `createdAt timestamp({ withTimezone: true }).notNull().defaultNow()`

    Indexes:
    - `uniqueIndex('embeddings_dedup_idx').on(t.accountId, t.sourceType, t.sourceId, t.chunkIdx, t.embeddingModelVersion)` — idempotency key (same source+chunk+model = same row; re-embeds via upsert; new model version = new row coexists for migration)
    - HNSW index on `embedding` with `vector_cosine_ops` operator class — drizzle-orm syntax: `index('embeddings_hnsw_idx').using('hnsw', t.embedding.op('vector_cosine_ops'))` (verify via context7)

    Co-located RLS: `tenantIsolationPolicy(t.accountId)`.

    Export `EmbeddingRow`, `EmbeddingInsert` types.

    **If `corpus` rows are tenant-shared (one corpus serves all tenants):** set `accountId` to a dedicated "system" account ID stored in `env.SYSTEM_ACCOUNT_ID`, and have RLS allow SELECT on rows where `account_id = env.SYSTEM_ACCOUNT_ID` for any authenticated user via a second policy `corpus_shared_read`. Alternatively: per-tenant copy of corpus embeddings (heavier; simpler RLS). **Choose per-tenant copy for now** (simpler RLS, ~50 docs × N tenants is tiny); deferred ticket: tenant-shared corpus optimization once N>50 tenants.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/embeddings.ts` exports `embeddings` pgTable
    - `embedding` column is `vector` type with dimensions: 1024
    - `embeddingModelVersion` is `text notNull` (NOT enum — must accept future model strings without ALTER TYPE)
    - Dedup unique index spans (accountId, sourceType, sourceId, chunkIdx, embeddingModelVersion)
    - HNSW index exists on `embedding` with `vector_cosine_ops`
    - RLS applied via `tenantIsolationPolicy(t.accountId)`
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update src/db/schema/index.ts (barrel) to re-export the new modules</name>
  <files>src/db/schema/index.ts</files>
  <read_first>
    - src/db/schema/index.ts (current barrel)
  </read_first>
  <action>
    Append three lines to the barrel after the existing Phase 1 exports:

    ```ts
    export * from '@/db/schema/memory';
    export * from '@/db/schema/timeline';
    export * from '@/db/schema/embeddings';
    ```

    Update the file-level comment to note the Phase 2 additions: "Phase-2 tables: business_memory, pipeline_entry, interaction, timeline_event, embeddings."
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/index.ts` re-exports memory.ts, timeline.ts, embeddings.ts
    - `npm run typecheck` exits 0
    - Drizzle picks up all three new schema files on `npm run db:generate`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Generate + manually review migration</name>
  <files>src/db/migrations/0XXX_phase2_memory.sql</files>
  <read_first>
    - drizzle.config.ts
    - src/db/migrations/ (existing migrations — pattern for ENABLE ROW LEVEL SECURITY + CREATE POLICY)
    - **MCP context7**: lookup `drizzle-kit generate` current flag set (verify command surface hasn't changed)
  </read_first>
  <action>
    Run `npm run db:generate` (which calls `drizzle-kit generate`). Open the produced migration file in `src/db/migrations/`. Manually verify it contains:

    1. CREATE TABLE for all five new tables
    2. CREATE TYPE for all new enums (`pipeline_investor_type_t`, `pipeline_stage_t`, `interaction_kind_t`, `timeline_source_module_t`, `embedding_source_type_t`)
    3. ENABLE ROW LEVEL SECURITY for all five new tables
    4. CREATE POLICY `tenant_isolation_*` for each (Drizzle's `pgPolicy` helper emits these from the schema files)
    5. CREATE INDEX statements including:
       - `embeddings_dedup_idx` (unique)
       - `embeddings_hnsw_idx` USING hnsw (embedding vector_cosine_ops)
       - `timeline_event_account_event_at_idx`
       - `timeline_event_account_source_module_idx`
       - `timeline_event_account_investor_partial_idx`
       - `pipeline_entry_account_stage_idx`
       - `interaction_account_created_at_idx`
       - `business_memory_account_id_unique_idx`
    6. GRANT statements consistent with Phase 1's pattern (typically `GRANT SELECT, INSERT, UPDATE, DELETE ON {table} TO authenticated`)

    **If the generated migration is missing the HNSW index or the policies:** edit the SQL file by hand to add what is missing. Drizzle's vector + HNSW support is recent; manual SQL additions are acceptable here. Document any manual additions with a header comment block: `-- MANUAL ADDITION (Phase 2, Plan 02-01): HNSW index, see RESEARCH gap`.

    Do NOT run `db:push` in this task — Task 7 does that as `autonomous: false`.
  </action>
  <verify>
    <automated>test -f src/db/migrations/*phase2_memory* && grep -q "USING hnsw" src/db/migrations/*phase2_memory* && grep -q "ENABLE ROW LEVEL SECURITY" src/db/migrations/*phase2_memory*</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at `src/db/migrations/0XXX_phase2_memory.sql` (or generated name)
    - File contains CREATE TABLE for all five new tables
    - File contains ENABLE ROW LEVEL SECURITY for all five new tables
    - File contains at least one CREATE POLICY per new table
    - File contains USING hnsw on the embeddings table
    - File contains the dedup unique index on embeddings
    - No banned strings in the SQL (banned-string CI passes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Extend schema-scan + two-user-isolation tests to cover new tables</name>
  <files>tests/integration/rls-memory.test.ts, tests/rls/two-user-isolation.test.ts, tests/rls/schema-scan.test.ts</files>
  <read_first>
    - tests/rls/schema-scan.test.ts (Phase 1 scan logic; how it iterates the schema)
    - tests/rls/two-user-isolation.test.ts (Phase 1 two-user pattern; existing test helpers in tests/db/test-db.ts)
    - tests/db/test-db.ts (test DB harness)
    - src/db/rls.ts (NON_TENANT_TABLES allowlist constant)
  </read_first>
  <action>
    1. **`tests/rls/schema-scan.test.ts`** — Confirm it auto-picks up the new tables via Drizzle's schema introspection. No code changes expected; if the test hard-codes a Phase 1 table count, update it to "≥9 tenant tables" or remove the count assertion (test the property, not the cardinality).

    2. **`tests/rls/two-user-isolation.test.ts`** — Phase 1 file. Add `business_memory`, `pipeline_entry`, `interaction`, `timeline_event`, `embeddings` to the list of tables it iterates; for each, insert a fixture row as tenant A, switch DB context to tenant B, assert zero rows returned across SELECT * and any indexed read. If the existing test discovers tenant-scoped tables dynamically from schema metadata, no code change is needed — just rerun and confirm coverage.

    3. **`tests/integration/rls-memory.test.ts`** (NEW) — Higher-level integration test specific to Phase 2 tables. Cases:
       - Insert a `business_memory` row + 5 `embeddings` rows + 3 `interaction` rows + 4 `timeline_event` rows as tenant A
       - Switch context to tenant B — assert 0 rows returned from each table
       - Insert as tenant B — confirm tenant A still sees only its 12 rows
       - Confirm the unique index on (accountId, sourceType, sourceId, chunkIdx, embeddingModelVersion) rejects a duplicate upsert (idempotency proof)
       - Confirm a SELECT with no auth context returns 0 rows (RLS default-deny)
       - Confirm `timeline_event.sourceModule` enum rejects an invalid value
       - Confirm `embeddings.embedding` accepts a 1024-dim Float32Array and rejects a 768-dim one

    Use the same `withTenantContext(tenantId, async (db) => ...)` helper pattern from Phase 1's `tests/db/test-db.ts`. If that helper doesn't exist with that exact name, use whatever Phase 1 actually uses (read `tests/rls/two-user-isolation.test.ts` first).
  </action>
  <verify>
    <automated>npx vitest run tests/rls/schema-scan.test.ts tests/rls/two-user-isolation.test.ts tests/integration/rls-memory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/rls-memory.test.ts` file exists and contains ≥6 test cases
    - `npx vitest run tests/integration/rls-memory.test.ts` exits 0 (all green)
    - `npx vitest run tests/rls/two-user-isolation.test.ts` exits 0 with the new tables in scope
    - `npx vitest run tests/rls/schema-scan.test.ts` exits 0
    - All five new tables present in schema-scan output
    - Idempotency test asserts the dedup unique index enforces correctly
    - Vector dim mismatch test asserts the column rejects wrong-dim arrays
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Push migration to staging Supabase + verify HNSW index (AUTONOMOUS: FALSE)</name>
  <files>(migration deployment; no repo file changes)</files>
  <read_first>
    - drizzle.config.ts (DATABASE_URL / DIRECT_URL routing)
    - .env.local (existing — read, do not clobber)
  </read_first>
  <action>
    **This task is NOT autonomous.** It runs against the staging Supabase project and may prompt for destructive-change confirmation.

    1. Confirm `.env.local` has staging `DATABASE_URL` + `DIRECT_URL` pointing at the Phase 1 Supabase project
    2. Run `npm run db:push` — accept any confirmation prompts manually
    3. Verify in Supabase SQL editor:
       ```sql
       \d+ business_memory
       \d+ embeddings
       SELECT relname, indexname FROM pg_indexes WHERE tablename = 'embeddings';
       SELECT * FROM pg_extension WHERE extname = 'vector';
       ```
       Confirm: HNSW index exists on `embeddings.embedding`; pgvector extension is enabled
    4. Run a smoke insert manually:
       ```sql
       SET LOCAL role authenticated;
       SET LOCAL "request.jwt.claims" = '{"sub": "<test-user-uuid>"}';
       INSERT INTO business_memory (account_id, company_name, provenance)
       VALUES ('<test-account-uuid>', 'Test Co', '{}'::jsonb)
       RETURNING id;
       ```
       Confirm it succeeds for the row's tenant and fails for another tenant (RLS sanity).
    5. Confirm no production database was touched — the production project URL should NOT appear in any DATABASE_URL value during this task. If staging is the same project as production (likely for solo Martins pre-Phase-5), tag the rows as test-only via a known account_id and clean up after smoke.

    If anything goes wrong: rollback by `DROP TABLE ... CASCADE` on the 5 new tables + `DROP TYPE` on the 5 new enums in Supabase SQL editor; fix the migration; redo Task 5 + Task 7.
  </action>
  <verify>
    <automated>echo "Manual verification — see acceptance criteria"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:push` completed successfully against staging Supabase
    - `\d+ embeddings` shows HNSW index on `embedding` column
    - `\d+ business_memory` shows `provenance jsonb DEFAULT '{}'::jsonb NOT NULL`
    - All five new tables visible in Supabase Table Editor with RLS toggle ON
    - Smoke insert as tenant A succeeds; same insert as tenant B fails
    - No production database touched
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Run full Week 1 verification loop</name>
  <files>(no file changes; verification only)</files>
  <read_first>
    - .planning/phases/02-knowledge-layer/02-PLAN.md (§4 Week 1 verification loop)
  </read_first>
  <action>
    Run the Week 1 verification loop from `02-PLAN.md` §4:

    1. `npm run db:generate` — clean (no new diff after Task 5; if a diff appears, fix and re-push)
    2. `npm run typecheck` — exit 0
    3. `npm run lint` — exit 0
    4. `npm run check:banned` — exit 0
    5. `npm run build` — exit 0 (Next.js 16.x clean build)
    6. `npx vitest run tests/rls/` — all RLS tests green
    7. `npx vitest run tests/integration/rls-memory.test.ts` — green
    8. `npx playwright test` — Phase 1 smoke tests still pass (no regression)
    9. `npm run test` (full Vitest suite) — all tests green
    10. Open a draft PR; tag for `/codex` second-opinion on the Drizzle schema (pipeline entries adjacent to financial primitives)
    11. Code Reviewer + Security Engineer review of the PR
    12. Merge to Phase 2 branch `phase-2-week-1-schema` after green CI
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run check:banned && npm run build && npx vitest run && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - All eight verification steps green
    - PR opened with Codex second-opinion comment attached
    - Code Reviewer + Security Engineer sign-off on PR
    - Branch merged to `phase-2-week-1-schema`
    - `tasks/lessons.md` updated with any Phase 2 Week 1 lessons (especially any drizzle-kit / pgvector / HNSW gotchas encountered)
    - `02-01-SUMMARY.md` written using the GSD summary template
  </acceptance_criteria>
</task>

</tasks>

<verification>
**Plan exit (Week 1 → Week 2 hand-off):**

- [ ] All eight tasks above complete with acceptance criteria green
- [ ] `02-01-SUMMARY.md` written using `$HOME/.claude/get-shit-done/templates/summary.md`
- [ ] Schema lock declared in `02-01-SUMMARY.md` — no further column additions to `business_memory`, `pipeline_entry`, `interaction`, `timeline_event`, `embeddings` for the remainder of Phase 2 (Pitfall 11 mitigation)
- [ ] PR merged to `phase-2-week-1-schema` branch
- [ ] Vercel preview deploys clean (Phase 1 deployment chain intact)
- [ ] Langfuse / Sentry / Amplitude all still reporting (no Phase 1 regression)
- [ ] Design partner outreach started (Week 1 todo item — runs in parallel to schema work) — at least 2 warm intros sent
</verification>

<must_haves>
See frontmatter `must_haves` block above.
</must_haves>

---

*Plan 02-01 — Schema foundation. Locks the Phase 2 spine. Author: gsd-plan-phase orchestrator, 2026-05-19.*
