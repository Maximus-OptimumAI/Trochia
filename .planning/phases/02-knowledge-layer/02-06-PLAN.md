---
phase: 02-knowledge-layer
plan: 06
type: execute
mode: mvp
wave: 6
week: 6
status: ready   # cross-AI CONVERGED at cycle 1 (current_high=0); execution-ready pending founder go. Founder-gated /codex + /cso fire at execution close (standing rule 12).
authored: 2026-06-01
review_history:
  - cycle: 0
    reviewed_at: 2026-06-01
    reviewers: [gsd-plan-checker]   # internal sonnet checker, pre-Codex (mirrors 02-05's "APPROVED-WITH-NOTES pre-cycle-1")
    reviews_file: .planning/phases/02-knowledge-layer/02-REVIEWS.md
    findings: { critical: 0, high: 1, medium: 2, low: 3 }
    addressed_in_replan: true   # all 6 folded into this doc 2026-06-01 before the Codex cycle
    summary: "APPROVED-WITH-NOTES. All 3 founder guardrails PASS; grounding verified against live code (cosineDistance in drizzle 0.44.7; voyage.embed query entrypoint + trace shape; getRequestClient(token).rls(fn); getServiceClient/@anthropic-ai/sdk correctly excluded; OD-1 query-time FTS sound + schema-lock diff guard asserted). F-1 (HIGH): per-side test-predicate-assertion mechanics under-specified + collide with embed-memory.test.ts's 'ignore _pred' fake → T02 now mandates vector-side .toSQL() serialization + FTS-side sql-param inspection, and forbids copying the embed-memory shortcut. F-2 (MED): OD-4 synthetic source_type:'memory' for a query is a knowingly-false value in the privacy-contract trace → OD-4 revised to widen the adapter trace.sourceType union with an honest 'query' literal (does NOT change TRACE_METADATA_KEYS; Case 8b key-set pin holds) + test pins the query-trace shape. F-3 (MED): weak-but-nonzero path (vector far rows + FTS empty) untested → T02 case (i) added. F-4 (LOW): vectorScore range contract documented (= 1 - cosineDistance = cosine similarity ∈ [-1,1]). F-5 (LOW): both queries now share ONE ctx.rls transaction snapshot. F-6 (LOW): CANDIDATE_POOL → CANDIDATE_POOL_FACTOR so the per-side LIMIT scales with caller topK."
  - cycle: 1
    reviewed_at: 2026-06-01
    reviewers: [codex]   # Codex CLI 0.130.0, read-only sandbox, model_reasoning_effort=high, plan-only framing; ~89k tokens
    reviews_file: .planning/phases/02-knowledge-layer/02-REVIEWS.md
    findings: { critical: 0, high: 0, medium: 1, low: 1 }
    addressed_in_replan: true   # both folded in-cycle 2026-06-01
    summary: "CONVERGED — current_high=0 on the first cross-AI cycle. Codex confirmed: OD-1 Option A query-time FTS is technically sound + needs no schema change; the schema-lock diff guard is asserted; RRF k=60 rank-based math + the 1/(60+1)+1/(60+3) worked example correct; OD-4 'query' union widening is genuinely key-set-preserving (Case 8b holds); vectorScore = 1 - cosineDistance is the correct cosine similarity for the 0.6 floor; tenant scope + single Voyage chokepoint + mock-all-deps all sound; scope correctly fenced to retrieval. 2 NEW findings folded: C-F1 (MED) topK uncapped → added MAX_TOP_K=50 + effTopK=clamp(trunc(topK??8),1,50) normalization before any SQL (T-02-06-04 + truth + T02 case (j)); C-F2 (LOW) test predicate-proof harness brittleness → T02 now specifies drizzle.mock({schema}) + .toSQL() on the real query objects + a separate mocked executor, not private-shape poking."
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
predecessor_commits:
  - "1d72cb3"   # HEAD of phase-2-knowledge-layer at start of this plan author (02-05 close tail; SUMMARY commit)
  - "29228e8"   # schema-lock baseline — git diff --quiet 29228e8 -- src/db/schema/ MUST stay exit 0 (see OD-1)
strategic_tickets: []
requirements:
  - KNW-05a   # RAG retrieve service (the RETRIEVAL portion of KNW-05). KNW-05 itself completes at 02-07 when the cited, <8s-median, "I don't know"-capable Q&A sidebar lands on top of this retriever. retrieve.ts is the read-path engine; 02-07 (KNW-05c) is the agent + sidebar + cost cap. Verified against REQUIREMENTS.md (KNW-05) + 02-CONTEXT.md traceability (KNW-05a → KNW-05 retrieval portion) + embeddings.ts header (retrieve.ts = KNW-05a).
notes: "Read path for the Knowledge Layer: the hybrid retriever src/ai/rag/retrieve.ts. Vector side = pgvector cosine over the per-tenant embeddings written by Plan 02-04's embed-memory pipeline; keyword side = Postgres full-text search over chunk_text; the two candidate lists are fused by Reciprocal Rank Fusion (RRF) and returned ranked with scores. Tenant-scoped end-to-end (accountId in every query + RLS default-deny). Query embedding is a NEW per-token Voyage egress on the read path (PULL-OBS-COST-01-FORWARD — the $5/user/day cap ships in the 02-07 joint merge, NOT here). This plan is RETRIEVAL ONLY — no Opus synthesis, no citations, no 'I don't know' decision (those are 02-07/KNW-05c); retrieve.ts returns ranked candidates + scores so 02-07 can apply the grounding threshold. DEPLOY-DEFERRED posture stands: branch-only, PR #7 DRAFT, main untouched; the read path debuts to prod in the 02-07 joint write+read+cost-cap merge."
autonomous: true   # 2 autonomous tasks; no human-action checkpoints (founder-gated /codex + /cso fire at execution close per standing rule 12, not mid-plan)
security_gate: cso
codex_review_required: true
files_modified:
  # Created — the hybrid retriever (the whole read path for Phase 2)
  - src/ai/rag/retrieve.ts                         # CORE — NEW. hybridRetrieve({ accountId, query, topK }, { rls }) → ranked Candidate[]; embeds query via voyage.embed(inputType:'query'); vector side = cosineDistance over embeddings; keyword side = query-time websearch_to_tsquery/ts_rank_cd over chunk_text (OD-1 Option A, no schema change); RRF fusion; tenant + source_type + embedding_model_version filters in EVERY query
  # Created — tests (mock ALL live deps: voyage + db; the env-gate-keys-on-presence lesson from 02-05)
  - tests/ai/rag/retrieve.test.ts                  # NEW — unit tests, voyage.embed + db both mocked; RRF fusion correctness, tenant-scope assertion, model-version filter, source_type filter, empty/weak-result path, websearch_to_tsquery construction, query-text-never-traced
  # Modified — Voyage adapter (OD-4: honest 'query' trace.sourceType — pure TS union widening, key-set-preserving)
  - src/ai/integrations/voyage.adapter.ts          # CORE — OD-4: widen VoyageEmbedInput.trace.sourceType to include 'query'. Additive only; TRACE_METADATA_KEYS UNCHANGED (Case 8b key-set pin holds). NO schema change, NO chokepoint change. (If OD-4 resolves to the zero-adapter-touch alternative, this file drops from the list.)
  - tests/ai/integrations/voyage.adapter.test.ts   # +1 case: a 'query' embed traces with input_type:'query' + source_type:'query'; existing Case 8/8b/8c stay green (key set unchanged)
  # Modified — docs (capture the read-path egress + RRF + within-lock FTS decision)
  - tasks/lessons.md                               # capture: query embedding is a read-path AI egress (cost cap is 02-07); RRF-over-rank beats score-normalization for incomparable cosine-vs-ts_rank scales; query-time FTS is within-schema-lock at Phase-2 per-tenant scale (GIN index deferred to FOLLOWUP-FTS-GIN-INDEX-01); a query embed traces source_type:'query' (never a false 'memory') on the privacy surface
mandatory_skills:
  - rag-implementation                  # hybrid retrieval architecture: vector + keyword fusion, candidate-pool sizing, RRF
  - vector-database-engineer            # pgvector cosine query shape (cosineDistance / <=> ), HNSW recall, top-k tuning
  - embedding-strategies                # query-side embedding (inputType:'query'), document-vs-query asymmetry, model-version pinning
  - claude-api                          # NOT used for synthesis here (that's 02-07); read for the AI-egress + Langfuse trace-whitelist discipline that also binds the Voyage query embed
  - privacy-by-design                   # query text is MORE sensitive than document text (it is what the founder is asking) — NEVER in a Langfuse trace, log, or error string
  - postgresql                          # websearch_to_tsquery / ts_rank_cd / to_tsvector query-time FTS; why the tenant filter makes a seq tsvector negligible at Phase-2 scale
mandatory_mcp_servers:
  - context7                            # drizzle-orm cosineDistance + sql`` tagged-template shape for to_tsvector/websearch_to_tsquery; langfuse trace signature; pgvector operator class
  - github                              # n/a for new files but available if the executor inspects prior workflow/test conventions

must_haves:
  truths:
    - "src/ai/rag/retrieve.ts exports a single hybridRetrieve(args, ctx) function that takes { accountId: string, query: string, topK?: number } and a request-scoped DB context, and returns a ranked Candidate[] where each Candidate carries { sourceType, sourceId, chunkIdx, chunkText, vectorScore, ftsScore, rrfScore } — RETRIEVAL ONLY; it performs NO Anthropic call, NO synthesis, NO citation assembly, and makes NO 'I don't know' decision (those are Plan 02-07 / KNW-05c)"
    - "Every SQL query the retriever issues is tenant-scoped: it runs inside the request-scoped tenant client (getRequestClient(token).rls(fn) — RLS default-deny enforces account_id = jwt.tenant_id) AND carries an explicit WHERE account_id = <accountId> belt-and-suspenders predicate. There is no getServiceClient() / RLS-bypass path anywhere in the retriever (that path is for Inngest jobs only, per 02-04)"
    - "The query is embedded through the EXISTING Voyage adapter with inputType:'query' (voyage.embed({ texts:[query], inputType:'query', trace })) — the adapter stays the single surface that calls api.voyageai.com; retrieve.ts adds NO new HTTP call to Voyage and imports NO @anthropic-ai/sdk"
    - "The vector side queries the embeddings table by cosine distance over the 1024-dim embedding column (drizzle cosineDistance / the <=> operator on the HNSW vector_cosine_ops index), filtered by account_id + source_type IN ('memory','corpus') + embedding_model_version = 'voyage-3-large'; the model-version filter honors the rolling re-embed contract from 02-01"
    - "The keyword side runs a Postgres full-text search over embeddings.chunk_text using websearch_to_tsquery('english', <query>) matched against to_tsvector('english', chunk_text), ranked by ts_rank_cd, under the SAME account_id + source_type + embedding_model_version filters as the vector side"
    - "OD-1 resolved to within-schema-lock (Option A): the FTS to_tsvector is computed at QUERY TIME (no stored GENERATED column, no GIN index added) so git diff --quiet 29228e8 -- src/db/schema/ STILL exits 0 at plan close, zero migrations are emitted, and FOLLOWUP-DBDIFF-01 stays deferred. (If the founder/convergence elects Option B — stored tsvector column + GIN index — this plan gains a migration task AND activates FOLLOWUP-DBDIFF-01; see OD-1.)"
    - "Vector and keyword candidate lists are fused by Reciprocal Rank Fusion (RRF): rrfScore(d) = Σ 1/(k + rank_i(d)) over each list the doc appears in, k = 60 (documented constant); fusion is by RANK, never by raw score, because cosine distance (0..2) and ts_rank_cd (unbounded) are on incomparable scales (OD-2). Final result is sorted by rrfScore desc and truncated to topK (default 8)"
    - "Query text NEVER enters a Langfuse trace, a log line, an error message, or the returned Candidate metadata beyond the chunk_text that was actually retrieved. The Voyage query-embed trace carries ONLY the existing TRACE_METADATA_KEYS whitelist (account_id, input_type='query', token_count, latency_ms, …) — the adapter already strips everything else; retrieve.ts adds no logger.* call that includes the query (OD-4)"
    - "Query embedding is acknowledged as a NEW read-path AI-token egress under PULL-OBS-COST-01-FORWARD: the $5/user/day cap (Anthropic + Voyage aggregate) ships in the 02-07 joint merge, NOT in this plan; this plan adds the egress at exactly one chokepoint (the single voyage.embed call in retrieve.ts) so 02-07's cap has one place to meter the query side"
    - "All live dependencies are MOCKED in the unit tests — voyage.embed is vi.mock'd (no live Voyage call) AND the DB is mocked/stubbed (no live Postgres); a present-but-invalid key in .env.local must NOT be able to fire a real Voyage call from the test suite (the env-gate-keys-on-credential-presence lesson, lessons.md 2026-06-01)"
    - "Zero schema changes (OD-1 Option A): git diff --quiet 29228e8 -- src/db/schema/ exits 0; zero new migrations under src/db/migrations/**; zero ALTER statements; npx drizzle-kit check stays clean"
    - "Zero new npm dependencies: package.json dependencies + devDependencies are byte-identical to the 1d72cb3 baseline; pgvector cosine via the already-installed drizzle-orm cosineDistance, FTS via raw sql`` tagged templates over the already-installed postgres driver"
    - "The retriever exposes vectorScore + ftsScore + rrfScore on each Candidate (not just the fused rank) so Plan 02-07's qa-rag agent can apply the '<threshold → I don't know' grounding contract (02-CONTEXT.md: start at 0.6 cosine, tune via eval) WITHOUT re-querying — 02-06 returns the evidence, 02-07 makes the grounding call. vectorScore is documented as cosine SIMILARITY (= 1 - cosineDistance) ∈ [-1,1], higher=better — NOT a distance — so the 02-CONTEXT 0.6-cosine floor compares directly against it (F-4); vectorScore is null on an FTS-only hit and ftsScore is null on a vector-only hit"
    - "Both the vector and keyword queries execute inside a SINGLE ctx.rls(async tx => …) transaction so the fused candidate lists are read from one consistent snapshot (F-5); the two queries never straddle two transactions"
    - "topK is normalized before any SQL runs: effTopK = clamp(Math.trunc(topK ?? 8), 1, MAX_TOP_K) with MAX_TOP_K = 50 (C-F1); a zero/negative/NaN/non-integer/oversized topK can never produce an invalid SQL LIMIT, an unbounded scan, or an oversized payload — every LIMIT and the final slice use effTopK"
    - "Operator voice held — code comments + the retriever's docstring + lessons.md use 'retrieves / ranks / fuses / scopes / filters' verbs; never 'detects / handles / wants / helps / feels'; zero banned compliance strings (npm run check:banned green)"
  artifacts:
    - path: "src/ai/rag/retrieve.ts"
      provides: "export async function hybridRetrieve({ accountId, query, topK = 8 }: HybridRetrieveArgs, ctx: { rls: <request-tenant runner> }): Promise<Candidate[]>. Constants: CANDIDATE_POOL_FACTOR = 4 (per-side LIMIT = topK * CANDIDATE_POOL_FACTOR — scales with a caller-supplied topK, F-6), RRF_K = 60, EMBEDDING_MODEL_VERSION = 'voyage-3-large', SOURCE_TYPES = ['memory','corpus']. (1) embeds the query: voyage.embed({ texts:[query], inputType:'query', trace:{ accountId, sourceType:'query', sourceId: crypto.randomUUID() } }) → queryVec:number[1024] (OD-4 honest query-trace shape); (2)+(3) run BOTH the vector and keyword queries inside a SINGLE ctx.rls(async tx => { … }) so they share one transaction snapshot — consistent fusion input, one setup round-trip (F-5). VECTOR: SELECT id, source_type, source_id, chunk_idx, chunk_text, cosineDistance(embedding, queryVec) AS dist FROM embeddings WHERE account_id = accountId AND source_type IN ('memory','corpus') AND embedding_model_version = 'voyage-3-large' ORDER BY dist ASC LIMIT pool. KEYWORD: SELECT …, ts_rank_cd(to_tsvector('english', chunk_text), q) AS rank FROM embeddings, websearch_to_tsquery('english', query) q WHERE account_id = accountId AND <same source_type+model filters> AND to_tsvector('english', chunk_text) @@ q ORDER BY rank DESC LIMIT pool; (4) RRF fuse the two ranked lists (k=60, 1-based positions: score += 1/(60+position)) keyed on embeddings.id; (5) return top-topK Candidate[] sorted by rrfScore desc, each carrying vectorScore (= 1 - dist = cosine SIMILARITY ∈ [-1,1], higher=better — the scale 02-07's 0.6 floor compares against; null for an FTS-only hit, F-4), ftsScore (ts_rank_cd value; null for a vector-only hit), rrfScore. Exports the Candidate + HybridRetrieveArgs types. Docstring states: retrieval only, no synthesis; tenant-scoped; query embed is the read-path egress metered by 02-07's OBS-COST-01; vectorScore is cosine similarity not distance."
    - path: "tests/ai/rag/retrieve.test.ts"
      provides: "Unit tests with voyage.embed (vi.mock) AND the DB both mocked — NO live calls. PER-SIDE PREDICATE ASSERTION (F-1): the embed-memory.test.ts fake's 'ignore _pred / assume the only matching row' shortcut is INSUFFICIENT here and MUST NOT be copied — it would let cases (c)/(d) pass vacuously and silently void the cross-tenant mitigation's own proof. Instead: assert the VECTOR-side predicate by serializing the drizzle query with .toSQL() and asserting account_id / source_type / embedding_model_version appear in the parameterized SQL + params; assert the KEYWORD-side predicate by inspecting the raw sql`` fragment's bound params/chunks (accountId is a bound param — directly readable). Cases: (a) RRF math correct on a two-list fixture (a doc at vector rank #1 + fts rank #3 scores 1/(60+1)+1/(60+3); pins k=60 + 1-based positions); (b) only-vector-hit and only-fts-hit docs both surface fused; (c) account_id = <passed accountId> on BOTH queries (per the per-side mechanics above); (d) source_type IN ('memory','corpus') + embedding_model_version='voyage-3-large' on BOTH queries; (e) zero rows both sides → [] (no throw); (f) voyage.embed called exactly once with inputType:'query' + texts:[query] (single-chokepoint pin); (g) the query string never appears in any Langfuse trace arg nor any logger call (query-text-not-traced); (h) result length never exceeds topK; (i) WEAK path (F-3): FTS empty + vector returns low-similarity rows → result non-empty, ftsScore null on every candidate, rrfScore from vector rank only, length ≤ topK (pins the dominant real-world weak-retrieval shape and prevents an executor adding a vector-side distance cutoff that would leak 02-07's grounding decision into the retriever)."
  key_links:
    - from: "src/ai/rag/retrieve.ts"
      to: "src/ai/integrations/voyage.adapter.ts"
      via: "voyage.embed({ texts:[query], inputType:'query', trace })"
      pattern: "voyage\\.embed\\(|inputType:\\s*'query'"
    - from: "src/ai/rag/retrieve.ts"
      to: "src/db/schema/embeddings.ts"
      via: "query the embeddings table (vector + FTS sides)"
      pattern: "embeddings\\b"
    - from: "src/ai/rag/retrieve.ts"
      to: "drizzle-orm cosineDistance"
      via: "vector side ordering by cosine distance over the HNSW index"
      pattern: "cosineDistance\\("
    - from: "src/ai/rag/retrieve.ts"
      to: "Postgres FTS"
      via: "websearch_to_tsquery / to_tsvector / ts_rank_cd via sql`` tagged template"
      pattern: "websearch_to_tsquery|to_tsvector|ts_rank_cd"
    - from: "src/ai/rag/retrieve.ts"
      to: "src/db/client.ts request-scoped tenant runner"
      via: "queries run inside getRequestClient(token).rls(fn) — RLS enforces tenant isolation"
      pattern: "\\.rls\\(|getRequestClient"
    - from: "tests/ai/rag/retrieve.test.ts"
      to: "the Voyage + DB mocks"
      via: "vi.mock the voyage adapter and stub the db — no live calls"
      pattern: "vi\\.mock\\(|mockResolved"
---

## Open Decisions for Convergence

Five decisions surfaced for the cross-AI convergence cycle (gsd-plan-checker internal + Codex) and founder adjudication. OD-1 is load-bearing — it is the schema-lock fork the founder flagged as this being the first Phase-2 plan that could legitimately change schema. The tasks below are written against the **recommended** resolution of each; convergence may revise.

| ID | Decision | Recommended resolution (tasks written against this) | Trade-off / why |
|---|---|---|---|
| **OD-1** | **SCHEMA-LOCK fork (the founder's #1 guardrail).** The keyword side needs a `to_tsvector('english', chunk_text)`. Compute it at QUERY TIME (no schema change), or add a stored `GENERATED` tsvector column + GIN index (a schema change)? | **RESOLVED → Option A (FOUNDER-CONFIRMED 2026-06-01).** Query-time FTS, NO schema change. Compute `to_tsvector('english', chunk_text) @@ websearch_to_tsquery(...)` + `ts_rank_cd(...)` inline. `git diff --quiet 29228e8 -- src/db/schema/` STAYS exit 0; zero migrations; **FOLLOWUP-DBDIFF-01 stays deferred**. The stored-column+GIN-index is captured as **FOLLOWUP-FTS-GIN-INDEX-01**, to land (with the real `db:diff` tool, activating DBDIFF-01) in the first plan where a tenant's chunk count makes the index earn its keep. | The retrieval is ALWAYS `WHERE account_id = ?` first, so the candidate set after the tenant + source_type + model filters is a single founder's chunks — dozens to low hundreds at Phase-2 / design-partner scale. A query-time `to_tsvector` over that tiny filtered set is sub-millisecond; a GIN index buys nothing until per-tenant chunk counts reach the thousands. Option A is the "clean FTS approach within the locked schema" the founder asked to prefer. **Tradeoff flagged either way:** Option A is O(n) tsvector recompute per query within the already-tiny tenant set (negligible now, revisit at ~thousands of chunks/tenant); Option B is the indexed, scale-ready path BUT it is the first src/db/schema change since 29228e8 — it breaks the schema-lock diff guard, requires a new migration + a prod schema re-deploy, and ACTIVATES FOLLOWUP-DBDIFF-01 (build the reusable `db:diff` drift tool), pulling schema-change machinery into a plan the founder scoped as surgical. **NOTE:** `src/db/schema/embeddings.ts:89-97` documents an *intent* to add this column "in the retrieval plan (Plan 02-06)" — Option A consciously defers that intent rather than executing it silently; this OD is where the founder ratifies defer-vs-do. |
| **OD-2** | **Fusion method:** Reciprocal Rank Fusion (RRF) vs. weighted normalized-score blend? | **RRF, k=60 (RECOMMENDED).** Fuse by RANK position in each list, not by raw score. | Cosine distance (0..2, lower=better) and `ts_rank_cd` (unbounded, corpus-statistics-dependent, higher=better) are on incomparable scales — a weighted score blend needs per-corpus normalization that drifts as the tenant's data grows and is a tuning liability. RRF is scale-free, deterministic, and the standard hybrid-search default; k=60 is the canonical value. Tradeoff: RRF discards score *magnitude* (a doc that's a runaway #1 on both sides fuses the same as a narrow #1) — acceptable because 02-07 re-reads the raw vectorScore/ftsScore for its grounding threshold, so magnitude isn't lost downstream. |
| **OD-3** | **Read-path AI egress + cost.** The query embedding bills Voyage tokens on every retrieval. Where is it metered? | **Single chokepoint now; cap in 02-07 (RECOMMENDED).** retrieve.ts makes exactly ONE `voyage.embed` call (the query). The $5/user/day aggregate cap (OBS-COST-01) ships in the 02-07 joint merge as **PULL-OBS-COST-01-FORWARD**, gating at the Voyage adapter + Anthropic client. 02-06 does NOT add a cost cap. | Per the deploy-deferred posture, the read path debuts to prod only in the 02-07 joint merge — which is where the cap lands, so a tenant can never drive uncapped spend in prod. Putting the query embed at one call site means 02-07's cap has a single read-side meter point. Tradeoff: between now and the 02-07 merge the egress is branch-only (no prod exposure) so the absent cap is not a live risk. |
| **OD-4** | **Voyage query-trace shape.** `voyage.embed`'s `trace` requires `{ accountId, sourceType ∈ ('memory'|'corpus'|'interaction'), sourceId }`. A *query* has no source row — and the trace `source_type`/`source_id` feed the privacy-contract Langfuse payload + any future per-source analytics. | **Widen the adapter's `trace.sourceType` TS union with an honest `'query'` literal + pass a correlation-UUID `sourceId` (RECOMMENDED).** Change `VoyageInputType`-adjacent `VoyageEmbedInput.trace.sourceType` from `'memory'|'corpus'|'interaction'` to `…|'query'`; retrieve.ts passes `{ accountId, sourceType:'query', sourceId: crypto.randomUUID() }`. This is a pure TS union widening — it does NOT add/remove a `TRACE_METADATA_KEYS` entry, so the adapter's Case 8b **exact-key-set** pin still holds (it asserts the KEY set, not the value domain); add one adapter-test case confirming a `'query'` embed traces with `input_type:'query'` + `source_type:'query'`. (F-2) | The original draft passed `sourceType:'memory'` — a **knowingly-false** value written into the privacy-contract trace, which would corrupt any analytics that group by `source_type` and is a poor pattern to set on the privacy surface. The honest `'query'` literal costs one union member + one adapter-test case and changes NO schema, NO key whitelist, NO chokepoint. **Tradeoff (founder call):** the recommended path touches the frozen adapter (small, additive, key-set-preserving) vs. the zero-adapter-touch alternative (pass `sourceType:'memory'` + correlation UUID, accept the cosmetically-false `source_type` because `input_type:'query'` is the real discriminator). Recommend the honest widening — don't write false data onto the privacy trace. |
| **OD-5** | **Grounding / "I don't know" boundary.** 02-CONTEXT.md locks "max similarity < threshold → I don't know" (start 0.6 cosine). Does the retriever decide this? | **NO — retrieve.ts returns ranked Candidates + raw scores; 02-07 (KNW-05c) owns the threshold + the "I don't know" decision (RECOMMENDED).** | Keeps 02-06 a pure read engine and keeps the grounding policy with the agent that synthesizes the answer (single responsibility). The retriever exposing vectorScore/ftsScore/rrfScore is exactly the evidence 02-07 needs to apply the 0.6-cosine floor without re-querying. Tradeoff: an empty/weak retrieval still returns `[]` or low-score candidates from 02-06 — correct, because "weak" is a policy judgment 02-07 makes against the locked threshold, not a retrieval-layer concern. |

---

<objective>
Build the read path for the Knowledge Layer: a tenant-scoped hybrid retriever (`src/ai/rag/retrieve.ts`) that embeds a founder's query through the existing Voyage adapter, runs a pgvector cosine search and a Postgres full-text search over the per-tenant `embeddings` rows the 02-04 pipeline wrote, fuses the two candidate lists by Reciprocal Rank Fusion, and returns ranked candidates with their raw vector + FTS scores for Plan 02-07's qa-rag agent to ground against. Retrieval only — no synthesis, no citations, no "I don't know" decision, no schema change (OD-1 Option A), no cost cap (PULL-OBS-COST-01-FORWARD ships in 02-07). Planning-only at this stage: produce the converged plan; execution and the founder-gated /codex + /cso fire later.
</objective>

<threat_model>
Security gate: `cso` (mandatory at execution close per standing rule 12). ASVS-relevant threats for the read path:

| ID | STRIDE | Threat | Mitigation (in this plan) |
|----|--------|--------|---------------------------|
| T-02-06-01 | **Information disclosure (cross-tenant)** | Retriever returns chunks from another tenant's memory/corpus. | Every query runs inside `getRequestClient(token).rls(fn)` (RLS default-deny: `account_id = jwt.tenant_id`) AND carries an explicit `WHERE account_id = <accountId>` predicate. No `getServiceClient()`/RLS-bypass anywhere in the retriever. Test (c) asserts the predicate on BOTH queries. |
| T-02-06-02 | **Information disclosure (query content leak)** | The founder's query — more sensitive than document text — leaks into a Langfuse trace, a log, or an error string. | Query goes ONLY to `voyage.embed`, whose trace is whitelisted to `TRACE_METADATA_KEYS` (no text). retrieve.ts adds no `logger.*` call containing the query. Test (g) pins query-text-absence from trace/log args. (OD-4 keeps the query out of the trace's `source_id` by using a correlation UUID.) |
| T-02-06-03 | **Injection (SQL / tsquery)** | A crafted query string breaks out of the FTS query or the SQL. | FTS uses `websearch_to_tsquery` (which safely parses user text and never errors on malformed input, unlike `to_tsquery`); all query/embedding values are bound parameters via drizzle `sql``` tagged templates — never string-concatenated into SQL. |
| T-02-06-04 | **DoS (unbounded fan-out) + invalid LIMIT** | A pathological or hostile `topK` (huge, zero, negative, NaN, non-integer) forces a huge candidate scan, an oversized payload, or an invalid SQL `LIMIT`. | `topK` is normalized FIRST: `effTopK = clamp(Math.trunc(topK ?? 8), 1, MAX_TOP_K=50)` — a bad `topK` can never reach the SQL (C-F1). Both sides `LIMIT effTopK*CANDIDATE_POOL_FACTOR`; final result truncated to `effTopK`; the tenant filter bounds the base set to one founder's chunks; the Voyage 8-text batch cap bounds the embed (1 query text). |
| T-02-06-05 | **Tampering (model-version skew)** | Retrieval mixes embeddings from two model versions during a rolling re-embed, corrupting cosine comparisons. | Both queries filter `embedding_model_version = 'voyage-3-large'`; the query is embedded with the same model (the adapter pins `voyage-3-large`). Cross-version rows coexist in the table but never co-rank. |

No `pull_request_target`, no new external surface, no new secret. The query embed is the only egress; it is the metered chokepoint for 02-07's OBS-COST-01.
</threat_model>

<tasks>

<task id="T01" type="execute" autonomous="true">
<title>OD-4 adapter query-trace widening + hybrid retriever src/ai/rag/retrieve.ts (vector + FTS + RRF, tenant-scoped)</title>

<read_first>
- src/ai/integrations/voyage.adapter.ts            # the query-embed entrypoint: voyage.embed({ texts, inputType:'query', trace }); the TRACE_METADATA_KEYS whitelist; the 8-text batch cap; the VoyageEmbedInput.trace shape (OD-4 wrinkle lives here)
- src/db/schema/embeddings.ts                       # the table being queried: columns (account_id, source_type, source_id, chunk_text, chunk_idx, embedding vector(1024), embedding_model_version), the HNSW vector_cosine_ops index, the dedup key, the per-tenant + source_type model (header lines 84-97 documenting FTS-in-02-06)
- src/db/client.ts                                  # getRequestClient(accessToken) → { rls, db }; the rls(fn) request-scoped tenant runner (SET LOCAL role=authenticated + request.jwt.claims) — the ONLY client retrieve.ts may use; getServiceClient is OFF-LIMITS here
- src/db/rls.ts                                     # tenantIsolationPolicy + NON_TENANT_TABLES — why account_id = jwt.tenant_id is enforced and why corpus embeddings live per-tenant in embeddings
- .planning/phases/02-knowledge-layer/02-CONTEXT.md # phase-level LOCKED retrieval decisions (line 46 hybrid pgvector+FTS fused; line 121/134 the 0.6-cosine "I don't know" threshold that 02-07 owns; line 221 retrieve.ts target)
- .planning/phases/02-knowledge-layer/02-04-SUMMARY.md # Hand-off to Plan 02-06 (lines 307-318): rows exist with source_type='memory'/'corpus'; voyage inputType:'query' entrypoint; HNSW cosine; model-version filter; query-text-not-in-trace
- tasks/constraints.md                              # AI chokepoint (Anthropic via client.ts; Voyage via adapter — never cross-import); RLS default-deny; banned strings; operator voice
- docs/BRAND.md                                     # operator voice for the docstring + comments ("retrieves/ranks/fuses", not "detects/handles/wants")
</read_first>

<action>
Create `src/ai/rag/retrieve.ts` exporting `hybridRetrieve(args, ctx)` and the `Candidate` + `HybridRetrieveArgs` types.

Signature: `hybridRetrieve({ accountId, query, topK = 8 }: HybridRetrieveArgs, ctx: { rls: <the request-tenant runner from getRequestClient>}): Promise<Candidate[]>`. The caller (Plan 02-07) supplies `ctx.rls` from `getRequestClient(accessToken)`; retrieve.ts NEVER constructs a service client.

Constants (documented): `CANDIDATE_POOL_FACTOR = 4` (per-side LIMIT = `effTopK * CANDIDATE_POOL_FACTOR`, scales with caller-supplied topK — F-6), `MAX_TOP_K = 50` (hard ceiling), `RRF_K = 60`, `EMBEDDING_MODEL_VERSION = 'voyage-3-large'`, `SOURCE_TYPES = ['memory','corpus'] as const`.

topK normalization (C-F1, T-02-06-04): derive `effTopK = clamp(Math.trunc(topK ?? 8), 1, MAX_TOP_K)` at the top of the function — a zero/negative/NaN/non-integer/oversized `topK` can never reach the SQL `LIMIT` (no invalid LIMIT, no unbounded scan, no oversized payload). All LIMITs and the final slice use `effTopK`.

Step 0 (OD-4 prerequisite) — widen the Voyage adapter trace union: in `src/ai/integrations/voyage.adapter.ts`, change `VoyageEmbedInput.trace.sourceType` from `'memory' | 'corpus' | 'interaction'` to `'memory' | 'corpus' | 'interaction' | 'query'`. Additive TS widening ONLY — do NOT touch `TRACE_METADATA_KEYS`, `buildTraceMetadata`, or any other key (Case 8b asserts the key SET, which is unchanged). This lets the query embed trace honestly as `source_type:'query'` instead of a false `'memory'`.

Step 1 — embed the query: `const [queryVec] = (await voyage.embed({ texts: [query], inputType: 'query', trace: { accountId, sourceType: 'query', sourceId: crypto.randomUUID() } })).embeddings` (OD-4 honest query-trace; NEVER `'memory'`). Add a comment at the call site: this is the read-path AI-token egress metered by 02-07's OBS-COST-01 (PULL-OBS-COST-01-FORWARD); it is the single Voyage call site in the read path.

Steps 2+3 — run BOTH queries inside a SINGLE `ctx.rls(async (tx) => { … })` so they share one transaction snapshot (consistent fusion input, one round-trip — F-5):
- **VECTOR side:** select `id, source_type, source_id, chunk_idx, chunk_text` and `cosineDistance(embeddings.embedding, queryVec)` aliased `dist`, `WHERE account_id = accountId AND source_type IN ('memory','corpus') AND embedding_model_version = 'voyage-3-large'`, `ORDER BY dist ASC LIMIT effTopK*CANDIDATE_POOL_FACTOR`. Use drizzle's `cosineDistance` helper. (Build the WHERE so it is `.toSQL()`-inspectable for the T02 tenant-predicate assertion — F-1.)
- **KEYWORD side:** a raw `sql`` tagged-template SELECT (OD-1 Option A, query-time tsvector — NO stored column): bind the query into `websearch_to_tsquery('english', ${query})` as `q`, select the same columns + `ts_rank_cd(to_tsvector('english', chunk_text), q)` aliased `rank`, `WHERE account_id = ${accountId} AND source_type IN ('memory','corpus') AND embedding_model_version = 'voyage-3-large' AND to_tsvector('english', chunk_text) @@ q`, `ORDER BY rank DESC LIMIT effTopK*CANDIDATE_POOL_FACTOR`. ALL user/runtime values bound as parameters (never concatenated — T-02-06-03).

Step 4 — RRF fuse keyed on `embeddings.id`: for each list, `score += 1 / (RRF_K + position)` with `position` 1-based (canonical RRF). A doc present in only one list still scores from that one list.

Step 5 — build `Candidate[]`: `{ sourceType, sourceId, chunkIdx, chunkText, vectorScore, ftsScore, rrfScore }` where `vectorScore = 1 - dist` (cosine SIMILARITY ∈ [-1,1], higher=better; `null` if the doc was an FTS-only hit — F-4) and `ftsScore = rank` (`null` if vector-only hit); sort by `rrfScore` desc, `slice(0, effTopK)`. Return `[]` when both sides are empty (no throw). A weak query (vector returns far rows, FTS empty) returns vector-only candidates with `ftsScore:null` — the grounding/"I don't know" call is 02-07's against these scores (OD-5), NOT a vector-side cutoff here.

Docstring (operator voice): retrieval-only (no synthesis/citation/"I don't know" — 02-07/KNW-05c); tenant-scoped (RLS + explicit predicate); query embed is the read-path egress 02-07 caps; OD-1 Option A (query-time FTS; GIN index deferred to FOLLOWUP-FTS-GIN-INDEX-01); `vectorScore` is cosine similarity not distance. Do NOT import `@anthropic-ai/sdk`; do NOT import `getServiceClient`.
</action>

<acceptance_criteria>
- `src/ai/rag/retrieve.ts` exists and exports `hybridRetrieve` + the `Candidate` and `HybridRetrieveArgs` types.
- `grep -n "voyage.embed" src/ai/rag/retrieve.ts` shows exactly ONE call, with `inputType: 'query'` and `sourceType: 'query'`.
- `grep -n "'query'" src/ai/integrations/voyage.adapter.ts` shows `'query'` added to the `trace.sourceType` union; `grep -c "" ` of `TRACE_METADATA_KEYS` membership is UNCHANGED vs baseline (the 8 keys are byte-identical — Case 8b stays green); `npx vitest run tests/ai/integrations/voyage.adapter.test.ts` exits 0 (existing Case 8/8b/8c green + the new query-trace case).
- `grep -n "cosineDistance" src/ai/rag/retrieve.ts` matches (vector side); `grep -nE "websearch_to_tsquery|ts_rank_cd|to_tsvector" src/ai/rag/retrieve.ts` matches (keyword side).
- `grep -nE "account_id|accountId" src/ai/rag/retrieve.ts` shows the tenant predicate present on BOTH the vector and keyword query; `grep -n "getServiceClient" src/ai/rag/retrieve.ts` returns ZERO hits.
- `grep -n "@anthropic-ai/sdk" src/ai/rag/retrieve.ts` returns ZERO hits; the ESLint Anthropic-boundary rule stays green.
- `grep -n "RRF_K = 60\|RRF_K=60" src/ai/rag/retrieve.ts` matches (the documented fusion constant); `grep -n "voyage-3-large" src/ai/rag/retrieve.ts` matches (model-version filter on both queries).
- `git diff --quiet 29228e8 -- src/db/schema/` exits 0 (no schema change — OD-1 Option A); zero new files under `src/db/migrations/`.
- `npm run typecheck` clean; `npm run lint` clean (0 new errors); `npm run check:banned` green.
</acceptance_criteria>
</task>

<task id="T02" type="execute" autonomous="true">
<title>Unit tests — tests/ai/rag/retrieve.test.ts (all live deps mocked) + adapter query-trace case</title>

<read_first>
- src/ai/rag/retrieve.ts                            # the unit under test (after T01)
- tests/ai/integrations/voyage.adapter.test.ts      # the MSW/mock convention for the voyage adapter; how the trace whitelist is pinned — mirror the mocking style
- tests/ai/eval/checks/cache-hit.test.ts            # the env-gate-keys-on-presence pattern: mock getLangfuseClient/the live dep so a present-but-invalid .env.local key can NEVER fire a real call (lessons.md 2026-06-01)
- tests/inngest/functions/embed-memory.test.ts      # how the db / drizzle query builder is stubbed in this repo (mock shape to assert WHERE predicates)
- tasks/lessons.md                                  # the env-gate-keys-on-credential-presence lesson; ALWAYS mock the live dependency in vitest
</read_first>

<action>
Create `tests/ai/rag/retrieve.test.ts`. `vi.mock` the Voyage adapter so `voyage.embed` resolves a fixed 1024-dim vector and records its call args (NO live Voyage). Stub `ctx.rls` so the two SQL queries return hand-constructed fixture rows and the test can inspect the issued query (WHERE predicates, LIMITs). NO live Postgres.

**Per-side predicate assertion (F-1 + C-F2):** do NOT copy the embed-memory fake's "ignore `_pred`" shortcut — it makes (c)/(d) pass vacuously. Use a STABLE harness, not private-object-shape poking: build the actual vector/FTS query objects with a `drizzle.mock({ schema })` builder (or the real query builder) and assert their `.toSQL()` `{ sql, params }` — `account_id`/`source_type`/`model-version` appear in the serialized SQL + params (vector side via the drizzle `.toSQL()`; FTS side via the raw `sql`` fragment's `.toSQL()`/bound params, where `accountId` is a bound param). A SEPARATE mocked executor returns the fixture rows for the fusion-logic cases. This proves the real query the executor will run carries the predicate, without depending on a fragile internal mock shape.

Cases (mirror the must_haves.artifacts test list): (a) RRF math correct on a two-list fixture — a doc at vector rank #1 + fts rank #3 scores `1/(60+1) + 1/(60+3)`, pinning RRF_K=60 and 1-based positions; (b) a vector-only-hit doc and an fts-only-hit doc both appear in the fused result; (c) the WHERE predicate carries `account_id = <passed accountId>` on BOTH queries (per the per-side mechanics above); (d) `source_type IN ('memory','corpus')` and `embedding_model_version='voyage-3-large'` on both queries; (e) both sides empty → returns `[]`, no throw; (f) `voyage.embed` called exactly once with `inputType:'query'` + `sourceType:'query'` + `texts:[query]`; (g) the query string never appears in any Langfuse trace arg nor any logger call captured by the mocks (query-text-not-traced); (h) `result.length <= effTopK` for a fixture larger than topK; (i) WEAK path (F-3) — FTS empty + vector returns low-similarity rows → result non-empty, `ftsScore:null` on every candidate, `rrfScore` from vector rank only, length ≤ effTopK; (j) topK normalization (C-F1) — `topK=0`, `topK=-5`, `topK=3.7`, and `topK=9999` each clamp to `effTopK ∈ [1,50]` (assert the serialized LIMIT param is the clamped integer; no invalid/NaN LIMIT reaches SQL).

**Adapter test (OD-4, in `tests/ai/integrations/voyage.adapter.test.ts`):** +1 case — a `voyage.embed({ inputType:'query', trace:{ sourceType:'query', … } })` call traces with `input_type:'query'` AND `source_type:'query'`; confirm the existing Case 8 (sentinel absence) / 8b (exact key set) / 8c (null-Langfuse no-op) stay green (the key set is unchanged by the union widening).
</action>

<acceptance_criteria>
- `tests/ai/rag/retrieve.test.ts` exists; `grep -nE "vi\\.mock|mockResolved" tests/ai/rag/retrieve.test.ts` matches (voyage + db mocked).
- `npx vitest run tests/ai/rag/retrieve.test.ts` exits 0 with ≥ 10 cases passing (a–j, where (j) pins topK normalization).
- `npx vitest run tests/ai/integrations/voyage.adapter.test.ts` exits 0 — the new `'query'`-trace case passes AND existing Case 8/8b/8c stay green (key set unchanged).
- Case (i) asserts `ftsScore:null` on every candidate and a non-empty result for the FTS-empty + vector-far-rows path.
- The suite makes ZERO network calls: a present-but-invalid `VOYAGE_API_KEY` in `.env.local` does not cause a real fetch (proven by the mock asserting `voyage.embed` is the mocked impl, not the real one).
- Case (c) + (d) assert the tenant + source_type + model-version predicates on BOTH the vector and keyword queries.
- Case (g) asserts the query text is absent from every recorded trace/logger arg.
- `npm run typecheck` clean; full `npx vitest run` shows no NEW failures beyond the documented pre-existing FOLLOWUP-HARDCODED-DOMAIN-REGEX-01 (the 2 trochia.asranest email/billing tests — D4 carve-out).
</acceptance_criteria>
</task>

</tasks>

<deferred_items>
- **FOLLOWUP-FTS-GIN-INDEX-01** (NEW) — When a tenant's `embeddings` chunk count makes the query-time `to_tsvector` non-trivial (~thousands/tenant), add the stored `to_tsvector('english', chunk_text)` GENERATED column + GIN index. This is the first legitimate Phase-2 schema change and lands WITH **FOLLOWUP-DBDIFF-01** (build the reusable `db:diff` drift tool) + a migration + a prod schema re-deploy. Until then, OD-1 Option A holds the schema lock. (Resolves the `embeddings.ts:89-97` documented intent — deferred, not abandoned.)
- **PULL-OBS-COST-01-FORWARD** (carried) — The $5/user/day aggregate cap (Anthropic + Voyage) ships in the 02-07 joint merge, gating at `src/ai/client.ts` + `src/ai/integrations/voyage.adapter.ts`. 02-06 adds the read-path query-embed egress at one chokepoint so the cap has a single read-side meter point; it does NOT add the cap.
- **FOLLOWUP-DBDIFF-01** (carried) — Stays deferred under OD-1 Option A; activates only if OD-1 resolves to Option B (stored column + GIN index) here, or at FOLLOWUP-FTS-GIN-INDEX-01.
- **Query-embed app-level cache** (NEW, optional) — A normalized-query LRU could cut repeat read-path Voyage egress + latency. Not needed for the KNW-05 <8s median (that budget is the 02-07 Opus synthesis, not the embed + two SQL queries). Revisit if 02-05's eval / 02-07's cost telemetry shows repeat-query egress is material.
- **Retrieval-quality eval (recall@k)** (NEW) — A retrieve.ts-specific eval is NOT in 02-06 (no labeled relevance fixtures yet). `qa-grounding.ts` stays `'pending'` until 02-07 lands the qa-rag agent; the end-to-end grounding quality is measured there. If retrieval recall needs its own gate, capture as a follow-up with a labeled fixture set.
</deferred_items>

<handoff_to_02_07>
Plan 02-07 (qa-rag agent + ambient Q&A sidebar + OBS-COST-01 — the joint write+read+cost-cap merge) inherits:
- **`hybridRetrieve(args, ctx)` is the read engine** — 02-07's qa-rag agent calls it with the request-scoped `ctx.rls` from the Q&A request's session, gets ranked `Candidate[]` with `vectorScore`/`ftsScore`/`rrfScore`, and applies the **0.6-cosine "I don't know" floor** (02-CONTEXT.md) against those scores WITHOUT re-querying. The grounding decision + citations + Opus synthesis are 02-07's, not the retriever's (OD-5).
- **PULL-OBS-COST-01-FORWARD lands here** — the $5/user/day cap must meter BOTH the Anthropic synthesis (client.ts) AND the Voyage query embed (the single retrieve.ts call site). Tests must show one tenant cannot drive aggregate daily spend over $5.
- **`qa-grounding.ts` flips from `'pending'` to real AND `'qa-grounding'` is removed from `PENDING_ALLOWED`** in `src/ai/eval/runner.ts` (G-EVAL-1 → 0 literal pending) — per 02-05 hand-off.
- **OD-1 revisit hook** — if 02-07's load profile or eval shows query-time FTS is a latency floor, FOLLOWUP-FTS-GIN-INDEX-01 (+ DBDIFF-01) is the schema-change path.
- **DEPLOY-DEFERRED** — the read path debuts to prod in this 02-07 joint merge; PR #7 flips DRAFT→ready then. Until then main stays untouched.
</handoff_to_02_07>

<deploy_deferred_note>
No prod deploy fires at this plan. 02-06 is branch-only on `phase-2-knowledge-layer`; PR #7 stays a DRAFT; main untouched at `acfab36`. The read path + write-path prod-debut + OBS-COST-01 cap ship together in the 02-07 joint merge. Founder-gated `/codex` + `/cso` fire at 02-06 execution close (standing rule 12), not at this planning stage.
</deploy_deferred_note>
