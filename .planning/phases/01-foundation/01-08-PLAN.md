---
phase: 01-foundation
plan: 08
type: execute
wave: 3
depends_on: [2, 6]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/pricing/page.tsx
  - src/app/(marketing)/manifesto/page.tsx
  - src/app/(marketing)/legal/privacy/page.tsx
  - src/app/(marketing)/legal/terms/page.tsx
  - src/app/(marketing)/legal/security/page.tsx
  - src/app/(marketing)/legal/dpa/page.tsx
  - src/components/marketing/hero-timeline.tsx
  - src/components/marketing/how-it-works.tsx
  - src/components/marketing/modules-grid.tsx
  - src/components/marketing/founder-voices.tsx
  - src/components/marketing/pricing-cards.tsx
  - src/components/marketing/faq-accordion.tsx
  - src/content/manifesto.mdx
  - e2e/marketing.spec.ts
  - .lighthouserc.json
autonomous: true
requirements: [FND-01, FND-08, XC-05]
must_haves:
  truths:
    - "The marketing site renders from the same repo: / (homepage), /pricing (all 4 tiers, Active Raise featured, Close Mode + Alumni with the 'Available with the close stack' V3 badge and no purchase CTA), /manifesto (1500–2000 word draft), /legal/{privacy,terms,security,dpa}"
    - "The homepage hero is left-aligned (centered hero text is banned) with the animated raise timeline (Memory → Pitch Lab → Pipeline → Live Raise → Close, Ink→Signal sequentially, looping) that respects prefers-reduced-motion (→ static composition)"
    - "Lighthouse > 90 (perf / a11y / best-practices / SEO) on / — a Phase 1 exit gate"
    - "The marketing top bar nav is How it works / Pricing / Manifesto (no Docs/Changelog); the footer product-nav is Pricing / Manifesto / Status (no Changelog)"
    - "/legal/dpa links to the downloadable public/legal/dpa.pdf and shares content with the DPA module (Plan 06); all marketing/legal copy passes the banned-string check"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "the 8-section homepage: hero (left-aligned + animated timeline) → trust strip → how it works (4 numbered steps) → modules grid (2×3) → founder voices (2 placeholder testimonials) → pricing teaser (4 cards) → final CTA → footer"
    - path: "src/app/(marketing)/pricing/page.tsx"
      provides: "all 4 tiers + feature lists + monthly/annual Tabs + the full feature-matrix table + the 8-question FAQ Accordion"
    - path: "src/app/(marketing)/manifesto/page.tsx"
      provides: "the long-form manifesto (1500–2000 words, operator voice, pull quotes with the 4px Signal left-border bar)"
    - path: "src/app/(marketing)/legal/dpa/page.tsx"
      provides: "the DPA page — content from Plan 06's dpa module + a download link to public/legal/dpa.pdf"
    - path: ".lighthouserc.json"
      provides: "the Lighthouse CI config asserting ≥0.9 on / — now flipped from soft-fail to required"
  key_links:
    - from: "src/app/(marketing)/pricing/page.tsx"
      to: "src/server/routers/billing.ts (Plan 07 billingRouter.tiers)"
      via: "renders the TIERS data"
      pattern: "billing\\.tiers|TIERS"
    - from: "src/app/(marketing)/legal/dpa/page.tsx"
      to: "src/lib/compliance/dpa.ts (Plan 06)"
      via: "shared DPA content + the PDF download link"
      pattern: "dpa\\.pdf|DPA_VERSION|dpa-content"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/marketing/{marketing-top-bar,footer}.tsx (Plan 02)"
      via: "wraps every marketing page"
      pattern: "MarketingTopBar|Footer"
---

<objective>
Build the public marketing site from the same repo (FND-01): the homepage (`/` — the 8-section layout from the UI-SPEC: left-aligned hero with the animated raise-timeline live element, trust strip, "How it works" 4-step, the modules grid, founder voices (placeholder), the pricing teaser, the final CTA, the footer — Lighthouse > 90 on `/` is a Phase 1 exit gate), `/pricing` (all 4 tiers — Active Raise featured "Most chosen", Close Mode + Alumni with the "Available with the close stack" V3 badge and no purchase CTA per the UI-SPEC; the monthly/annual Tabs; the full feature-matrix table; the 8-question FAQ Accordion), `/manifesto` (a 1500–2000 word operator-voice draft), and `/legal/{privacy,terms,security,dpa}` (the DPA page links to `public/legal/dpa.pdf` from Plan 06 and shares its content). No `/docs` and no `/changelog` (founder decision). All copy uses the operator voice, the brand tokens, and passes the banned-string check (XC-05).

Purpose: the marketing site is the first impression for 25 design partners in a tight community; it must look operator-grade (harmonic.ai + firecrawl.dev aesthetic) and be fast (Lighthouse gate).
Output: the marketing routes + their section components + the manifesto content + the marketing e2e + the Lighthouse config flipped to required. Consumes the Plan-02 components (MarketingTopBar, Footer, Card, Tabs, Accordion, Avatar, Badge, motion primitives) and the Plan-07 billing tiers data; consumes Plan-06's DPA content + PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@docs/BRAND.md
@docs/DESIGN-REFERENCE.md
@docs/BRAND-PACK-README.md
@tasks/constraints.md
@CLAUDE.md

<interfaces>
<!-- UI-SPEC §"Route / Screen Contract" (public marketing surface) — the EXACT homepage section list (1 Hero left-aligned: eyebrow "THE AGENTIC OPERATOR FOR YOUR RAISE" text-label text-signal, H1 text-display "Run your raise from one operator." max-w-3xl, subhead text-body text-graphite max-w-xl, CTAs [Primary "Start your raise"][Link "See how it works →"], + the live element = the animated raise timeline Memory→Pitch Lab→Pipeline→Live Raise→Close, each module illuminating Ink→Signal sequentially 800ms/step, loop with 2s pause, SVG + Framer Motion (use the `motion` package), respects prefers-reduced-motion → static composition; NO secondary "live output" card — founder decision 2026-05-12; 2 Trust strip: single line text-body-sm text-graphite centered + a small honest-placeholder accelerator logo strip ("Trusted by" + fake logos is a banned anti-pattern — copy must be honest about being placeholder); 3 How it works: 4 numbered horizontal step cards (01–04 text-mono text-signal, H3 title, 2-sentence body, arrow connectors desktop): Drop your context → Business Memory; Trochia matches investors → Pipeline; Run your pitches → Live Raise; Close → Cap Table; 4 Modules "What Trochia operates": 2×3 grid of module cards (small Ink mark icon top-left, H3 name, 2–3 sentence body, "See how →" link): Business Memory, Pitch Lab, Investor Pipeline, Live Raise, Data Room, Raise Ops (SAFE + Cap Table); 5 Founder voices: 2 testimonial cards (quote text-h3 Geist 500, name+company+round text-mono-sm, 24px headshot) — PLACEHOLDER content until design partners exist; 6 Pricing teaser: 4 small cards ($49/$199/$399/$19) with one-line positioning, Close Mode + Alumni carry the subtle text-mono-sm text-graphite "Available with the close stack" badge, + "See full pricing →" → /pricing; 7 Final CTA: full-width py-32 centered: H2 "Stop juggling. Start raising.", subhead, Primary "Start your raise", optional secondary "Talk to founder" → calendar; 8 Footer. Lighthouse >90 on / is a Phase-1 exit gate.
/pricing: all 4 tiers in a card grid (side-by-side desktop, stacked mobile): tier name text-h3, price in Geist Mono text-4xl + /month (Inter graphite), one-line positioning, feature list (8–12 items, success-colored checkmark icons), CTA at bottom. Active Raise = featured (border-2 border-signal + "Most chosen" Signal badge top-right). Close Mode ($399) + Alumni ($19) carry the subtle "Available with the close stack" badge and do NOT show a purchase/checkout CTA (use a muted "Activates at launch" label or omit the CTA slot — never a live Stripe-checkout button). Monthly/annual Tabs (Pre-Raise $49/$39, Active Raise $199/$159; the close-stack tiers show their flat price with the badge, no toggle pricing). Below: full feature-matrix comparison table (all 4 tiers as columns) + FAQ Accordion (8 questions). The active-tier prices/feature lists come from Plan 07's billingRouter.tiers / the TIERS data.
/manifesto: long-form single column, credibility piece. Hero: title + author + date (max-w-3xl mx-auto). Body 1500–2000 words on why Trochia exists / what's broken about fundraising / the operator philosophy, set text-body 18px max-w-prose (~65ch). Pull quotes in larger Geist with a 4px Signal left-border bar. Section breaks use the divider pattern. (Content can be a strong draft for Phase 1; refined later — content authorship is the planner's call: a content subagent draft is fine.)
/legal/{privacy,terms,security,dpa}: long-form legal pages using the /manifesto type treatment (text-body 16–17px, max-w-prose). The DPA page is the target of the sign-up clickwrap and links to public/legal/dpa.pdf (Plan 06) — and the DPA content is shared from src/lib/compliance/dpa.ts / dpa-content.tsx (Plan 06). privacy/terms/security are Trochia-authored drafts (operator voice, banned-string clean, the "not legal advice" disclaimer where applicable — but these are the company's own legal pages, not the Legal Stack module).
NO /docs, NO /changelog (founder decision 2026-05-12) — not in the nav or footer.
UI-SPEC §Motion Contract — hover 120ms ease-out, page enter 200ms fade-up, scroll reveal 300ms, hero live-element loop 800ms/step ease-in-out 2s pause; respect prefers-reduced-motion (Phase-1 exit-gate item). Use `motion` (NOT framer-motion).
UI-SPEC §Anti-Patterns — no gradient section backgrounds, no glassmorphism, no cursor-following spotlights, no centered hero text on desktop, no stock illustrations (show the mark / real product screenshots / code-output — never metaphor; Phase 1 has no real screenshots so the hero is the animated timeline + the mark), no carousels/auto-rotating banners (the slow logo strip is the only marquee-ish exception), no confetti/sparkles, no card drop shadows (1px stone borders), no scale-on-hover beyond 0.98 press, no "AI buddy" copy, no "Trusted by" + fake logos, no Tailwind colors/fonts outside the token system, no bare generic CTA labels ("Start your raise" / "See how it works →" / "See full pricing →" — never bare "Submit"/"Learn more"), no banned compliance strings, no hardcoded site URLs, no pure #000/#FFF.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The marketing layout + the homepage (8 sections) + the hero animated raise-timeline component (prefers-reduced-motion-aware) + the section components</name>
  <files>src/app/(marketing)/layout.tsx, src/app/(marketing)/page.tsx, src/components/marketing/hero-timeline.tsx, src/components/marketing/how-it-works.tsx, src/components/marketing/modules-grid.tsx, src/components/marketing/founder-voices.tsx, src/components/marketing/pricing-cards.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"Route / Screen Contract" the homepage 8-section spec; §Spacing Scale — section padding py-20 md:py-32, max content width 1200px, prose max 65ch; §Typography; §Color; §Motion Contract; §Anti-Patterns; §Copywriting Contract — the marketing CTA copy, the footer tagline)
    - docs/DESIGN-REFERENCE.md (the harmonic.ai + firecrawl.dev aesthetic distillation; the page briefs for the homepage / hero / how-it-works / modules / pricing-teaser / final-CTA; the component patterns; the anti-patterns; the "docs feel like the product" principle — N/A for Phase 1 since no /docs ships)
    - docs/BRAND.md (tokens, typography, voice — wins on tokens/voice)
    - docs/BRAND-PACK-README.md (the mark assets — the hero/modules use the Ink mark; never recolor the dot to anything but Signal; never add shadow/glow/gradient/bevel/rotate)
    - src/components/marketing/marketing-top-bar.tsx, src/components/marketing/footer.tsx (Plan 02 — the layout wraps every marketing page in these), src/components/ui/* (Plan 02 — Card, Button, Badge, Avatar), src/components/primitives/section-divider.tsx (Plan 02)
    - src/lib/env.ts (Plan 01 — SITE_URL for the OG/canonical tags; never hardcode)
  </read_first>
  <action>
    Create `src/app/(marketing)/layout.tsx`: wraps every `(marketing)` page in `<MarketingTopBar />` + `{children}` + `<Footer />` (Plan 02); set per-page `metadata` defaults (title template, description, OG image from `public/og-image.png`, canonical from `SITE_URL` — never hardcoded). Create `src/components/marketing/hero-timeline.tsx`: a `'use client'` component rendering the animated raise timeline — an SVG (or a flex row of labelled nodes) `Memory → Pitch Lab → Pipeline → Live Raise → Close`, with each node illuminating Ink→Signal sequentially (800ms/step, `ease-in-out`), looping with a 2s pause at the end; use the `motion` package (NOT `framer-motion`); wrap the animation in a `prefers-reduced-motion: reduce` check (via a `useReducedMotion()` hook or a CSS media query) → when reduced, render the static composition (all nodes in their resting state, no animation). This is the SOLE hero live element (no secondary "live output" card — founder decision). Create `src/components/marketing/how-it-works.tsx`: the 4 numbered horizontal step cards (`01`–`04` in `text-mono text-signal`, `H3` title, a 2-sentence body, arrow connectors between cards on desktop): "Drop your context → Business Memory", "Trochia matches investors → Pipeline", "Run your pitches → Live Raise", "Close → Cap Table". Create `src/components/marketing/modules-grid.tsx`: the 2×3 grid of module cards (small Ink mark icon top-left, `H3` name, a 2–3 sentence body, a "See how →" link — for Phase 1 the links can point at `/pricing` or the `(app)` module routes since the per-module marketing pages don't exist yet): Business Memory, Pitch Lab, Investor Pipeline, Live Raise, Data Room, Raise Ops (SAFE + Cap Table) — each body in the operator voice (Trochia "drafts/matches/briefs/scores/tracks", not "feels/helps"). Create `src/components/marketing/founder-voices.tsx`: 2 testimonial cards (quote in `text-h3` Geist 500, name+company+round in `text-mono-sm`, a 24px `Avatar` headshot) — PLACEHOLDER content, clearly drafted-not-real (e.g. "[Design-partner quote — coming once partners ship]" or a generic operator-voiced placeholder; never imply a real relationship that doesn't exist). Create `src/components/marketing/pricing-cards.tsx`: a reusable component for the 4 tier cards (used by both the homepage pricing teaser — small variant, one-line positioning — and `/pricing` — full variant with the 8–12 item feature list); it reads the tiers data (the `TIERS` structure / `billing.tiers` from Plan 07): Pre-Raise $49/$39, Active Raise $199/$159 (the featured "Most chosen" card — `border-2 border-signal` + a Signal `Badge` top-right), Close Mode $399 + Alumni $19 (with the subtle `text-mono-sm text-graphite` "Available with the close stack" badge and NO purchase CTA — a muted "Activates at launch" label or no CTA slot); the active tiers' CTA is "Start your raise" → `/sign-up` (the actual Checkout is in the onboarding flow from Plan 07). Now build `src/app/(marketing)/page.tsx` composing the 8 sections in order: (1) Hero — left-aligned (NOT centered): eyebrow `THE AGENTIC OPERATOR FOR YOUR RAISE` (`text-label text-signal`), `H1 text-display` "Run your raise from one operator." (`max-w-3xl`), subhead `text-body text-graphite max-w-xl`, CTAs `[Button primary "Start your raise" → /sign-up] [Button link "See how it works →" → #how-it-works]`, and `<HeroTimeline />` (right column on desktop, below the subhead on mobile); (2) Trust strip — a single line `text-body-sm text-graphite` centered + a slow honest-placeholder accelerator-logo strip (the copy makes clear it's placeholder — e.g. "Built for the founders of [accelerator] — partnerships in progress"); (3) `<HowItWorks />` (anchor `#how-it-works`); (4) `<ModulesGrid />` (anchor `#modules`); (5) `<FounderVoices />`; (6) the pricing teaser — `<PricingCards variant="teaser" />` + a "See full pricing →" link → `/pricing`; (7) Final CTA — full-width `py-32` centered: `H2` "Stop juggling. Start raising.", a subhead, `Button primary` "Start your raise" → `/sign-up`, optional secondary "Talk to founder" → a calendar link (or omit if no calendar URL yet); (8) the `<Footer />` is in the layout. Apply the page-enter fade-up (200ms) and scroll-reveal (300ms) motions sparingly via `motion`, all `prefers-reduced-motion`-aware. No gradient section backgrounds, no card shadows, no centered hero, no stock illustrations, no carousels, no "Trusted by" + fake logos, no bare generic CTA labels, no banned strings, no hardcoded URLs, no pure #000/#FFF.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run check:banned && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run check:banned`, `npm run typecheck` exit 0
    - `src/app/(marketing)/page.tsx` renders all 8 sections in order; the hero H1 is "Run your raise from one operator." and is left-aligned (NOT centered) — the hero section's container does not use `text-center`/`mx-auto` for the headline block
    - `src/components/marketing/hero-timeline.tsx` uses the `motion` package (NOT `framer-motion`) and renders a static composition when `prefers-reduced-motion: reduce`
    - the homepage has NO secondary "live output" card (only the animated timeline as the hero live element)
    - `<PricingCards>` shows 4 tiers with Active Raise featured ("Most chosen" Signal badge) and Close Mode + Alumni carrying the "Available with the close stack" badge and NO purchase CTA
    - the trust strip uses honest placeholder copy (no "Trusted by" + fake logos)
    - `grep -rn "text-center" src/app/(marketing)/page.tsx` — the hero headline block is not centered (the final-CTA section being centered is fine; the hero must be left-aligned)
    - no card drop shadows (`grep -rn "shadow-" src/components/marketing/` returns nothing on Cards); no gradient section backgrounds; no `#FFF`/`#000`
  </acceptance_criteria>
  <done>The marketing layout + the 8-section homepage are built per the UI-SPEC: left-aligned hero with the animated, reduced-motion-aware raise timeline, the trust strip (honest placeholder), the 4-step how-it-works, the 2×3 modules grid, placeholder founder voices, the 4-tier pricing teaser, the final CTA — operator voice, brand tokens, no anti-patterns, banned-string clean.</done>
</task>

<task type="auto">
  <name>Task 2: /pricing (all 4 tiers + feature lists + monthly/annual Tabs + the full feature-matrix table + the 8-question FAQ Accordion) and the marketing e2e + the Lighthouse gate flipped to required</name>
  <files>src/app/(marketing)/pricing/page.tsx, src/components/marketing/faq-accordion.tsx, e2e/marketing.spec.ts, .lighthouserc.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"Route / Screen Contract" the /pricing spec — all 4 tiers card grid, Active Raise featured, Close Mode + Alumni "Available with the close stack" badge + no purchase CTA, monthly/annual Tabs, the feature-matrix table, the 8-question FAQ Accordion; the homepage pricing teaser feeds off the same data; §"Open Questions — RESOLVED" Q4 — SHOW ALL 4 TIERS with the V3 badge)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Validation Architecture" — the FND-01 smoke row loads `/`, `/pricing`, `/sign-up`, `/app` redirect; Lighthouse >90 via @lhci/cli on `/`)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14 — Lighthouse > 90 on `/` is a Phase 1 exit gate; D-02 the tier prices)
    - src/server/routers/billing.ts (Plan 07 — billingRouter.tiers / the TIERS data — the source of truth for tier prices + feature lists), src/components/marketing/pricing-cards.tsx (Task 1 — the reusable card component), src/components/ui/{tabs,accordion}.tsx (Plan 02)
    - .github/workflows/ci.yml + .lighthouserc.json (Plan 01 — the Lighthouse CI step is currently `continue-on-error: true` with a TODO to flip it once `/` ships — flip it now)
  </read_first>
  <action>
    Create `src/components/marketing/faq-accordion.tsx`: an 8-question FAQ `Accordion` (Plan 02 component) with operator-voiced Q&A copy (e.g. "Is there a free tier?" → "No — a 7-day free trial, card on file at signup, then $49 or $199/month. Free dilutes the buyer pool." (passes the banned-string check); "What happens after my raise?" → "Close Mode and the $19 Alumni tier — available with the close stack." ; "Do you train AI on my data?" → "No. Your business memory, decks, and transcripts never enter a training pipeline. See our DPA." (the "not" makes the compliance strings safe); "Which countries?" → "US, UK, and India today; the EU at V2." ; etc. — 8 questions, all banned-string-clean, all operator voice). Create `src/app/(marketing)/pricing/page.tsx`: the page hero (a short headline + subhead), then the monthly/annual `Tabs` toggle wired so the active-tier cards show the right price (Pre-Raise $49 / $39-annual, Active Raise $199 / $159-annual; the close-stack tiers show their flat price with the "Available with the close stack" badge, no toggle effect), then `<PricingCards variant="full" />` (the 4 tiers with the 8–12 item feature lists, `success`-colored checkmark icons, Active Raise featured), then the full feature-matrix comparison table (all 4 tiers as columns, feature rows with checkmarks/values — Geist Mono for any numeric cells), then `<FaqAccordion />`, then the `<Footer />` (from the layout). The active-tier CTA is "Start your raise" → `/sign-up`; the close-stack tiers carry no purchase CTA (a muted "Activates at launch" label or no CTA slot). Set the page `metadata` (title "Pricing — Trochia", description, OG). Flip the Lighthouse CI step: in `.lighthouserc.json` set `ci.collect.url` to `[${PLAYWRIGHT_BASE_URL or http://localhost:3000}/]` and `ci.assert.assertions` to require `categories:performance ≥ 0.9`, `categories:accessibility ≥ 0.9`, `categories:best-practices ≥ 0.9`, `categories:seo ≥ 0.9`; in `.github/workflows/ci.yml` remove the `continue-on-error: true` from the `lhci autorun` step so a Lighthouse regression on `/` fails CI (the Phase 1 exit gate). Write `e2e/marketing.spec.ts` (Playwright): assert `/` returns 200 and renders the hero H1 "Run your raise from one operator." (left-aligned) + the "Start your raise" CTA + the "See how it works →" link + the 4-step "How it works" section + the 6 module cards + the pricing teaser with 4 tiers; assert `/pricing` returns 200 and renders 4 tier cards (Pre-Raise / Active Raise / Close Mode / Alumni), the "Most chosen" badge on Active Raise, the "Available with the close stack" badge on Close Mode + Alumni, NO Stripe-checkout button on Close Mode/Alumni, the monthly/annual Tabs toggle (clicking "Annual" changes the displayed price for the active tiers), the feature-matrix table, and the 8-question FAQ Accordion; assert `/manifesto` and `/legal/{privacy,terms,security,dpa}` return 200 (content built in Task 3); assert the marketing top bar nav contains "How it works", "Pricing", "Manifesto" and does NOT contain "Docs" or "Changelog"; assert the footer product-nav contains "Pricing", "Manifesto", "Status" and does NOT contain "Changelog"; assert no console errors on `/`.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run check:banned && npx playwright test e2e/marketing.spec.ts && npx lhci autorun --collect.url=http://localhost:3000/ --upload.target=temporary-public-storage || echo "lhci run (may need the local server up — CI runs it against the preview)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run check:banned` exit 0
    - `e2e/marketing.spec.ts` passes: `/` and `/pricing` and `/manifesto` and `/legal/{privacy,terms,security,dpa}` all return 200; `/pricing` shows 4 tier cards with Active Raise featured ("Most chosen" badge), Close Mode + Alumni with the "Available with the close stack" badge and NO checkout button, the monthly/annual Tabs toggle changing the active-tier prices, the feature-matrix table, the 8-question FAQ; the marketing top bar nav = How it works / Pricing / Manifesto (no Docs/Changelog); the footer product-nav = Pricing / Manifesto / Status (no Changelog)
    - `.github/workflows/ci.yml`'s `lhci autorun` step has no `continue-on-error: true` (the Lighthouse gate is now required); `.lighthouserc.json` asserts ≥0.9 on perf/a11y/best-practices/SEO for `/`
    - the `/pricing` tier prices + feature lists come from the Plan-07 `TIERS` data (not hardcoded duplicates)
    - all FAQ + pricing copy passes `npm run check:banned`
    - Lighthouse ≥ 90 on `/` (verified in CI against the Vercel preview; locally `lhci` may need the server up — the requirement is the CI gate)
  </acceptance_criteria>
  <done>/pricing renders all 4 tiers (Active Raise featured, Close Mode + Alumni with the V3 badge and no checkout CTA), the monthly/annual Tabs, the feature-matrix table, and the 8-question FAQ — fed by the Plan-07 tiers data; the marketing e2e covers the homepage + pricing + legal routes + the nav contracts; the Lighthouse > 90 gate on `/` is flipped to required in CI.</done>
</task>

<task type="auto">
  <name>Task 3: /manifesto (1500–2000 word operator-voice draft) and /legal/{privacy,terms,security,dpa}</name>
  <files>src/app/(marketing)/manifesto/page.tsx, src/content/manifesto.mdx, src/app/(marketing)/legal/privacy/page.tsx, src/app/(marketing)/legal/terms/page.tsx, src/app/(marketing)/legal/security/page.tsx, src/app/(marketing)/legal/dpa/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"Route / Screen Contract" the /manifesto spec — long-form single column, hero title+author+date max-w-3xl mx-auto, body 1500–2000 words text-body 18px max-w-prose ~65ch, pull quotes in larger Geist with a 4px Signal left-border bar, section breaks use the divider pattern, content can be a strong draft; the /legal/* spec — same type treatment, text-body 16–17px max-w-prose, the DPA page is the sign-up clickwrap target; §Copywriting Contract — the operator voice; §Anti-Patterns — no "AI buddy" copy, no anthropomorphic verbs, no banned strings)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14 — /manifesto 1500–2000 word draft, content authorship is the planner's call: a content subagent draft is fine, refined later; D-11 — the DPA page is the /legal/dpa clickwrap target)
    - docs/BRAND.md (voice — operator not assistant; Trochia drafts/matches/briefs/scores/tracks, not feels/loves/wants/helps; no emoji)
    - .planning/PROJECT.md (the "Why now" / "Why founders pay" / "What customers must believe" framing — raw material for the manifesto)
    - .planning/intel/Trochia_AI_Strategy_v1.md (strategic context — more manifesto raw material)
    - src/lib/compliance/dpa.ts / src/lib/compliance/dpa-content.tsx (Plan 06 — the DPA content the /legal/dpa page renders + the link to public/legal/dpa.pdf; DPA_VERSION)
    - src/components/primitives/section-divider.tsx + legal-disclaimer-banner.tsx (Plan 02), tasks/banned-strings.txt (Plan 01 — all this copy passes the check)
  </read_first>
  <action>
    Write `src/content/manifesto.mdx` (or a plain TSX content module — your call; MDX is fine if you wire `@next/mdx`, otherwise a structured TSX component is simpler): a 1500–2000 word operator-voice essay on why Trochia exists / what's broken about fundraising (the founder juggling 6 tools, a botched investor meeting costing $25K–$250K of expected value, general AI not knowing your business, the memory + workflow-ownership moat) / the operator philosophy (Trochia operates ALONGSIDE the founder — it drafts, matches, briefs, scores, tracks; it does not pitch, does not speak in calls, does not send autonomously; the founder approves every external action) / the privacy posture ("it's defensibly mine, not training someone else's model"). Operator voice: short sentences, concrete nouns, verbs that move; no emoji; no "AI buddy" tone; no anthropomorphic verbs; no banned compliance strings (never "rolling fund", "investment advice", "legal advice" without "not"). A content subagent draft is acceptable — it will be refined later. Create `src/app/(marketing)/manifesto/page.tsx`: render the content with the UI-SPEC type treatment — hero `max-w-3xl mx-auto` (title `text-h2` or `text-display`, author + date `text-mono-sm text-graphite`), body `text-body` 18px `max-w-prose` (~65ch, centered column), pull quotes in larger Geist with a `border-l-4 border-signal pl-6` bar, section breaks via `<SectionDivider />`; set `metadata`. Create `src/app/(marketing)/legal/privacy/page.tsx`, `terms/page.tsx`, `security/page.tsx`: Trochia-authored legal drafts (operator voice, banned-string clean — these are the COMPANY's own privacy/terms/security pages, not the Legal Stack module; the privacy policy references `docs/vendor-data-flow.md`'s vendors as sub-processors and the no-training commitment (XC-01); the terms reference the 7-day trial / no-permanent-free-tier / the DPA; the security page describes RLS / encryption at rest / SOC 2 Type I prep (Phase 11) / the vendor posture) — same type treatment as `/manifesto` (`text-body` 16–17px `max-w-prose`); each with `metadata`; where a "this is not legal advice" disclaimer is appropriate, use the `LegalDisclaimerBanner` (Plan 02) variant (the allowlisted "not legal advice" form). Create `src/app/(marketing)/legal/dpa/page.tsx`: render the DPA content from `src/lib/compliance/dpa.ts` / `dpa-content.tsx` (Plan 06 — the SAME source the PDF is generated from), with a prominent "Download PDF" link → `/legal/dpa.pdf` (i.e. `public/legal/dpa.pdf`), the `DPA_VERSION` shown, and a note that signing up constitutes clickwrap acceptance; same type treatment; `metadata`. All four legal pages + the manifesto must pass `npm run check:banned`.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run check:banned && npx playwright test e2e/marketing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run check:banned` exit 0
    - `/manifesto` renders a 1500–2000 word essay (a `wc -w` on the content module is in that range), operator voice, the UI-SPEC type treatment (hero `max-w-3xl mx-auto`, body `max-w-prose` ~65ch 18px, pull quotes with a 4px Signal left-border bar, section dividers)
    - `/legal/privacy`, `/legal/terms`, `/legal/security` render Trochia-authored drafts in the `/manifesto` type treatment; the privacy page references the sub-processor list + the no-training commitment
    - `/legal/dpa` renders the DPA content from `src/lib/compliance/dpa.ts` (Plan 06's shared source — not a duplicate), shows `DPA_VERSION`, and has a working "Download PDF" link to `/legal/dpa.pdf`
    - `e2e/marketing.spec.ts` (from Task 2) passes — including the `/manifesto` and `/legal/*` 200 checks
    - all manifesto + legal copy passes `npm run check:banned` (no banned compliance strings; the allowlisted "not legal advice" form where used)
  </acceptance_criteria>
  <done>/manifesto ships a 1500–2000 word operator-voice draft with the UI-SPEC type treatment; /legal/{privacy,terms,security} ship Trochia-authored drafts; /legal/dpa renders Plan 06's shared DPA content + links to the downloadable PDF — all banned-string clean, all in the brand type system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → marketing pages | static-ish content; no auth; no user data; the risk is mostly content (compliance strings, misleading claims) and performance/SEO |
| marketing copy → compliance | banned strings / misleading "Trusted by" / anthropomorphic "AI buddy" copy are the real exposure here |
| /legal/dpa ↔ the DPA module | the page and the PDF must show the SAME DPA text/version — a drift means founders accept terms that differ from what they can download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-47 | Repudiation / Spoofing (brand) | the marketing copy contains a banned compliance string ("rolling fund", "investment advice", "legal advice" without "not") | mitigate | `npm run check:banned` runs in CI and is a verify step on every task here; the FAQ/manifesto/legal copy uses the allowlisted "not legal advice" form; Code Reviewer rejects violations |
| T-1-48 | Spoofing (brand) | "Trusted by" + fake accelerator logos imply relationships that don't exist | mitigate | the trust strip uses honest placeholder copy ("partnerships in progress"); the "Trusted by" + fake-logos pattern is banned by the UI-SPEC anti-patterns; the founder-voices section is clearly placeholder |
| T-1-49 | Tampering | the `/legal/dpa` page and `public/legal/dpa.pdf` show different DPA text/version | mitigate | both render from the single source `src/lib/compliance/dpa.ts` / `dpa-content.tsx` (Plan 06); the PDF is generated from that source at build time; `DPA_VERSION` is shown on the page |
| T-1-50 | Tampering (config) | a hardcoded `https://trochia...` URL in an OG tag / canonical / CTA link breaks the trochia.ai migration | mitigate | all URLs (OG, canonical, internal links) come from `@/lib/env` `SITE_URL`/`APP_URL` or are relative paths; the ESLint `no-restricted-syntax` rule (Plan 01) flags literal `https://trochia` URLs; Code Reviewer backstop |
| T-1-51 | Denial of Service (perf budget) | the marketing site ships heavy JS / unoptimized images → Lighthouse < 90, slow first impression | mitigate | the Lighthouse CI gate (≥0.9 perf/a11y/best-practices/SEO on `/`) is required (this plan flips it from soft-fail); the hero animation is the only client JS of note and is reduced-motion-aware; images use `next/image`; SSG/ISR for the static pages |
| T-1-52 | Information Disclosure | the marketing site exposes a build artifact / env var | accept | the marketing pages are static-ish with no server data; `NEXT_PUBLIC_*` vars are by-design public; no secrets are referenced in `(marketing)` |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run check:banned && npm run typecheck` exit 0
- `npx playwright test e2e/marketing.spec.ts` passes (homepage 8 sections + /pricing 4 tiers + /manifesto + /legal/* + the nav/footer contracts)
- Lighthouse ≥ 90 on `/` (CI, against the Vercel preview); the `lhci` step is no longer `continue-on-error`
- the hero is left-aligned; uses `motion` not `framer-motion`; reduced-motion-aware
- no `/docs` or `/changelog` route, nav item, or footer link
- /legal/dpa shares content with Plan 06's dpa module + links to public/legal/dpa.pdf
- all copy banned-string clean
</verification>

<success_criteria>
The marketing site renders from the same repo (FND-01): `/` (the 8-section homepage — left-aligned hero with the animated, reduced-motion-aware raise timeline; trust strip with honest placeholder; 4-step how-it-works; 2×3 modules grid; placeholder founder voices; 4-tier pricing teaser; final CTA — Lighthouse > 90 gate flipped to required), `/pricing` (all 4 tiers with Active Raise featured, Close Mode + Alumni with the "Available with the close stack" V3 badge and no purchase CTA, the monthly/annual Tabs, the feature-matrix table, the 8-question FAQ), `/manifesto` (a 1500–2000 word operator-voice draft), `/legal/{privacy,terms,security,dpa}` (Trochia-authored drafts; the DPA page shares Plan 06's content + links to the PDF). No `/docs`/`/changelog`. Operator voice, brand tokens, no anti-patterns, banned-string clean (XC-05), no hardcoded URLs (FND-08). The marketing e2e + the Lighthouse gate pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md` documenting: the homepage section components + the hero-timeline implementation (the `motion` API used, the reduced-motion fallback), the `/pricing` structure + how it consumes the Plan-07 tiers data, the manifesto's word count + a one-line summary of its argument, the legal pages' contents + how `/legal/dpa` shares Plan 06's source, the Lighthouse CI config + the flip-to-required, and the marketing e2e coverage.
</output>
