---
phase: 01-foundation
plan: 07
type: execute
wave: 3
depends_on: [2, 3, 5, 6]
files_modified:
  - src/app/proxy.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/app/(auth)/sign-up/page.tsx
  - src/app/(auth)/sign-in/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/auth/callback/route.ts
  - src/app/(app)/app/page.tsx
  - src/app/(app)/layout.tsx
  - src/app/reactivate/page.tsx
  - src/modules/billing/entitlements.ts
  - src/modules/billing/tiers.ts
  - src/modules/billing/state.ts
  - src/modules/billing/dedupe.ts
  - src/modules/billing/checkout.ts
  - src/modules/billing/portal.ts
  - src/server/routers/billing.ts
  - src/server/routers/index.ts
  - src/app/api/webhooks/stripe/route.ts
  - tests/billing/entitlements.test.ts
  - tests/billing/checkout-session.test.ts
  - tests/billing/webhook-idempotency.test.ts
  - tests/billing/tiers.test.ts
  - e2e/skeleton.spec.ts
  - e2e/auth.spec.ts
  - .planning/phases/01-foundation/SKELETON.md
autonomous: false
requirements: [FND-04, FND-05, FND-06, FND-12]
must_haves:
  truths:
    - "A founder can sign in with Google SSO (Supabase Auth); the session persists 30 days and refreshes on activity (1h JWT expiry, 30d inactivity timeout; 90d absolute cap best-effort per platform support)"
    - "Stripe billing is live: Pre-Raise ($49/mo, $39/mo annual) and Active Raise ($199/mo, $159/mo annual), a self-serve Customer Portal, a 7-day free trial, card-on-file captured at signup, no permanent free tier"
    - "The Stripe webhook is idempotent (dedupes on event id) with a reconciliation path (Plan 04's reconcile-stripe cron); it writes accounts.subscription_status / tier / current_period_end on checkout.session.completed, customer.subscription.updated/deleted, invoice.payment_failed"
    - "entitlements(account) gates features by the persisted Stripe tier — never calls Stripe inline — and is extensible to Close Mode / Alumni as a data change, not a refactor"
    - "/app is hard-gated from day 1: no active trial-or-subscription → redirect to /reactivate"
    - "The Walking Skeleton works end-to-end deployed on Vercel: signup → tier picker → Stripe Checkout → webhook updates accounts.subscription_status → the founder lands on /app showing their tier"
  artifacts:
    - path: "src/modules/billing/entitlements.ts"
      provides: "entitlements(account) → { tier, features }; assertEntitled(feature) tRPC middleware (replaces Plan 03's stub)"
    - path: "src/modules/billing/tiers.ts"
      provides: "the tier→features map as data (pre_raise, active_raise; close_mode/alumni present but inactive — Phase 11); the Stripe price-id mapping"
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "the idempotent Stripe webhook handler — verify sig → dedupe on event.id → applySubscriptionState → emit Inngest event"
    - path: "src/app/proxy.ts"
      provides: "@supabase/ssr cookie refresh on every request + the /app subscription gate (was middleware.ts — Next 16 uses proxy.ts)"
    - path: "src/app/(auth)/sign-up/page.tsx"
      provides: "the sign-up screen with Google SSO + email + the legal/DPA clickwrap lines (per UI-SPEC)"
    - path: ".planning/phases/01-foundation/SKELETON.md"
      provides: "the Walking Skeleton record — architectural decisions the rest of the project builds on"
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/modules/billing/state.ts"
      via: "applySubscriptionState(event) → updates accounts.subscription_status"
      pattern: "applySubscriptionState|subscription_status"
    - from: "src/app/proxy.ts"
      to: "src/modules/billing/entitlements.ts"
      via: "the /app gate checks the persisted subscription status"
      pattern: "entitlements|subscription_status|reactivate"
    - from: "src/app/(app)/app/page.tsx"
      to: "src/server/routers/account.ts (Plan 03 accountRouter.me)"
      via: "renders the founder's tier"
      pattern: "account\\.me|tier"
    - from: "src/modules/billing/entitlements.ts"
      to: "src/server/trpc.ts (Plan 03 assertEntitled stub)"
      via: "replaces the stub with the real check"
      pattern: "assertEntitled"
---

<objective>
**The Walking Skeleton (D-08).** Build the thinnest real end-to-end slice that exercises the whole foundation — `signup → tier picker → Stripe Checkout → webhook updates accounts.subscription_status → the founder lands on /app showing their tier` — and deploy it on Vercel. Concretely: Supabase Auth Google SSO (FND-04, 30-day sessions per D-10), Stripe billing (FND-05 — Pre-Raise $49/$39-annual + Active Raise $199/$159-annual, Customer Portal, 7-day trial, card-on-file at signup, idempotent webhooks + the reconciliation cron from Plan 04), `entitlements()` (FND-06 — tier→features as data, extensible to Close Mode/Alumni; replaces Plan 03's `assertEntitled` stub), the hard `/app` gate (D-02b — no active sub → `/reactivate`), and the onboarding entry (the sign-up screen + the post-sign-in welcome + the tier picker + Checkout — the funnel events from Plan 05's taxonomy fire here; the rest of the onboarding stepper is Plan 09). Emit `SKELETON.md`. "If this slice works, the foundation works."

Purpose: this is the slice that proves auth + RLS + Stripe + webhook + tRPC + `entitlements()` all work together, deployed. Every later phase builds vertical slices on top of it without re-litigating these architectural decisions.
Output: the auth wiring + proxy.ts gate, the billing module + webhook + Customer Portal, `entitlements()`, the sign-up/sign-in/welcome screens + the tier picker + Checkout, `/app` showing the tier, `/reactivate`, the billing tests + the skeleton/auth e2e specs, the Vercel deploy, and `SKELETON.md`.

**`autonomous: false`** — requires (a) a human to configure Google OAuth in the Supabase Auth dashboard (the OAuth consent screen + redirect URLs), (b) a human to create the Stripe products/prices (Pre-Raise + Active Raise, monthly + annual) and the webhook endpoint in the Stripe dashboard and inject the secret + webhook-signing-secret + price IDs into Vercel env, (c) a human to set the Supabase Auth session-timer config (D-10: 1h JWT expiry / 30d inactivity / 90d absolute — confirm which of the three the platform exposes per RESEARCH.md Q2), and (d) a human to confirm the Vercel deploy succeeds at `trochia.asranest.com`. The executor does all the code wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/skeleton-template.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@docs/BRAND.md
@docs/DESIGN-REFERENCE.md
@tasks/constraints.md
@CLAUDE.md

<interfaces>
<!-- RESEARCH.md §"Code Examples" — the Stripe webhook handler (idempotent): verify sig with stripe.webhooks.constructEvent → if isEventProcessed(event.id) return 200 → switch on checkout.session.completed / customer.subscription.updated / customer.subscription.deleted / invoice.payment_failed → applySubscriptionState(event) (updates accounts.subscription_status/tier/current_period_end) → inngest.send({name:'billing/subscription.changed'}) → markEventProcessed(event.id) → 200. Dedupe store = Upstash Redis OR a processed_stripe_events table (Plan 03 didn't create that table — if you go the table route, add it as a small migration here; Redis is also fine).
RESEARCH.md §"Standard Stack" — stripe Node SDK + Stripe Tax: trial_period_days: 7 + payment_method_collection: 'always' (card-on-file with a trial); Customer Portal for self-serve; Products/Prices for Pre-Raise/Active Raise monthly+annual. @supabase/ssr — PKCE cookies in proxy.ts + server components; new publishable/secret keys (Plan 03 wired SUPABASE_PUBLISHABLE_KEY/SECRET_KEY).
RESEARCH.md §"Component Responsibilities" — proxy.ts (Supabase session cookie refresh on every request + redirect unauthenticated/un-subscribed away from (app) routes — Next 16 renamed middleware.ts→proxy.ts); modules/billing/entitlements.ts (entitlements(account) → {tier, features}, pure function over the persisted accounts.subscription_status + accounts.tier, never calls Stripe inline; assertEntitled(ctx,'feature') tRPC middleware consults it; tier→features map is data so adding Close Mode/Alumni is a data change); app/api/webhooks/stripe/route.ts (the idempotent handler).
RESEARCH.md §"Pattern 1" — assertEntitled was a stub in Plan 03 (`server/trpc.ts`, always-passes, TODO Plan 07) — replace it here with the real check via entitlements(ctx.account).
RESEARCH.md §"Pitfall 3" — non-idempotent Stripe webhook → wrong billing tier: dedupe on event.id + the reconciliation cron (Plan 04's reconcile-stripe — now wire its body to actually re-pull from Stripe and reconcile accounts). RESEARCH.md §"Open Question Q2" — confirm which of Supabase Auth's "JWT expiry limit" / "Inactivity timeout" / "Time-box user sessions" exist (D-10 fallback: JWT expiry + inactivity timeout are must-haves, 90d absolute cap best-effort).
D-02 onboarding order: Google sign-in → welcome → tier picker (Pre-Raise $49/$39 · Active Raise $199/$159) → Stripe Checkout (card-on-file, 7-day trial) → Knowledge Pack Import → deck upload → auto deck review (skeleton) → dashboard. THIS plan ships: sign-up/sign-in, the welcome screen, the tier picker, Checkout, and the dashboard /app showing the tier. The Knowledge-Pack-Import + deck-upload + auto-review STEPS of the stepper are Plan 09. The funnel Amplitude events (signup_started, welcome_viewed, tier_selected, checkout_started, checkout_completed [server-side from the webhook], dashboard_viewed) fire from this plan's screens + webhook (Plan 05 defined the names).
UI-SPEC §"Auth + onboarding shell": /sign-up = centered card max-w-md, Mark 48px, H3 "Start your raise", sub "Trochia operates alongside you through every stage.", Email Input full width, Primary "Continue with email" full width, Divider "or", "Continue with Google" (secondary variant full width — FND-04), footer link "Already have an account? Sign in →", legal line "By continuing you agree to our Terms and Privacy." + the DPA clickwrap line "By signing up you agree to our Data Processing Addendum." (links /legal/terms, /legal/privacy, /legal/dpa). /sign-in = same card, H3 "Welcome back", footer link "New to Trochia? Start raising →". Post-sign-in welcome = mark + "Welcome to Trochia" + "Let's set up your operator. Two quick steps." + Primary "Get started". UI-SPEC §"/app (dashboard)": app shell (sidebar+top bar from Plan 02), top bar title "Dashboard"; the empty-dashboard state + the three FND-12 CTA cards are Plan 09 — for the SKELETON, /app just needs to render the founder's tier (proving entitlements()).
UI-SPEC pricing: Pre-Raise $49/$39, Active Raise $199/$159; Active Raise is the featured "Most chosen" card; Close Mode $399 + Alumni $19 carry the "Available with the close stack" V3 badge and NO purchase CTA in Phase 1.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Auth Google SSO — server/client helpers, the proxy.ts session refresh + /app subscription gate, the auth callback route, the sign-up/sign-in screens + the post-sign-in welcome</name>
  <files>src/app/proxy.ts, src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/app/(auth)/sign-up/page.tsx, src/app/(auth)/sign-in/page.tsx, src/app/(auth)/layout.tsx, src/app/auth/callback/route.ts, src/app/(app)/layout.tsx, src/app/(app)/app/page.tsx, src/app/reactivate/page.tsx, e2e/auth.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-10 — Supabase Auth, Google SSO only at MVP, session config 30d inactivity / 90d absolute / 1h JWT; magic-link + MFA are V2; D-02b — /app hard-gated, no active trial-or-subscription → /reactivate; D-12 — the onboarding funnel: Google sign-in → welcome → ...)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Standard Stack" @supabase/ssr PKCE cookies in proxy.ts + server components; §"Component Responsibilities" proxy.ts row; §"State of the Art" middleware.ts→proxy.ts (Next 16), Supabase publishable/secret keys; §"Open Question Q2" the session timers; §"Security Domain" V2/V3 Authentication/Session — HttpOnly Secure cookies via @supabase/ssr, 1h JWT + 30d inactivity, TOTP MFA deferred Phase 8)
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"Auth + onboarding shell" — the exact /sign-up, /sign-in, welcome screen copy + layout; §Copywriting Contract — the auth copy table; the legal line + DPA clickwrap line; "Bare generic CTA labels" anti-pattern)
    - src/server/context.ts, src/server/trpc.ts (Plan 03 — createTRPCContext uses @supabase/ssr; protectedProcedure), src/db/schema/tenancy.ts (Plan 03 — accounts, the region default, dpa_accepted_at), src/lib/compliance/dpa.ts (Plan 06 — recordDpaAcceptance, acceptDpa)
    - src/components/ui/* + src/components/shell/* (Plan 02 — Button/Input/Form/Card, AppShell), src/lib/analytics.ts (Plan 05 — track + the funnel event names), src/lib/env.ts (Plan 01 — SITE_URL/APP_URL, never hardcode)
  </read_first>
  <action>
    Provision (human): in the Supabase dashboard, enable the Google OAuth provider — configure the Google Cloud OAuth client (consent screen, authorized redirect URIs including `${SITE_URL}/auth/callback` and the Supabase callback) and paste the client id/secret into Supabase Auth; set the session-timer config (D-10: confirm which of "JWT expiry limit" (set to 1h), "Inactivity timeout" (set to 30d), "Time-box user sessions" (set to 90d if exposed) the dashboard offers — per RESEARCH.md Q2; document what was set). Add `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (the client-side ones — Plan 03 wired the server SUPABASE_URL/PUBLISHABLE_KEY/SECRET_KEY; ensure both the `NEXT_PUBLIC_*` client variants and the server variants exist) to `.env.local`/`.env.example`/Vercel env; update `src/lib/env.ts` accordingly.
    Create `src/lib/supabase/server.ts`: `createServerSupabaseClient()` using `@supabase/ssr`'s `createServerClient` with the Next 16 cookies API (read/write cookies in route handlers / server components / proxy) — per the current `@supabase/ssr` docs (Context7 `@supabase/ssr`). Create `src/lib/supabase/client.ts`: `createBrowserSupabaseClient()` for client components (the sign-up page's Google button uses it to call `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${APP_URL}/auth/callback` } })`). Create `src/app/proxy.ts` (NOT `middleware.ts` — Next 16): on every request, refresh the Supabase session cookies via `@supabase/ssr` (the standard "update session" pattern); then, for requests to `(app)` routes (`/app`, `/app/*`, `/onboarding` — but NOT `/styleguide`, which is open per Plan 02's TODO → now make `/styleguide` session-gated: require a logged-in user but not an active subscription), if there's no session → redirect to `/sign-in`; if there's a session but the account has no active trial-or-subscription (read `accounts.subscription_status` — `'trialing'` or `'active'` pass; anything else fails) → redirect to `/reactivate`. Use `entitlements()` (task 2) for the active-or-trialing check so the logic lives in one place. Create `src/app/auth/callback/route.ts`: the PKCE code-exchange handler — `supabase.auth.exchangeCodeForSession(code)`, then ensure an `accounts` row exists for the user (create one with `region: 'us'`, `subscription_status: 'none'` if first sign-in — this is the resolveAccount path; do it via `getServiceClient()` since the session isn't fully established yet, or via the Supabase client right after the exchange), then redirect: if the account has no DPA acceptance → continue to `/onboarding` (which starts with the welcome screen — the welcome screen records DPA acceptance via `acceptDpa` if the sign-up clickwrap was checked, OR the sign-up page records it before the OAuth redirect — wire one of these and document it), if onboarding incomplete → `/onboarding`, else → `/app`. Fire `track('signed_in')` server-side. Create `src/app/(auth)/layout.tsx`: a minimal centered layout (`max-w-md mx-auto py-24 px-6`, the Mark, no app shell). Create `src/app/(auth)/sign-up/page.tsx` per the UI-SPEC: Mark 48px centered, H3 "Start your raise", sub "Trochia operates alongside you through every stage.", an email `Input` (full width) + Primary `Button` "Continue with email" (full width — magic-link is V2, so for Phase 1 this can be disabled-with-a-tooltip "Email sign-in coming soon — use Google" OR a stub; Google SSO is the working path), a divider with "or", a `secondary`-variant full-width `Button` "Continue with Google" (calls `signInWithOAuth`; fires `track('signup_started')`), a footer link "Already have an account? Sign in →" → `/sign-in`, the legal line "By continuing you agree to our Terms and Privacy." (links `/legal/terms`, `/legal/privacy` — `text-body-sm text-graphite` centered) + the DPA clickwrap line "By signing up you agree to our Data Processing Addendum." (links `/legal/dpa`) — and record DPA/ToS acceptance (call `acceptDpa` etc.) at the appropriate point (on the welcome screen after the OAuth round-trip is the cleanest — document the choice). Create `src/app/(auth)/sign-in/page.tsx`: the same card, H3 "Welcome back", same sub, the Google button, footer link "New to Trochia? Start raising →" → `/sign-up`. Create `src/app/(app)/layout.tsx`: wraps `(app)` pages in the `AppShell` (Plan 02 — sidebar + top bar + content slot + the empty Q&A region); the welcome screen at `/onboarding` (built more fully in Plan 09) is shown OUTSIDE the full app shell or inside a minimal variant — for this plan, build just enough: a `/onboarding/welcome` (or `/onboarding` step 0) screen with Mark + "Welcome to Trochia" + "Let's set up your operator. Two quick steps." + Primary "Get started" → the tier picker (task 2) → Checkout → and then (Plan 9 fills the remaining steps) → `/app`; fire `track('welcome_viewed')`. Create `src/app/(app)/app/page.tsx`: inside the `AppShell`, top bar title "Dashboard"; for the SKELETON, render the founder's current tier — call `accountRouter.me` (Plan 03) and show "You're on the {tier} plan" (e.g. "Active Raise") + the subscription status + a link to `/app/billing` (Plan 09 builds the billing screen; for now the link can go to the Stripe Customer Portal directly via task 2's `portal.ts`); fire `track('dashboard_viewed')`. (The empty-dashboard state + the three FND-12 CTA cards are Plan 09 — `/app` here is the skeleton payoff: it proves `entitlements()` end-to-end.) Create `src/app/reactivate/page.tsx`: a screen for users with no active sub — "Your subscription isn't active." + a Button to re-enter the tier picker / Checkout (or open the Customer Portal). Write `e2e/auth.spec.ts` (Playwright): assert `/sign-up` and `/sign-in` render the expected headings + the Google button + the legal/DPA lines; assert visiting `/app` while unauthenticated redirects to `/sign-in`; (mock the OAuth round-trip if feasible, or use a test Supabase user / Supabase admin API to create a session) assert that a logged-in user with `subscription_status='none'` visiting `/app` redirects to `/reactivate`, and one with `'trialing'` reaches `/app` and sees their tier.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npx playwright test e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `src/app/proxy.ts` exists (NOT `src/middleware.ts`) and refreshes the Supabase session + gates `(app)` routes (no session → `/sign-in`; session but no active trial-or-sub → `/reactivate`); `/styleguide` is session-gated (logged-in only, no sub required)
    - `e2e/auth.spec.ts` passes: `/sign-up` renders "Start your raise" + "Continue with Google" + "By continuing you agree to our Terms and Privacy." + "By signing up you agree to our Data Processing Addendum."; `/sign-in` renders "Welcome back"; `/app` unauthenticated → `/sign-in`; logged-in `subscription_status='none'` → `/reactivate`; logged-in `'trialing'` → `/app` shows the tier
    - `src/app/auth/callback/route.ts` exchanges the PKCE code, ensures an `accounts` row exists (region `us`, status `none` on first sign-in), and redirects to `/onboarding` or `/app`
    - the sign-up/sign-in screens use the Plan-02 Button/Input/Form components and no bare generic CTA labels (the buttons are "Continue with Google" / "Continue with email" / "Sign in" / "Get started", never bare "Submit"/"Continue")
    - DPA + ToS acceptance is recorded (via `acceptDpa` from Plan 06) at a documented point in the sign-up→welcome flow
    - no hardcoded site URLs — `redirectTo`/callback URLs come from `@/lib/env` `APP_URL`
    - the funnel events `signup_started`, `signed_in`, `welcome_viewed`, `dashboard_viewed` fire at the right points
  </acceptance_criteria>
  <done>Supabase Auth Google SSO works (sign-up/sign-in screens per the UI-SPEC, the PKCE callback creating the account, the proxy.ts session refresh + the hard /app subscription gate → /reactivate, /styleguide now session-gated); the welcome screen + /app-shows-tier scaffold the skeleton; auth e2e passes; DPA/ToS acceptance recorded; no hardcoded URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Stripe billing — tiers + price-id map, entitlements(), the Checkout + Customer Portal flows, the idempotent webhook, the reconcile-stripe cron body; replace the assertEntitled stub</name>
  <files>src/modules/billing/tiers.ts, src/modules/billing/entitlements.ts, src/modules/billing/state.ts, src/modules/billing/dedupe.ts, src/modules/billing/checkout.ts, src/modules/billing/portal.ts, src/server/routers/billing.ts, src/server/routers/index.ts, src/app/api/webhooks/stripe/route.ts, src/server/trpc.ts, src/inngest/functions/reconcile-stripe.ts, tests/billing/entitlements.test.ts, tests/billing/checkout-session.test.ts, tests/billing/webhook-idempotency.test.ts, tests/billing/tiers.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02 — Stripe Checkout inside onboarding after welcome; the tiers Pre-Raise $49/$39 / Active Raise $199/$159; D-02b — entitlements() is the single gate, extensible to Close Mode/Alumni as a data change; D-02c — the webhook is the source of truth, writes accounts.subscription_status/tier/period on checkout.session.completed / customer.subscription.updated / customer.subscription.deleted / invoice.payment_failed; idempotent (dedupe on event id) + a reconciliation path (re-pull from Stripe); entitlements(account) reads persisted status, never calls Stripe inline; self-serve Customer Portal; the in-app cancel trigger still shows the destructive Dialog before redirect)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Code Examples" the idempotent Stripe webhook handler; §"Standard Stack" stripe Node SDK + Stripe Tax, trial_period_days:7 + payment_method_collection:'always', Customer Portal, Products/Prices for the two tiers monthly+annual; §"Component Responsibilities" entitlements.ts / the webhook handler / reconcile-stripe (the safety net); §"Pitfall 3" non-idempotent webhook; §"Pattern 1" assertEntitled was a Plan-03 stub to replace here; Assumption A5 — re-check the current Stripe API param names; A6 — a processed_stripe_events table is fine if no Upstash Redis)
    - .planning/phases/01-foundation/01-UI-SPEC.md (the /pricing tiers + the tier picker; Active Raise is the featured "Most chosen"; Close Mode $399 + Alumni $19 carry the "Available with the close stack" V3 badge and NO purchase CTA in Phase 1; the destructive "Cancel subscription" Dialog → Customer Portal)
    - src/db/schema/tenancy.ts, src/db/schema/billing.ts (Plan 03 — accounts.stripe_customer_id/subscription_status/tier/current_period_end; the subscriptions table), src/db/client.ts (Plan 03 — getServiceClient for the webhook, which has no user session), src/server/trpc.ts (Plan 03 — the assertEntitled stub with the `TODO(Plan 07)`; protectedProcedure)
    - src/inngest/client.ts, src/inngest/functions/reconcile-stripe.ts (Plan 04 — the cron whose body this plan fills), src/lib/analytics.ts (Plan 05 — track; checkout_completed fires server-side from the webhook via the node SDK), src/lib/email/client.ts + templates (Plan 05 — trial-ending / payment-failed emails), src/lib/env.ts (Plan 01 — STRIPE_*; never hardcode URLs)
  </read_first>
  <action>
    Provision (human): in the Stripe dashboard — create the Products + Prices: Pre-Raise (monthly $49, annual $39/mo billed yearly) and Active Raise (monthly $199, annual $159/mo billed yearly); enable Stripe Tax; create the Customer Portal configuration (allow plan switch between the two tiers + cancel); create the webhook endpoint pointing at `${SITE_URL}/api/webhooks/stripe` subscribed to `checkout.session.completed`, `customer.subscription.created/updated/deleted`, `invoice.payment_failed`, `invoice.paid`. Add `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and the four `STRIPE_PRICE_*` ids (`STRIPE_PRICE_PRE_RAISE_MONTHLY`, `_PRE_RAISE_ANNUAL`, `_ACTIVE_RAISE_MONTHLY`, `_ACTIVE_RAISE_ANNUAL`) to `.env.local`/`.env.example`/Vercel env; update `src/lib/env.ts` (required in prod). If you go the dedupe-table route (no Upstash): add a tiny `processed_stripe_events` table (`event_id text pk, processed_at timestamptz default now()`) as a Drizzle migration here + `npm run db:push` it (this is the only schema change this plan makes — RLS not needed since it's a non-tenant system table; the schema-scan test's allowlist must include it — update Plan 03's allowlist or add it here).
    Create `src/modules/billing/tiers.ts`: `type Tier = 'pre_raise' | 'active_raise' | 'close_mode' | 'alumni'`; a `TIERS` data structure mapping each tier → `{ displayName, monthlyPriceUsd, annualPriceUsd, stripePriceIds: { monthly, annual }, features: string[], active: boolean }` — `pre_raise` and `active_raise` are `active: true` with their price ids from env; `close_mode` ($399) and `alumni` ($19) are `active: false` with no price ids (Phase 11 activates them); the `features` arrays are the FND-06 feature gates (Phase 1 has few real features — list the module names: `'business_memory'`, `'pitch_lab'`, `'investor_pipeline'`, `'live_raise'`, `'data_room'`, `'raise_ops'` — and which tier unlocks which; for Phase 1, both active tiers can unlock the same set since the modules are mostly stubs — the point is the SHAPE is data-driven). A `priceIdToTierAndPeriod(priceId): { tier, period } | null` reverse lookup (the webhook uses it). Create `src/modules/billing/entitlements.ts`: `export function entitlements(account: { subscription_status: string; tier: string | null }): { tier: Tier | null; features: string[]; active: boolean }` — a PURE function: if `subscription_status` is `'trialing'` or `'active'` → return `{ tier: account.tier as Tier, features: TIERS[tier].features, active: true }`; else → `{ tier: null, features: [], active: false }`. NEVER calls Stripe. Also `export function isActiveOrTrialing(account): boolean` (used by `proxy.ts`'s gate and `/reactivate`'s check). Replace the `assertEntitled` stub in `src/server/trpc.ts`: `assertEntitled(feature: string)` → a `protectedProcedure` that does `const ent = entitlements(ctx.account); if (!ent.features.includes(feature)) throw new TRPCError({ code: 'FORBIDDEN' }); return next();` — remove the `TODO(Plan 07)` comment. Create `src/modules/billing/state.ts`: `applySubscriptionState(event: Stripe.Event, db): Promise<void>` — given a `checkout.session.completed` / `customer.subscription.*` / `invoice.payment_failed` event, extract the customer id + the subscription's price id + status, look up the `accounts` row by `stripe_customer_id`, and update `subscription_status` (mapping Stripe statuses → `'trialing'`/`'active'`/`'past_due'`/`'canceled'`/`'none'`), `tier` (via `priceIdToTierAndPeriod`), `current_period_end`, and upsert the `subscriptions` row; for `checkout.session.completed`, also set `stripe_customer_id` on the account if not already set (the Checkout session carries `client_reference_id` = the account id, set when creating the session — use it to find the account); for `invoice.payment_failed`, send the `payment-failed` email (Plan 05); idempotent (safe to re-apply). Create `src/modules/billing/dedupe.ts`: `isEventProcessed(eventId): Promise<boolean>` + `markEventProcessed(eventId): Promise<void>` — backed by the `processed_stripe_events` table (or Upstash Redis with a TTL). Create `src/modules/billing/checkout.ts`: `createCheckoutSession({ accountId, tier, period }): Promise<{ url: string }>` — `stripe.checkout.sessions.create({ mode: 'subscription', customer or customer_email, client_reference_id: accountId, line_items: [{ price: TIERS[tier].stripePriceIds[period], quantity: 1 }], subscription_data: { trial_period_days: 7 }, payment_method_collection: 'always', success_url: `${APP_URL}/onboarding?checkout=success` (the stepper continues — Plan 9 handles the next step), cancel_url: `${APP_URL}/onboarding?checkout=cancelled`, automatic_tax: { enabled: true }, ... })` — returns the session url; fires `track('checkout_started')`. Create `src/modules/billing/portal.ts`: `createPortalSession({ accountId }): Promise<{ url: string }>` — `stripe.billingPortal.sessions.create({ customer: account.stripe_customer_id, return_url: `${APP_URL}/app/billing` })`. Create `src/server/routers/billing.ts`: `billingRouter` with `tiers` (a `publicProcedure.query` returning the `TIERS` data — the /pricing page (Plan 07's marketing plan... no, Plan 08) and the tier picker consume it), `createCheckout` (a `protectedProcedure.mutation({ input: z.object({ tier: z.enum(['pre_raise','active_raise']), period: z.enum(['monthly','annual']) }) })` → `createCheckoutSession`; fires `track('tier_selected', {tier, period})` then returns the url for a client redirect), `openPortal` (a `protectedProcedure.mutation` → `createPortalSession`; fires `track('manage_billing_clicked')`), `currentSubscription` (a `protectedProcedure.query` returning `{ tier, status, currentPeriodEnd, ...entitlements }` for the dashboard + the billing screen). Add `billing: billingRouter` to `appRouter`. Create `src/app/api/webhooks/stripe/route.ts` per the RESEARCH.md example: `POST` — read the raw body + the `stripe-signature` header, `stripe.webhooks.constructEvent(body, sig, STRIPE_WEBHOOK_SECRET)` (400 on failure), `if (await isEventProcessed(event.id)) return 200`, `switch (event.type) { case 'checkout.session.completed': case 'customer.subscription.created': case 'customer.subscription.updated': case 'customer.subscription.deleted': case 'invoice.payment_failed': case 'invoice.paid': await applySubscriptionState(event, getServiceClient()); if (event.type === 'checkout.session.completed') track('checkout_completed', { tier, period }) /* server-side via the Amplitude node SDK */; await inngest.send({ name: 'billing/subscription.changed', data: { eventId: event.id } }); break; }`, `await markEventProcessed(event.id)`, `return 200` fast. Fill in `src/inngest/functions/reconcile-stripe.ts` (Plan 04's stub): for each account with a non-null `stripe_customer_id`, `stripe.subscriptions.list({ customer })`, take the active/trialing one, and reconcile `accounts.subscription_status`/`tier`/`current_period_end` to match (logging any drift it corrected) — webhooks are the optimization, this poller is the safety net (PITFALLS §18); remove the `TODO(Plan 07)`. Build the **tier picker** screen as part of the `/onboarding` flow (between the welcome screen from task 1 and Checkout): a card grid of the two active tiers (Pre-Raise / Active Raise, monthly/annual `Tabs` toggle, Active Raise as the featured "Most chosen" card per the UI-SPEC) + the two close-stack tiers shown with the "Available with the close stack" badge and NO purchase CTA; selecting a tier calls `billing.createCheckout` and redirects to the returned Stripe URL. Write `tests/billing/tiers.test.ts`: assert `TIERS` has `pre_raise`/`active_raise` `active:true` with price ids from env and `close_mode`/`alumni` `active:false`; assert `priceIdToTierAndPeriod` round-trips. Write `tests/billing/entitlements.test.ts`: a table test over each `subscription_status` value (`trialing`→active+features, `active`→active+features, `past_due`/`canceled`/`none`→inactive+no-features) for each tier, plus a hypothetical `close_mode` tier flipped to `active:true` to prove extensibility is a data change; assert `entitlements()` never imports or calls Stripe (a structural check — grep its module for `stripe`). Write `tests/billing/checkout-session.test.ts`: mock the Stripe SDK; assert `createCheckoutSession` passes `subscription_data.trial_period_days: 7`, `payment_method_collection: 'always'`, `client_reference_id: accountId`, `automatic_tax.enabled: true`, the right `price` id for the tier+period, and `success_url`/`cancel_url` derived from `APP_URL` (no hardcoded URL). Write `tests/billing/webhook-idempotency.test.ts`: mock Stripe + the db + the dedupe store; send a `checkout.session.completed` event → assert `applySubscriptionState` is called once + the account's `subscription_status` becomes `'trialing'`; replay the SAME event id → assert `applySubscriptionState` is NOT called again (the handler returns 200 from the dedupe check) — idempotent; send a `customer.subscription.deleted` → assert the account's status becomes `'canceled'`; assert a bad signature returns 400.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npm run check:banned && npx vitest run tests/billing/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; the Stripe products/prices/webhook/Customer-Portal config exist in the dashboard (verifiable by the founder; the price ids are in env)
    - `tests/billing/tiers.test.ts` passes: `pre_raise`/`active_raise` `active:true` with env price ids; `close_mode`/`alumni` `active:false`; `priceIdToTierAndPeriod` round-trips
    - `tests/billing/entitlements.test.ts` passes: the status→features table holds for each tier; flipping a hypothetical `close_mode` to active is a pure data change (no code change needed); `entitlements()`'s module does not import `stripe`
    - `tests/billing/checkout-session.test.ts` passes: the Checkout session carries `trial_period_days: 7`, `payment_method_collection: 'always'`, `client_reference_id`, `automatic_tax.enabled: true`, the correct price id, and `APP_URL`-derived `success_url`/`cancel_url`
    - `tests/billing/webhook-idempotency.test.ts` passes: a first `checkout.session.completed` updates `subscription_status` to `'trialing'`; a replayed event id is a no-op; `customer.subscription.deleted` → `'canceled'`; a bad signature → 400
    - the `assertEntitled` stub in `src/server/trpc.ts` is replaced with the real `entitlements()`-backed check (no `TODO(Plan 07)` remains)
    - `reconcile-stripe.ts` re-pulls from Stripe and reconciles (no `TODO(Plan 07)` remains)
    - `billingRouter` (tiers, createCheckout, openPortal, currentSubscription) is in `appRouter`
    - the `processed_stripe_events` table (if used) is added via a migration, pushed, and in the schema-scan allowlist
    - no hardcoded URLs (`success_url`/`cancel_url`/`return_url`/the webhook endpoint all derive from env)
    - `npm run check:banned` passes (the tier picker / pricing copy uses no banned strings)
  </acceptance_criteria>
  <done>Stripe billing is live: the two active tiers + the two inactive close-stack tiers as data, `entitlements()` (pure, Stripe-free, extensible — replaces Plan 03's stub), the Checkout flow (7-day trial, card-on-file, automatic tax, client_reference_id), the Customer Portal flow, the idempotent webhook (dedupe on event id, writes accounts.subscription_status/tier/period, emits an Inngest event, fires checkout_completed server-side), the reconcile-stripe cron now real, and the tier picker screen — FND-05, FND-06 satisfied; billing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Walking-Skeleton e2e + deploy to Vercel + SKELETON.md</name>
  <files>e2e/skeleton.spec.ts, .planning/phases/01-foundation/SKELETON.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 / the Walking-Skeleton spine, stated verbatim: signup → tier picker → Stripe Checkout → webhook updates Supabase accounts.subscription_status → user lands on /app showing their tier; "If this slice works, the foundation works"; SKELETON.md is built around this; the first deliverable proves it deployed on Vercel)
    - $HOME/.claude/get-shit-done/references/skeleton-template.md (the SKELETON.md format)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Validation Architecture" — the FND-01 smoke row "loads /, /pricing, /sign-up, /app redirect"; the FND-05 rows; §"Runtime State Inventory" — "the code is written ≠ the webhook is registered in the Stripe dashboard ≠ the Vercel env vars are set on Preview too" — provisioning is part of the exit gate)
    - .planning/PROJECT.md ("Known build-environment notes" — repo at C:\Users\ejehe\trochia\Trochia, Vercel project `trochia` org `ejeherimartins-9592s-projects`, `trochia.asranest.com` live via Namecheap CNAME → Vercel, NEXT_PUBLIC_SITE_URL/APP_URL set on Production/Preview/Development)
  </read_first>
  <action>
    Write `e2e/skeleton.spec.ts` (Playwright — runs against the Vercel preview URL in CI via `PLAYWRIGHT_BASE_URL`): the Walking-Skeleton path — using Stripe test mode + a test Supabase user (or the Supabase admin API to mint a session, and Stripe's test card `4242 4242 4242 4242` via the Checkout test flow OR by directly POSTing a signed test `checkout.session.completed` to `/api/webhooks/stripe` and then asserting `/app` shows the tier): (1) visit `/sign-up` → click "Continue with Google" (mock/stub the OAuth round-trip, or skip to a pre-authed session) → land on `/onboarding` welcome → click "Get started" → see the tier picker → select "Active Raise" monthly → (the test either drives the Stripe Checkout test page to completion with the test card, OR — more reliable in CI — stubs the redirect and fires a signed test webhook) → the webhook sets `subscription_status='trialing'`, `tier='active_raise'` → the founder lands on `/app` → assert `/app` shows "Active Raise" (the tier) and the subscription status. Also assert the gate works: a fresh user with `subscription_status='none'` visiting `/app` → `/reactivate`. Mark this spec as the canonical "skeleton works" check. **Deploy:** confirm the Vercel project picks up the push (the repo is already linked); ensure ALL the env vars from Plans 01–07 are set on Vercel's Production AND Preview environments (Supabase URL/keys/DB URLs, Stripe secret/webhook-secret/price ids, Anthropic/OpenAI/Langfuse/Sentry/Amplitude/Resend/Inngest keys, NEXT_PUBLIC_SITE_URL/APP_URL); confirm the Vercel↔Inngest integration is connected (it auto-wires the `/api/inngest` endpoint + the Inngest env vars); confirm the Stripe webhook endpoint in the dashboard points at `https://trochia.asranest.com/api/webhooks/stripe`; trigger a deploy and confirm it succeeds and `https://trochia.asranest.com` loads (the `/` page is built in Plan 08 — for now `/` may 404 or show a placeholder; what must work is `/sign-up`, the auth flow, the webhook, and `/app`). **Document the deploy in `SKELETON.md`** using the template: **Capability Proven End-to-End** = "A founder signs in with Google, picks a tier, completes Stripe Checkout (7-day trial, card-on-file), and lands on /app seeing their tier — exercising Supabase Auth + RLS + Stripe + the idempotent webhook + tRPC + entitlements(), deployed on Vercel at trochia.asranest.com." **Architectural Decisions** table: Framework = Next.js 16.x App Router (pinned), Data layer = Supabase Postgres + Drizzle 0.44 + RLS default-deny, Auth = Supabase Auth Google SSO + @supabase/ssr PKCE cookies, Billing = Stripe (webhook → accounts.subscription_status; entitlements() reads persisted state), Background jobs = Inngest single serve() endpoint, AI = ai/client.ts chokepoint, Deployment target = Vercel Pro / Fluid Compute / trochia.asranest.com (→ trochia.ai via env swap), Directory layout = single Next package, App Router route groups (marketing)/(app)/(auth), src/{server,db,ai,modules,inngest,lib,components}. **Stack Touched in Phase 1** checklist: all boxes checked (scaffold ✓, routing ✓, DB read+write ✓, interactive UI wired to API ✓ — the tier picker → createCheckout → webhook → /app, deployment ✓). **Out of Scope (Deferred)**: magic-link/MFA (Phase 8), the India/EU regions' real Supabase projects, the future-phase domain tables (businesses/decks/investors/pipeline), Close Mode/Alumni billing activation (Phase 11), the full onboarding stepper steps (Plan 09 within this phase), the marketing site polish (Plan 08). **Subsequent Slice Plan**: Phase 2 = Business Memory + Memory spine + grounded Q&A; Phase 3 = Deck Reviewer + eval harness; Phase 4 = Investor Pipeline; Phase 5 = Live Raise (MVP soft-launch checkpoint); ... (list through Phase 11 from ROADMAP.md). Note in `SKELETON.md` that later phases add vertical slices ON TOP of these decisions without renegotiating them.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/skeleton.spec.ts e2e/auth.spec.ts && test -f .planning/phases/01-foundation/SKELETON.md && grep -qi "Capability Proven" .planning/phases/01-foundation/SKELETON.md</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/skeleton.spec.ts` passes (locally against the dev server with stubs, and in CI against the Vercel preview): the signup → tier picker → Checkout(/webhook) → /app-shows-tier path works; a `subscription_status='none'` user → `/reactivate`
    - the Vercel deploy succeeds; `https://trochia.asranest.com/sign-up` loads; the Stripe webhook endpoint in the dashboard points at `https://trochia.asranest.com/api/webhooks/stripe`; all Plans 01–07 env vars are set on Vercel Production + Preview; the Vercel↔Inngest integration is connected
    - `.planning/phases/01-foundation/SKELETON.md` exists, follows the template, and records the Capability Proven End-to-End, the Architectural Decisions table, the Stack-Touched checklist (all boxes checked), the Out-of-Scope list, and the Subsequent Slice Plan
    - the founder has manually confirmed (recorded in VALIDATION.md's Manual-Only table): a real Stripe test-mode Checkout completes and the webhook flips the account to `trialing`; the Customer Portal opens; the deploy is live
  </acceptance_criteria>
  <done>The Walking Skeleton works end-to-end deployed on Vercel — signup → tier picker → Stripe Checkout (7-day trial, card-on-file) → idempotent webhook → /app shows the tier — and `SKELETON.md` records the architectural decisions the rest of the project builds on. "The foundation works."</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder's browser → Supabase Auth (Google OIDC + PKCE) | the identity boundary; the session cookie is HttpOnly+Secure via @supabase/ssr |
| Stripe → /api/webhooks/stripe | an unauthenticated public endpoint receiving billing-state events — must verify the signature and be idempotent |
| founder's browser → tRPC billing procedures | createCheckout/openPortal must act only on the session's own account |
| webhook → service-role DB client | the webhook has no user session; it looks up the account from the event payload (client_reference_id / customer id) and writes via getServiceClient() |
| proxy.ts gate → /app | the subscription gate decides who reaches the product — a bypass = free access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-38 | Spoofing / Tampering | a forged or replayed Stripe webhook flips a founder's tier (free access, or locks out a paying founder) | mitigate | `stripe.webhooks.constructEvent` signature verification (400 on bad sig); dedupe on `event.id` (the `processed_stripe_events` table / Upstash Redis) → replays are no-ops; the `reconcile-stripe` cron re-pulls from Stripe as the safety net; `tests/billing/webhook-idempotency.test.ts` proves it |
| T-1-39 | Elevation of Privilege | a user reaches `/app` without an active trial-or-subscription (free access) | mitigate | `proxy.ts` checks `entitlements(account).active` (i.e. `subscription_status` ∈ {`trialing`,`active`}) on every `(app)` request → redirects to `/reactivate`; `assertEntitled(feature)` is the tRPC-layer backstop; `e2e/skeleton.spec.ts` asserts a `none` user → `/reactivate` |
| T-1-40 | Spoofing | a stolen/forged Supabase session cookie impersonates a founder | mitigate | the cookie is HttpOnly+Secure (set by @supabase/ssr); the JWT is signed by Supabase and verified server-side; 1h JWT expiry + 30d inactivity timeout limit the window (D-10); sign-out clears the session |
| T-1-41 | Elevation of Privilege | a client calls `billing.createCheckout`/`openPortal` for someone else's account | mitigate | both are `protectedProcedure` — they act on `ctx.tenantId` (the session's own account) only; the Checkout session's `client_reference_id` is the server-set account id, not client-supplied |
| T-1-42 | Information Disclosure | Stripe card data flows through Trochia | mitigate | Trochia never sees the PAN — `payment_method_collection: 'always'` captures a card on file via Stripe Checkout (Stripe-hosted); Trochia stores only the Stripe customer id; PCI scope stays with Stripe |
| T-1-43 | Tampering | `entitlements()` calls Stripe inline and reads stale/wrong state, or a network blip makes the gate fail open | mitigate | `entitlements()` is a pure function over the PERSISTED `accounts.subscription_status` — never calls Stripe inline (asserted by the test); the webhook keeps the persisted state fresh; the reconcile cron corrects drift |
| T-1-44 | Repudiation | it's unclear which DPA/ToS version a founder accepted at signup | mitigate | the sign-up→welcome flow records acceptance via `acceptDpa` (Plan 06) with `DPA_VERSION` to `legal_acceptances` + `accounts` |
| T-1-45 | Spoofing | the Google OAuth redirect URI / consent screen is misconfigured, enabling an open-redirect or token-leak | mitigate | the redirect URIs are an explicitly-configured allowlist in the Supabase + Google console (`${SITE_URL}/auth/callback` + the Supabase callback); `redirectTo` is built from `@/lib/env` `APP_URL`, never client-supplied; the PKCE flow protects the code exchange |
| T-1-46 | Denial of Service | the public webhook endpoint is flooded | accept | Stripe's events are signature-verified (junk is dropped at the signature check, cheap); Vercel/Cloudflare absorb volume; the handler returns 200 fast and offloads slow work to Inngest |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run typecheck && npm run check:banned` exit 0
- `npx vitest run tests/billing/` passes (entitlements / checkout-session / webhook-idempotency / tiers)
- `npx playwright test e2e/auth.spec.ts e2e/skeleton.spec.ts` passes
- `src/app/proxy.ts` exists (no `src/middleware.ts`); the `(app)` gate redirects to `/sign-in` (no session) and `/reactivate` (no active sub)
- the `assertEntitled` stub is replaced; `reconcile-stripe`'s body is real (no `TODO(Plan 07)`)
- the Vercel deploy is live at `trochia.asranest.com`; the Stripe webhook points at it; all env vars set on Production + Preview
- `SKELETON.md` exists and follows the template
</verification>

<success_criteria>
The Walking Skeleton works end-to-end deployed on Vercel: `signup → tier picker → Stripe Checkout → webhook updates accounts.subscription_status → the founder lands on /app showing their tier`. Concretely: Supabase Auth Google SSO with 30-day sessions (FND-04), Stripe billing — Pre-Raise $49/$39 + Active Raise $199/$159, Customer Portal, 7-day trial, card-on-file at signup, idempotent webhooks + the reconcile cron (FND-05), `entitlements()` gating by persisted tier and extensible to Close Mode/Alumni as a data change (FND-06, replacing Plan 03's stub), the hard `/app` gate → `/reactivate` (D-02b), the onboarding entry (sign-up/sign-in/welcome/tier-picker/Checkout with the funnel events firing — FND-12 partial; Plan 09 finishes the stepper), `/styleguide` now session-gated, and `SKELETON.md` recording the architectural decisions. Billing tests + the skeleton/auth e2e specs pass. (The marketing site is Plan 08; the rest of the onboarding stepper + the app shell pages are Plan 09 — both consume this plan's auth + billing + entitlements.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md` documenting: the resolved Supabase Auth session-timer config (which of the three D-10 timers the platform exposed + the values set), the auth callback → account-creation flow, where DPA/ToS acceptance is recorded, the `TIERS` data structure + the feature-gate map, the `entitlements()` signature, the webhook event types handled + the dedupe store choice (table vs Redis) + the `processed_stripe_events` schema-scan-allowlist update, the Checkout/Portal session params, the reconcile-stripe logic, the Vercel env-var checklist + the Stripe webhook endpoint URL, and the manual post-deploy verification checklist (real test-mode Checkout flips the account; Customer Portal opens; deploy live) for VALIDATION.md. Also note: the `assertEntitled` stub from Plan 03 is now the real check; `reconcile-stripe` from Plan 04 is now real.
</output>
