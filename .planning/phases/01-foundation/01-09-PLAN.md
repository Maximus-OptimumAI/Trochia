---
phase: 01-foundation
plan: 09
type: execute
wave: 6
depends_on: [2, 5, 6, 7]
files_modified:
  - src/app/(app)/onboarding/page.tsx
  - src/app/(app)/onboarding/welcome/page.tsx
  - src/app/(app)/onboarding/import/page.tsx
  - src/app/(app)/onboarding/deck/page.tsx
  - src/app/(app)/onboarding/review/page.tsx
  - src/components/onboarding/stepper.tsx
  - src/app/(app)/app/page.tsx
  - src/components/dashboard/cta-cards.tsx
  - src/components/dashboard/empty-dashboard.tsx
  - src/app/(app)/app/settings/page.tsx
  - src/app/(app)/app/billing/page.tsx
  - src/app/(app)/app/memory/page.tsx
  - src/app/(app)/app/pitch/page.tsx
  - src/app/(app)/app/pipeline/page.tsx
  - src/app/(app)/app/live-raise/page.tsx
  - src/server/routers/onboarding.ts
  - src/server/routers/index.ts
  - src/db/schema/tenancy.ts
  - src/db/migrations/*
  - e2e/onboarding.spec.ts
  - e2e/app-shell.spec.ts
autonomous: false
requirements: [FND-12, XC-02, XC-04]
must_haves:
  truths:
    - "The onboarding flow runs end-to-end: Google sign-in → welcome → Knowledge Pack Import (paste/dropzone shell) → deck upload (dropzone shell) → automatic deck review (skeleton) → dashboard with the three CTAs — target completion under 5 minutes"
    - "Every onboarding stage transition fires an Amplitude event (the Plan-05 taxonomy: welcome_viewed, knowledge_pack_step_viewed, deck_upload_step_viewed, review_step_viewed, dashboard_viewed) — the funnel is instrumented"
    - "The /app dashboard shows: when no Business Memory exists → the empty-dashboard state ('Welcome to Trochia' + 'Start Business Memory' CTA → /app/memory); always → the three FND-12 action cards ('Generate VC fit list' / 'Prepare for an upcoming call' / 'Draft outreach') each linking to its Phase-2/4/5 destination with a 'Coming Phase N' badge where not yet built — never disabled with no affordance"
    - "/app/settings has the destructive Delete-account Dialog (typed DELETE confirm, 'Keep my account' dismiss) wired to requestAccountDeletion + the 'Export my data' action wired to requestDataExport (XC-04); /app/billing shows the current tier + 'Manage billing' → Stripe Customer Portal + the destructive Cancel-subscription Dialog ('Keep subscription' dismiss) before the Portal redirect"
    - "The module placeholder pages (/app/memory, /app/pitch, /app/pipeline, /app/live-raise) render inside the app shell with a 'Coming in Phase N' empty-state; Data Room + Raise Ops nav items are disabled with a phase badge"
  artifacts:
    - path: "src/components/onboarding/stepper.tsx"
      provides: "the 3-step stepper UI (01 Import context · 02 Upload deck · 03 Review) wrapping the onboarding screens"
    - path: "src/components/dashboard/cta-cards.tsx"
      provides: "the three FND-12 action cards (Generate VC fit list / Prepare for an upcoming call / Draft outreach) with 'Coming Phase N' badges"
    - path: "src/app/(app)/app/settings/page.tsx"
      provides: "the Settings screen — profile basics, Delete-account Dialog (→ requestAccountDeletion), Export-my-data action (→ requestDataExport)"
    - path: "src/app/(app)/app/billing/page.tsx"
      provides: "the Billing screen — current tier/status, Manage-billing → Customer Portal, Cancel-subscription Dialog (→ openPortal or a cancel mutation), trial/card-on-file status"
    - path: "src/server/routers/onboarding.ts"
      provides: "onboardingRouter — markStepComplete, getProgress, skipStep; the onboarding_completed_at / onboarding_step columns on accounts"
  key_links:
    - from: "src/app/(app)/onboarding/*"
      to: "src/lib/analytics.ts (Plan 05)"
      via: "track(<stage>_viewed) on each step"
      pattern: "track\\(|knowledge_pack_step_viewed|review_step_viewed"
    - from: "src/app/(app)/app/settings/page.tsx"
      to: "src/server/routers/compliance.ts (Plan 06)"
      via: "requestAccountDeletion / requestDataExport mutations + the DestructiveConfirmDialog primitive (Plan 02)"
      pattern: "requestAccountDeletion|requestDataExport|DestructiveConfirmDialog"
    - from: "src/app/(app)/app/billing/page.tsx"
      to: "src/server/routers/billing.ts (Plan 07)"
      via: "currentSubscription query + openPortal mutation"
      pattern: "currentSubscription|openPortal"
    - from: "src/app/(app)/app/page.tsx"
      to: "src/components/dashboard/cta-cards.tsx + empty-dashboard.tsx"
      via: "renders the empty state + the three CTA cards"
      pattern: "EmptyDashboard|CtaCards"
---

<objective>
Complete FND-12 — the onboarding shell end-to-end (the parts not in Plan 07's skeleton): the 3-step stepper (`01 Import context · 02 Upload deck · 03 Review`) wrapping the Knowledge-Pack-Import screen (paste textarea OR file dropzone — extraction logic is Phase 2), the deck-upload screen (PDF/PPTX/Slides-URL dropzone — logic is Phase 3), the automatic-deck-review screen ("Reviewing your deck…" skeleton/progress — logic is Phase 3) → redirect to `/app`; the dashboard (`/app` — the empty-dashboard state when no Business Memory exists + the three FND-12 action cards "Generate VC fit list" / "Prepare for an upcoming call" / "Draft outreach" linking to their Phase-2/4/5 destinations with "Coming Phase N" badges); the Settings screen (`/app/settings` — profile basics, the destructive Delete-account Dialog wired to `requestAccountDeletion` (XC-04), the "Export my data" action wired to `requestDataExport` (XC-04)); the Billing screen (`/app/billing` — current tier/status, "Manage billing" → the Stripe Customer Portal, the destructive Cancel-subscription Dialog before the Portal redirect, trial/card-on-file status); the module placeholder pages (`/app/memory`, `/app/pitch`, `/app/pipeline`, `/app/live-raise` — "Coming in Phase N" empty-states inside the app shell; Data Room + Raise Ops disabled with a phase badge); and the onboarding-funnel instrumentation (every stage transition an Amplitude event from the Plan-05 taxonomy). Target onboarding completion < 5 minutes; the funnel is instrumented in Amplitude. (This plan `depends_on: [2, 5, 6, 7]` and runs in wave 6 — it consumes Plan 02's app shell + DestructiveConfirmDialog + SkeletonBlock + EmptyState, Plan 05's analytics taxonomy, Plan 06's data-rights tRPC procedures, Plan 07's auth + proxy.ts gate + billing/entitlements + the welcome screen + tier picker + Checkout + the `/onboarding` index it scaffolded + the `/app` skeleton page it created.)

Purpose: this is the founder's first 5 minutes — 25 design partners in a tight community amplify a bad first impression; the shell must be ruthless about step count and the skeleton states must look intentional (the UI-SPEC's `bg-stone/60 animate-pulse` blocks, not spinners).
Output: the onboarding stepper + screens (refining Plan 07's `/onboarding/welcome` + `/onboarding/page.tsx`, adding the `import`/`deck`/`review` steps), the dashboard + CTA cards + empty state (replacing Plan 07's `/app` skeleton), the Settings + Billing screens, the module placeholders, the `onboardingRouter`, the small `accounts.onboarding_step`/`onboarding_completed_at` schema addition + migration + push, and the onboarding + app-shell e2e specs. Consumes Plan 02 (app shell, DestructiveConfirmDialog, SkeletonBlock, EmptyState), Plan 05 (the analytics taxonomy), Plan 06 (the data-rights tRPC procedures), Plan 07 (auth, the proxy.ts gate, billing/entitlements, the welcome screen + tier picker + Checkout + the `/onboarding` index + the `/app` skeleton page it already built).

**`autonomous: false`** — it modifies the Drizzle schema (adds `accounts.onboarding_step`/`onboarding_completed_at`), so it runs `drizzle-kit push` after the schema change ([BLOCKING] task) — non-interactive for plain ADD COLUMN, but if it prompts, a human confirms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@docs/BRAND.md
@docs/DESIGN-REFERENCE.md
@tasks/constraints.md
@CLAUDE.md

<interfaces>
<!-- UI-SPEC §"Auth + onboarding shell": Post-sign-in welcome (built/scaffolded in Plan 07) → /onboarding stepper: 01 Import context · 02 Upload deck · 03 Review.
  Step 1 — Knowledge Pack Import: heading "Import your context" — body "Drop in your existing AI context — ChatGPT instructions, Claude project notes, a Notion brief — or paste 500–5,000 words. Trochia builds your Business Memory from it." (feature logic Phase 2; Phase 1 ships the screen + the paste textarea OR file dropzone — a Continue/Skip button to step 2). 
  Step 2 — Deck upload: heading "Upload your deck" — body "PDF, PPTX, or a Google Slides link. Trochia reviews it next." (logic Phase 3; Phase 1 ships the dropzone + a Continue/Skip).
  Step 3 — Auto-review: heading "Reviewing your deck…" — body "This takes about a minute. We'll take you to your dashboard when it's done." (skeleton/progress state — use SkeletonBlock, NOT a spinner; logic Phase 3; Phase 1 just shows the skeleton for a beat then → /app).
  → on done, redirect to /app dashboard. Target: under-5-minute completion; funnel instrumented in Amplitude (FND-12) — every stage transition an event.
UI-SPEC §"/app (dashboard)": app shell (sidebar + top bar from Plan 02). Top bar title "Dashboard". Content: when no Business Memory exists → the empty-dashboard state ("Welcome to Trochia" + body "Start by dropping your context into Business Memory. Everything else builds on it." + Primary CTA "Start Business Memory" → /app/memory). Always-visible: the three FND-12 CTAs rendered as three secondary-variant action cards — "Generate VC fit list" (→ /app/pipeline, "Coming Phase 4" badge) / "Prepare for an upcoming call" (→ /app/live-raise, "Coming Phase 5" badge) / "Draft outreach" (→ /app/pipeline, "Coming Phase 4" badge) — each linking to its destination with a text-mono-sm "Coming Phase N" badge where the destination isn't built yet. Never disabled with no affordance — always show where they go. The ambient Q&A sidebar slot (right side) is mounted but empty (Plan 2's AppShell already mounts the empty Sheet region — Phase 2 fills it).
UI-SPEC §"/app/settings": Account screen: profile basics, the destructive Delete-account Dialog (typed `DELETE` confirm; dismiss button "Keep my account"), Export-my-data action (XC-04 plumbing — calls Plan 06's requestDataExport). The DestructiveConfirmDialog primitive is from Plan 02.
UI-SPEC §"/app/billing": Billing screen: current tier, "Manage billing" → Stripe Customer Portal link (FND-05 — calls Plan 07's openPortal), the destructive Cancel-subscription Dialog (dismiss button "Keep subscription"; or pre-redirect confirm). 7-day trial / card-on-file status shown (from Plan 07's currentSubscription query).
UI-SPEC §"Module routes": Phase 1: thin placeholder pages inside the shell with an empty-state ("Coming in Phase N — here's what it'll do: …") so the sidebar nav resolves. Real UI lands in those phases' own UI-SPECs. Data Room and Raise Ops nav items are disabled with a "Phase N" badge (Plan 2's Sidebar already does this — Data Room "Phase 7", Raise Ops "Phase 9").
UI-SPEC §"Anti-Patterns": no spinners as the primary loading affordance for full-page loads (use SkeletonBlock); no bare generic CTA labels (the dismiss buttons are "Keep my account" / "Keep subscription", never bare "Cancel"); no banned strings; no hardcoded URLs.
Plan 07 already built/scaffolded: /onboarding/welcome (the welcome screen) + /onboarding/page.tsx (the index that routes to the right step) + the tier picker + Stripe Checkout; the Checkout success_url is `${APP_URL}/onboarding?checkout=success` — so this plan's /onboarding stepper picks up AFTER checkout returns. The proxy.ts gate already lets logged-in-but-no-active-sub users reach /onboarding (to pick a tier) but redirects them away from /app → /reactivate. /app/page.tsx exists as Plan 07's skeleton (renders the tier) — this plan replaces it with the full dashboard.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The onboarding stepper + the three step screens (Knowledge Pack Import / Deck upload / Auto-review skeleton) + the funnel instrumentation + the small accounts.onboarding_step schema addition [BLOCKING: drizzle-kit push]</name>
  <files>src/components/onboarding/stepper.tsx, src/app/(app)/onboarding/page.tsx, src/app/(app)/onboarding/welcome/page.tsx, src/app/(app)/onboarding/import/page.tsx, src/app/(app)/onboarding/deck/page.tsx, src/app/(app)/onboarding/review/page.tsx, src/server/routers/onboarding.ts, src/server/routers/index.ts, src/db/schema/tenancy.ts, src/db/migrations/*, e2e/onboarding.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 — the 4-stage instrumented funnel; Phase 1 ships the SHELL not the feature logic: screens, navigation, skeletons, instrumentation — yes; feature logic — no; the three dashboard CTAs link to their Phase-2/4/5 destinations with "Coming Phase N" badges; D-02 — the onboarding order: Google sign-in → welcome → tier picker → Checkout → Knowledge Pack Import → deck upload → auto deck review → dashboard)
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"Auth + onboarding shell" — the exact stepper labels + each step's heading/body copy; the under-5-minute target; the funnel instrumentation; §Copywriting Contract — the onboarding step copy; §Anti-Patterns — no spinners for full-page loads (use SkeletonBlock))
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Validation Architecture" — the FND-12 row: `e2e/onboarding.spec.ts` clicks through all 4 stages + asserts Amplitude `track` calls via MSW; §"Pitfall 4" — onboarding >5 min / card-on-file spooks people / the skeleton states get no design love — ruthless step count, no "about your company" form, skeleton states use the UI-SPEC's `bg-stone/60 animate-pulse` blocks)
    - src/lib/analytics.ts (Plan 05 — track + the funnel event names: welcome_viewed, knowledge_pack_step_viewed, deck_upload_step_viewed, review_step_viewed, dashboard_viewed), src/components/primitives/skeleton-block.tsx (Plan 02), src/components/ui/* (Plan 02 — Button, Card, Tabs, Input/textarea), src/components/shell/app-shell.tsx (Plan 02)
    - src/db/schema/tenancy.ts (Plan 03 — the accounts table to add `onboarding_step`/`onboarding_completed_at` to), src/server/trpc.ts (Plan 03 — protectedProcedure), src/app/(app)/onboarding/welcome/page.tsx + src/app/(app)/onboarding/page.tsx (Plan 07 — Plan 07 created the welcome screen + the `/onboarding` index; this plan refines them and adds the `import`/`deck`/`review` steps + makes the index key off `onboarding_step`)
    - .planning/phases/01-foundation/01-VALIDATION.md (the schema-push requirement for any Drizzle schema change)
  </read_first>
  <action>
    Add to `src/db/schema/tenancy.ts`: `accounts.onboarding_step text` (nullable — e.g. `'welcome'|'tier'|'import'|'deck'|'review'|null`-when-done) and `accounts.onboarding_completed_at timestamptz` (nullable). Run `npm run db:generate` then **[BLOCKING] `npm run db:push`** against the live Supabase project ([if drizzle-kit prompts on the ADD COLUMN — it shouldn't for plain adds — a human confirms]). Create `src/server/routers/onboarding.ts`: `onboardingRouter` with `getProgress` (a `protectedProcedure.query` returning `{ step: account.onboarding_step, completedAt: account.onboarding_completed_at }`), `markStepComplete` (a `protectedProcedure.mutation({ input: z.object({ step: z.enum(['welcome','tier','import','deck','review']) }) })` — advances `onboarding_step` to the next step; when `step === 'review'` it sets `onboarding_completed_at = now()` and `onboarding_step = null`), `skipStep` (advance without doing the step's work — the import + deck steps are skippable in Phase 1 since the logic isn't built). Add `onboarding: onboardingRouter` to `appRouter` in `src/server/routers/index.ts`. Create `src/components/onboarding/stepper.tsx`: a `'use client'` component rendering the 3-step header (`01 Import context · 02 Upload deck · 03 Review` — with the current step highlighted, completed steps checked) above a `{children}` slot; takes `currentStep: 'import' | 'deck' | 'review'`. Refine the onboarding tree under `src/app/(app)/onboarding/`: `welcome/page.tsx` — (Plan 07 created this) refine its final form: Mark + `H3` "Welcome to Trochia" + body "Let's set up your operator. Two quick steps." + Primary `Button` "Get started" → (if no tier selected yet) the tier picker (Plan 07's tier-picker screen — link to it) else → `/onboarding/import`; fires `track('welcome_viewed')`; ensure DPA/ToS acceptance is recorded here if not done earlier (Plan 07's choice — coordinate). `page.tsx` (the `/onboarding` index) — (Plan 07 scaffolded this) now make it read `onboarding.getProgress` and redirect to the right step (`/onboarding/welcome` if just signed in, `/onboarding/import` after checkout `?checkout=success`, `/onboarding/deck`, `/onboarding/review`, or `/app` if `onboarding_completed_at` is set); also handle `?checkout=cancelled` (return to the tier picker). `import/page.tsx` — wrapped in `<Stepper currentStep="import" />`: heading "Import your context", body "Drop in your existing AI context — ChatGPT instructions, Claude project notes, a Notion brief — or paste 500–5,000 words. Trochia builds your Business Memory from it." + a paste `textarea` AND/OR a file `dropzone` (just the UI — no extraction; a hint "We'll process this when you finish onboarding" — extraction is Phase 2), + Primary `Button` "Continue" (calls `onboarding.markStepComplete({step:'import'})` then → `/onboarding/deck`) + a `link` "Skip for now" (calls `onboarding.skipStep` then → `/onboarding/deck`); fires `track('knowledge_pack_step_viewed')`. `deck/page.tsx` — `<Stepper currentStep="deck" />`: heading "Upload your deck", body "PDF, PPTX, or a Google Slides link. Trochia reviews it next." + a file `dropzone` (PDF/PPTX) + a URL `Input` (Google Slides link) — just the UI, no parsing (Phase 3) + Primary "Continue" (→ `markStepComplete({step:'deck'})` → `/onboarding/review`) + "Skip for now" (→ `skipStep` → `/onboarding/review`); fires `track('deck_upload_step_viewed')`. `review/page.tsx` — `<Stepper currentStep="review" />`: heading "Reviewing your deck…", body "This takes about a minute. We'll take you to your dashboard when it's done." + a `SkeletonBlock`-based progress mock (rows of `bg-stone/60 animate-pulse` blocks matching a future review-results layout — NOT a spinner) — after a short beat (or on mount), call `onboarding.markStepComplete({step:'review'})` (which sets `onboarding_completed_at`) then redirect to `/app`; fires `track('review_step_viewed')`. Write `e2e/onboarding.spec.ts` (Playwright — with a pre-authed test session + an active `subscription_status='trialing'` so the proxy.ts gate lets `(app)` through, and MSW-mocking the Amplitude SDK): start at `/onboarding/welcome` → click "Get started" → (the tier picker → select Active Raise → the test stubs the Checkout redirect to `?checkout=success`) → `/onboarding/import` → assert the heading + the paste textarea + the dropzone render → click "Continue" → `/onboarding/deck` → assert the dropzone + the Slides URL input → click "Continue" → `/onboarding/review` → assert the skeleton blocks render (and NO spinner element) → it auto-advances → `/app` → assert the dashboard renders; assert (via the MSW-captured Amplitude calls) that `welcome_viewed`, `knowledge_pack_step_viewed`, `deck_upload_step_viewed`, `review_step_viewed`, `dashboard_viewed` all fired in order; assert the whole click-through completes quickly (a sanity check on step count — no "about your company" form appears anywhere).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npm run db:generate && npx playwright test e2e/onboarding.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run typecheck` exit 0; `npm run db:push` has been run (the `accounts` table on the live DB has `onboarding_step` + `onboarding_completed_at` — verifiable via the Supabase dashboard / `\d accounts`)
    - `e2e/onboarding.spec.ts` passes: the full welcome → (tier picker → checkout stub) → import → deck → review → /app flow works; the heading + UI of each step matches the UI-SPEC copy; the review step shows `SkeletonBlock`s and NO spinner; the Amplitude events `welcome_viewed`, `knowledge_pack_step_viewed`, `deck_upload_step_viewed`, `review_step_viewed`, `dashboard_viewed` fire in order; no "about your company" form appears
    - the import + deck steps are skippable ("Skip for now" advances without doing the step's work); the review step auto-advances and sets `onboarding_completed_at`
    - `onboardingRouter` (getProgress, markStepComplete, skipStep) is in `appRouter`
    - the `/onboarding` index redirects to the right step based on `onboarding.getProgress` / the `?checkout=` query param / `onboarding_completed_at`
    - no spinners as a full-page loading affordance; no bare generic CTA labels; no banned strings; no hardcoded URLs
  </acceptance_criteria>
  <done>The onboarding stepper + the three step screens (Knowledge Pack Import / Deck upload / Auto-review skeleton) are built per the UI-SPEC, skippable where the logic isn't built, instrumented with the Amplitude funnel events, and the small `accounts.onboarding_step`/`onboarding_completed_at` schema addition is migrated + pushed — FND-12's onboarding shell is end-to-end (the welcome→tier→Checkout half came from Plan 07; this plan completes the import→deck→review→dashboard half). The schema-push gate is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: The /app dashboard — the empty-dashboard state + the three FND-12 CTA cards (with "Coming Phase N" badges); the module placeholder pages</name>
  <files>src/app/(app)/app/page.tsx, src/components/dashboard/cta-cards.tsx, src/components/dashboard/empty-dashboard.tsx, src/app/(app)/app/memory/page.tsx, src/app/(app)/app/pitch/page.tsx, src/app/(app)/app/pipeline/page.tsx, src/app/(app)/app/live-raise/page.tsx, e2e/app-shell.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"/app (dashboard)" — the empty-dashboard state copy ("Welcome to Trochia" / "Start by dropping your context into Business Memory. Everything else builds on it." / Primary "Start Business Memory" → /app/memory) + the three FND-12 action cards ("Generate VC fit list" / "Prepare for an upcoming call" / "Draft outreach" — secondary-variant action cards, each linking to its Phase-2/4/5 destination with a text-mono-sm "Coming Phase N" badge, never disabled with no affordance); §"Module routes" — thin placeholder pages inside the shell with a "Coming in Phase N — here's what it'll do: …" empty-state; Data Room + Raise Ops nav disabled with a phase badge (the Sidebar from Plan 02 already does this); §Copywriting Contract — the dashboard copy + the generic empty-state pattern ("{Nothing} yet" + a sentence + the first-item action + one Primary CTA, mark icon 64px above, centered, py-32, max-w-md))
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 — the three dashboard CTAs link to their Phase-2/4/5 destinations with "Coming Phase N" badges; Phase 1 ships screens/navigation/skeletons/instrumentation, not feature logic; the ambient Q&A sidebar slot exists as an empty Sheet-mounted region (Plan 2's AppShell))
    - src/components/shell/app-shell.tsx (Plan 02 — wraps these via the (app) layout from Plan 07), src/components/primitives/empty-state.tsx (Plan 02), src/components/ui/{card,badge,button}.tsx (Plan 02), src/components/brand/logo.tsx (Plan 02 — the 64px mark for empty states)
    - src/server/routers/account.ts (Plan 03 — accountRouter.me — the tier; for "no Business Memory exists" — since businesses isn't a Phase-1 table (D-03), the dashboard always shows the empty-dashboard state in Phase 1; wire a hook that returns "no memory yet" — a TODO notes Phase 2 replaces it with a real businesses-table check), src/lib/analytics.ts (Plan 05 — track('dashboard_viewed')), src/server/routers/billing.ts (Plan 07 — currentSubscription for the subtle tier line)
    - src/app/(app)/layout.tsx (Plan 07 — the (app) layout wrapping these in the AppShell), src/app/(app)/app/page.tsx (Plan 07 — Plan 07's skeleton /app just rendered the tier; this plan replaces it with the full dashboard)
  </read_first>
  <action>
    Create `src/components/dashboard/cta-cards.tsx`: a component rendering the three FND-12 action cards as `secondary`-variant `Card`s (no shadow, 1px stone border): "Generate VC fit list" (→ `/app/pipeline`, with a `text-mono-sm` `Badge` "Coming Phase 4"), "Prepare for an upcoming call" (→ `/app/live-raise`, "Coming Phase 5" badge), "Draft outreach" (→ `/app/pipeline`, "Coming Phase 4" badge) — each card is a link to its destination (which is a "Coming in Phase N" placeholder page — never a dead/disabled card with no affordance); each card has a short one-line description of what it'll do. Create `src/components/dashboard/empty-dashboard.tsx`: the empty-dashboard state per the UI-SPEC — a 64px Mark icon, `H3` "Welcome to Trochia", body `text-body` "Start by dropping your context into Business Memory. Everything else builds on it.", a Primary `Button` "Start Business Memory" → `/app/memory`; centered, `max-w-md`. Replace `src/app/(app)/app/page.tsx` (Plan 07's skeleton version): inside the `AppShell` (via the `(app)` layout), top bar title "Dashboard"; render `<EmptyDashboard />` (in Phase 1 always — no `businesses` table yet; a `// TODO(Phase 2): when the businesses table exists, show the empty state only if the founder has no Business Memory`), then below it `<CtaCards />` (always visible); also surface the current tier somewhere subtle (e.g. a `text-body-sm text-graphite` line "Active Raise · trial ends {date}" reading `accountRouter.me` / `billing.currentSubscription` — keeps the Plan-07 skeleton's tier display); the ambient Q&A sidebar slot is the empty Sheet region already mounted by Plan 2's `AppShell`; fires `track('dashboard_viewed')`. Create the module placeholder pages under `src/app/(app)/app/`: `memory/page.tsx` (top bar title "Business Memory"; an `EmptyState`: heading "Business Memory — coming in Phase 2", body "Trochia will turn your existing AI context into a confirmed Business Memory that every module reads from. Paste your ChatGPT/Claude context, confirm each field, resolve conflicts.", mark icon 64px, centered — no Primary CTA, or a "Back to dashboard" link), `pitch/page.tsx` (title "Pitch Lab"; "coming in Phase 3" — "Trochia will review your deck against your Business Memory and a defect taxonomy, flagging factual contradictions, vague language, missing context — with zero fabricated slide references."), `pipeline/page.tsx` (title "Investor Pipeline"; "coming in Phase 4" — "Trochia will match VCs and accelerators from your Business Memory, track applications, draft outreach, and map warm intros — all founder-approved."), `live-raise/page.tsx` (title "Live Raise"; "coming in Phase 5" — "Trochia will generate pre-call briefs, ingest transcripts, draft 24-hour follow-ups, and keep a Pipeline Memory kanban."). (Data Room + Raise Ops have no page in Phase 1 — their nav items are disabled with a phase badge by Plan 2's `Sidebar`; if a route is needed for the disabled-link href to resolve, add a minimal `/app/data-room` and `/app/raise-ops` "coming in Phase 7/9" placeholder too — or just have the Sidebar render them as non-links.) Write `e2e/app-shell.spec.ts` (Playwright — pre-authed test session with `subscription_status='trialing'`): visit `/app` → assert the `AppShell` renders (the sidebar with the nav order Business Memory / Pitch Lab / Pipeline / Live Raise / Data Room / Raise Ops, Data Room with a "Phase 7" badge + disabled, Raise Ops with a "Phase 9" badge + disabled; the top bar title "Dashboard"); assert the empty-dashboard state ("Welcome to Trochia" + "Start Business Memory" → /app/memory) renders; assert the three CTA cards render with their "Coming Phase 4/5" badges and are links (not disabled); click "Generate VC fit list" → lands on `/app/pipeline` which shows the "coming in Phase 4" empty-state (NOT a 404, NOT a dead card); visit `/app/memory`, `/app/pitch`, `/app/live-raise` and assert each renders its "coming in Phase N" empty-state inside the shell; visit `/styleguide` while authed → assert it renders (Plan 07 made it session-gated); visit `/styleguide` while unauthed → assert it redirects to `/sign-in`.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npm run check:banned && npx playwright test e2e/app-shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run typecheck`, `npm run check:banned` exit 0
    - `e2e/app-shell.spec.ts` passes: `/app` renders the AppShell (sidebar nav in the right order, Data Room "Phase 7" disabled, Raise Ops "Phase 9" disabled, top bar "Dashboard") + the empty-dashboard state ("Welcome to Trochia" + "Start Business Memory" → /app/memory) + the three CTA cards with "Coming Phase 4/5" badges as working links; clicking "Generate VC fit list" lands on `/app/pipeline`'s "coming in Phase 4" empty-state (not a 404, not a dead card); `/app/memory`, `/app/pitch`, `/app/live-raise` each render a "coming in Phase N" empty-state inside the shell; `/styleguide` renders when authed and redirects to `/sign-in` when unauthed
    - the three CTA cards are never disabled with no affordance — each links to a real (placeholder) destination
    - `track('dashboard_viewed')` fires on `/app`
    - no banned strings; no hardcoded URLs; no bare generic CTA labels
  </acceptance_criteria>
  <done>The /app dashboard shows the empty-dashboard state + the three FND-12 CTA cards (linking to their Phase-2/4/5 destinations with "Coming Phase N" badges, never dead); the module placeholder pages render "coming in Phase N" empty-states inside the app shell; Data Room + Raise Ops are disabled-with-a-badge in the sidebar; /styleguide is session-gated. The app-shell e2e passes.</done>
</task>

<task type="auto">
  <name>Task 3: The Settings screen (Delete account + Export my data — XC-04) and the Billing screen (current tier + Manage billing → Customer Portal + Cancel subscription — FND-05)</name>
  <files>src/app/(app)/app/settings/page.tsx, src/app/(app)/app/billing/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (§"/app/settings" — profile basics, the destructive Delete-account Dialog (typed `DELETE` confirm; dismiss "Keep my account"), Export-my-data action (XC-04); §"/app/billing" — current tier, "Manage billing" → Stripe Customer Portal, the destructive Cancel-subscription Dialog (dismiss "Keep subscription"; or pre-redirect confirm), 7-day trial / card-on-file status; §"Destructive actions in Phase 1" table — the exact Dialog copy for Delete account + Cancel subscription; §Copywriting Contract — "specific verb+noun dismiss label, never bare Cancel")
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02b/D-02c — the in-app cancel trigger still shows the destructive Dialog before the Customer Portal redirect; XC-04 — data export on demand + account deletion → 30-day soft delete)
    - src/components/primitives/destructive-confirm-dialog.tsx (Plan 02 — the DestructiveConfirmDialog with the `requireTypedConfirmation` + the "Keep {noun}" dismiss), src/components/ui/* (Plan 02 — Button, Card)
    - src/server/routers/compliance.ts (Plan 06 — requestAccountDeletion, requestDataExport, restoreAccount), src/server/routers/billing.ts (Plan 07 — currentSubscription, openPortal), src/server/routers/account.ts (Plan 03 — accountRouter.me for profile basics), src/lib/analytics.ts (Plan 05 — track('manage_billing_clicked'))
    - tasks/banned-strings.txt (Plan 01 — the Settings/Billing copy passes the check)
  </read_first>
  <action>
    Create `src/app/(app)/app/settings/page.tsx`: inside the `AppShell`, top bar title "Settings"; a `Card` with profile basics (the founder's email + name from `accountRouter.me` / the Supabase user — read-only or minimally editable in Phase 1); a "Your data" `Card` with an "Export my data" `Button` (`secondary` variant) that calls `compliance.requestDataExport` (mutation) and, on success, shows a toast "Your data export is on its way — check your email." (the export → Storage → signed URL → email is Plan 06's `exportAccountData`); a "Danger zone" `Card` with a "Delete account" `Button` (`bg-danger` / a destructive-styled button) that opens the `<DestructiveConfirmDialog>` (Plan 02) configured per the UI-SPEC: title "Delete your account?", body "This soft-deletes your account now and permanently purges all your data after 30 days. You can export your data first.", `confirmVerbNoun="Delete account"`, `dismissKeepNoun="Keep my account"`, `requireTypedConfirmation="DELETE"`, `onConfirm` → `compliance.requestAccountDeletion` mutation → on success, sign the user out and redirect to a "your account is scheduled for deletion" screen (or `/sign-in` with a toast); also surface an "Export my data first" action link inside the Dialog (calls `requestDataExport` before deletion). Create `src/app/(app)/app/billing/page.tsx`: inside the `AppShell`, top bar title "Billing"; a `Card` showing the current tier + status from `billing.currentSubscription` (e.g. "Active Raise — trial ends {date}" or "Active Raise — renews {date}" or "Pre-Raise"), the card-on-file status ("Card on file"), and the trial state; a "Manage billing" `Button` (`primary` or `secondary`) that calls `billing.openPortal` (mutation, fires `track('manage_billing_clicked')`) and redirects to the returned Stripe Customer Portal URL — for plan changes, updating the card, viewing invoices; a "Cancel subscription" `Button` (`bg-danger`) that opens the `<DestructiveConfirmDialog>` configured per the UI-SPEC: title "Cancel your subscription?", body "You'll keep access until the end of your billing period.", `confirmVerbNoun="Cancel subscription"`, `dismissKeepNoun="Keep subscription"` (no typed confirmation needed), `onConfirm` → redirect to the Stripe Customer Portal (the Portal handles the actual cancellation — per D-02c "the in-app trigger still shows the destructive-confirmation Dialog before redirect") OR a direct cancel mutation if you prefer (the UI-SPEC allows either; the Portal route is simpler and matches "self-serve Customer Portal"). Both screens: no bare generic CTA labels (the dismiss buttons are "Keep my account" / "Keep subscription"); no banned strings; no hardcoded URLs (the Portal/return URLs come from `@/lib/env` via Plan 07's `portal.ts`). Note: the e2e for these flows is light (Stripe Customer Portal and the real deletion are manual / Plan-06-and-07-tested) — but assert in `e2e/app-shell.spec.ts` (or a small addition) that `/app/settings` renders the "Delete account" button which opens a Dialog whose confirm button is disabled until "DELETE" is typed and whose dismiss button text is "Keep my account", and that `/app/billing` renders the current tier + a "Manage billing" button + a "Cancel subscription" button whose dismiss is "Keep subscription".
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npm run check:banned && npx playwright test e2e/app-shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run typecheck`, `npm run check:banned` exit 0
    - `/app/settings` renders profile basics + an "Export my data" button (→ `compliance.requestDataExport`) + a "Delete account" button opening the `DestructiveConfirmDialog` with `confirmVerbNoun="Delete account"`, `dismissKeepNoun="Keep my account"`, `requireTypedConfirmation="DELETE"` (the confirm button is disabled until "DELETE" is typed — asserted by the e2e), `onConfirm` → `compliance.requestAccountDeletion` then sign-out
    - `/app/billing` renders the current tier + status + card-on-file state (from `billing.currentSubscription`) + a "Manage billing" button (→ `billing.openPortal` → the Stripe Customer Portal) + a "Cancel subscription" button opening the `DestructiveConfirmDialog` with `dismissKeepNoun="Keep subscription"` → the Customer Portal
    - the dismiss buttons are "Keep my account" / "Keep subscription" (never bare "Cancel"); no banned strings; no hardcoded URLs
    - the e2e (in `e2e/app-shell.spec.ts`) asserts the Settings Delete-account Dialog's typed-confirm gate + the dismiss label, and the Billing screen's tier display + the two buttons + the Cancel dismiss label
  </acceptance_criteria>
  <done>The Settings screen wires the Delete-account Dialog (typed DELETE, "Keep my account" dismiss → requestAccountDeletion) and the Export-my-data action (→ requestDataExport) — XC-04; the Billing screen shows the current tier/status/card-on-file and wires "Manage billing" → the Stripe Customer Portal and the Cancel-subscription Dialog ("Keep subscription" dismiss) → the Portal — FND-05. Banned-string clean; the e2e covers the key affordances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder's browser → (app) routes | gated by proxy.ts (session + active sub from Plan 07) before reaching the dashboard/settings/billing |
| founder's browser → compliance/billing tRPC mutations | requestAccountDeletion / requestDataExport / openPortal must act only on the session's own account |
| onboarding step → accounts.onboarding_step | a forged step-skip shouldn't grant access to anything (the steps are UI shells; the gate is the subscription, not the onboarding step) |
| Settings/Billing copy → compliance | banned strings in this copy ship to every founder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-53 | Elevation of Privilege / Repudiation | a user triggers `requestAccountDeletion` on someone else's account | mitigate | `requestAccountDeletion` is a `protectedProcedure` (the session's own tenant only — Plan 06); the UI gates it behind the typed-`DELETE`-confirm `DestructiveConfirmDialog` (Plan 02); the soft-delete is reversible within 30 days via `restoreAccount` |
| T-1-54 | Information Disclosure | the "Export my data" action returns another tenant's data | mitigate | `requestDataExport` → `exportAccountData(ctx.tenantId)` (Plan 06) — tenant-scoped read; RLS is the backstop; the export JSON whitelists columns (no secrets) |
| T-1-55 | Elevation of Privilege | a forged `markStepComplete`/`skipStep` advances onboarding to bypass something | mitigate | the onboarding steps are UI shells with no security significance — the access gate is the subscription status (proxy.ts, Plan 07), not the onboarding step; `markStepComplete` is a `protectedProcedure` acting on `ctx.tenantId`; the worst a forged call does is mark the founder's own onboarding "done" |
| T-1-56 | Spoofing / Tampering | the "Manage billing" / "Cancel subscription" flow redirects to a forged Customer Portal URL | mitigate | the Portal URL comes from `stripe.billingPortal.sessions.create` (Plan 07's `portal.ts`) with a `return_url` built from `@/lib/env` `APP_URL` — server-generated, not client-supplied |
| T-1-57 | Repudiation | banned compliance string in the Settings/Billing/dashboard/onboarding copy | mitigate | `npm run check:banned` runs in CI and is a verify step on every task here; the copy uses the operator voice and no banned terms |
| T-1-58 | Denial of Service (UX) | the onboarding shell is slow / has a "tell us about your company" form → founders bounce | mitigate (UX) | ruthless step count (welcome → tier → checkout → import → deck → review → dashboard, no "about your company" form — the import step does that in Phase 2); the review step uses `SkeletonBlock`s not a spinner; the funnel is instrumented so Phase 2 can see drop-off; the e2e asserts no "about your company" form appears |
| T-1-59 | Tampering (config) | a hardcoded site URL in an onboarding redirect / a Settings link breaks the trochia.ai migration | mitigate | all redirects/links use `@/lib/env` `APP_URL` or relative paths; the ESLint rule (Plan 01) flags literal `https://trochia` URLs |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run typecheck && npm run check:banned` exit 0
- `npm run db:push` applied (the `accounts` table has `onboarding_step` + `onboarding_completed_at`)
- `npx playwright test e2e/onboarding.spec.ts e2e/app-shell.spec.ts` passes
- the onboarding funnel events fire in order; no "about your company" form; the review step uses SkeletonBlock not a spinner
- the three dashboard CTA cards link to real placeholder destinations (never dead/disabled)
- the Settings Delete-account Dialog has the typed-DELETE gate + the "Keep my account" dismiss; the Billing Cancel-subscription Dialog has the "Keep subscription" dismiss; both go through the Stripe Customer Portal / Plan 06's mutations
- /styleguide session-gated (authed → renders; unauthed → /sign-in)
- no banned strings; no hardcoded URLs; no bare generic CTA labels
</verification>

<success_criteria>
FND-12's onboarding shell is end-to-end: the 3-step stepper (Knowledge Pack Import / Deck upload / Auto-review skeleton — UI shells, skippable, the review step using SkeletonBlock not a spinner) following Plan 07's welcome→tier→Checkout half, every stage transition firing an Amplitude event (the funnel is instrumented), targeting < 5 min completion. The `/app` dashboard shows the empty-dashboard state ("Welcome to Trochia" + "Start Business Memory" → /app/memory) + the three FND-12 CTA cards ("Generate VC fit list" / "Prepare for an upcoming call" / "Draft outreach" linking to their Phase-2/4/5 destinations with "Coming Phase N" badges, never dead). The module placeholders render "coming in Phase N" inside the app shell; Data Room + Raise Ops are disabled-with-a-badge. `/app/settings` wires the Delete-account Dialog (typed DELETE, "Keep my account" dismiss → requestAccountDeletion) + Export-my-data (→ requestDataExport) — XC-04. `/app/billing` shows the tier/status/card-on-file + "Manage billing" → the Stripe Customer Portal + the Cancel-subscription Dialog ("Keep subscription" dismiss) → the Portal — FND-05. `/styleguide` is session-gated. The small `accounts.onboarding_*` schema addition is migrated + pushed. The onboarding + app-shell e2e specs pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-09-SUMMARY.md` documenting: the onboarding route tree + the stepper + how it picks up after Plan 07's Checkout, the `accounts.onboarding_step`/`onboarding_completed_at` columns + the migration, the `onboardingRouter` procedures, the dashboard's empty-state + CTA-cards structure + the `// TODO(Phase 2)` for the real Business-Memory check, the module placeholder pages, the Settings + Billing screens' wiring to Plan 06/07's procedures, the `/styleguide` session-gate confirmation, and the onboarding + app-shell e2e coverage. Note: the schema-push gate for Phase 1 is satisfied by this plan's `db:push` (and Plan 03's, and Plan 07's `processed_stripe_events` if used).
</output>
</output>
