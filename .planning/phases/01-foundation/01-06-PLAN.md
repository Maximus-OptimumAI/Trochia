---
phase: 01-foundation
plan: 06
type: execute
wave: 3
depends_on: [1, 3]
files_modified:
  - docs/vendor-data-flow.md
  - public/legal/dpa.pdf
  - src/lib/compliance/dpa.ts
  - src/lib/compliance/dpa-content.tsx
  - src/server/routers/compliance.ts
  - src/server/routers/index.ts
  - src/modules/data-rights/export.ts
  - src/modules/data-rights/delete-account.ts
  - tests/compliance/dpa.test.ts
  - tests/compliance/data-rights.test.ts
autonomous: true
requirements: [XC-01, XC-04]
must_haves:
  truths:
    - "A GDPR / UK-GDPR / DPDP (India) / LGPD (Brazil)-grade DPA is accepted via clickwrap at signup (recorded with timestamp + version on legal_acceptances) and is downloadable as a PDF"
    - "A founder can export all their data on demand (a JSON dump of their tenant's rows)"
    - "Account deletion sets accounts.deleted_at (30-day soft delete); the purge cron (Plan 04) permanently removes rows >30 days deleted"
    - "A vendor data-flow inventory artifact exists (docs/vendor-data-flow.md) covering Anthropic (no-training, 7-day retention), the OpenAI/Codex fallback, Claude-Code build tooling, and every data-touching vendor (Supabase, Stripe, Resend, Sentry, Amplitude, Langfuse, Inngest, Vercel, Upstash)"
    - "XC-01 (no customer data used for model training) is stated and traceable to the DPA + ToS + the vendor inventory"
  artifacts:
    - path: "docs/vendor-data-flow.md"
      provides: "the vendor data-flow inventory — columns {vendor, what data touches it, trains on inputs?, retention, DPA/contract status, notes}"
    - path: "public/legal/dpa.pdf"
      provides: "the downloadable DPA PDF (generated via @react-pdf/renderer or a static pre-rendered doc)"
    - path: "src/lib/compliance/dpa.ts"
      provides: "DPA_VERSION constant + recordDpaAcceptance/recordTosAcceptance/recordPrivacyAcceptance writing legal_acceptances + setting accounts.dpa_accepted_at/dpa_version"
    - path: "src/lib/compliance/dpa-content.tsx"
      provides: "the DPA text/content as a single source — consumed by the PDF generator AND (later) the /legal/dpa page (Plan 08)"
    - path: "src/modules/data-rights/export.ts"
      provides: "exportAccountData(accountId) → a JSON dump of every tenant-scoped row, delivered via a signed download (and the data-export-ready email from Plan 05)"
    - path: "src/modules/data-rights/delete-account.ts"
      provides: "softDeleteAccount(accountId) — sets deleted_at; restoreAccount within 30 days; the purge cron (Plan 04) finishes the job"
    - path: "src/server/routers/compliance.ts"
      provides: "tRPC procedures: acceptDpa, dpaStatus, requestDataExport, requestAccountDeletion, restoreAccount"
  key_links:
    - from: "src/server/routers/compliance.ts"
      to: "src/modules/data-rights/*"
      via: "tRPC procedure → module function"
      pattern: "exportAccountData|softDeleteAccount|recordDpaAcceptance"
    - from: "src/modules/data-rights/delete-account.ts"
      to: "src/inngest/functions/purge-soft-deleted.ts (Plan 04)"
      via: "deleted_at column the cron scans"
      pattern: "deleted_at"
    - from: "docs/vendor-data-flow.md"
      to: "XC-01"
      via: "the no-training claim's evidence"
      pattern: "no.?training|training"
---

<objective>
Build the compliance / privacy plumbing (XC-01, XC-04): the clickwrap DPA (a GDPR / UK-GDPR / DPDP / LGPD-grade addendum accepted at signup with timestamp+version recorded on `legal_acceptances`, downloadable as a PDF, content shared from a single module so the PDF and the later `/legal/dpa` page never drift), the data-subject-rights plumbing (on-demand full data export — a JSON dump of the tenant's rows, delivered via a signed download + the `data-export-ready` email from Plan 05; account deletion → `accounts.deleted_at` soft delete, with the `purge-soft-deleted` cron from Plan 04 finishing the 30-day purge; restore-within-30-days), and the vendor data-flow inventory artifact (`docs/vendor-data-flow.md` — every data-touching vendor, whether it trains on inputs, its retention, its DPA/contract status — backing the XC-01 "no customer data in training" claim, with the OpenAI/Codex fallback + Claude-Code build tooling explicitly covered). The banned-string CI check (XC-05) and the redacting logger (XC-03) were already built in Plan 01 — this plan completes the remaining XC plumbing. (This plan `depends_on: [1, 3]` — Plan 1 for the banned-string `scanText` core + env.ts, Plan 3 for `legal_acceptances`/`accounts.dpa_*`/`accounts.deleted_at`/`getServiceClient`/`protectedProcedure` and the `src/server/routers/index.ts` it extends.)
Output: the DPA module + PDF, the data-rights module + tRPC procedures, and `docs/vendor-data-flow.md`. The DPA clickwrap LINE on the sign-up screen and the Settings/Billing screens that trigger export/delete are built in Plans 07 (sign-up) and 09 (settings) — they call this plan's tRPC procedures; the `/legal/dpa` PAGE is Plan 08 (it renders this plan's `dpa-content.tsx` + links to the PDF). The reusable founder-approval Dialog + legal-disclaimer banner (XC-02) were built in Plan 02. The `lib/crypto.ts` field-encryption STUB (XC-03 seam) was built in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@tasks/constraints.md
@CLAUDE.md
@C:\Users\ejehe\CLAUDE.md

<interfaces>
<!-- RESEARCH.md §"Compliance plumbing": vendor data-flow inventory artifact = a markdown doc in repo (docs/vendor-data-flow.md) with columns {vendor, what data touches it, trains on inputs?, retention, DPA signed?, notes}; Anthropic API = no-training, 7-day retention (confirmed in PITFALLS §5); the codex bridge has no DB credentials (architectural boundary). XC-04: accounts.deleted_at soft-delete column (exists from Plan 03) + the Inngest cron that purges rows >30 days deleted (Plan 04 — purge-soft-deleted); a data-export endpoint (JSON dump of the tenant's rows); clickwrap acceptance recorded (timestamp + DPA version on accounts or a legal_acceptances table — Plan 03 created legal_acceptances + accounts.dpa_accepted_at/dpa_version).
RESEARCH.md §"Don't Hand-Roll": DPA-as-downloadable-PDF uses @react-pdf/renderer (Trochia-authored doc, no legal-template fidelity needed in Phase 1) — or a static pre-rendered PDF in public/; either is fine (Assumption A9). The data-export-ready email template was built in Plan 05.
RESEARCH.md §"Open Question Q3" RESOLVED: the vendor data-flow inventory lives as a living markdown artifact (docs/vendor-data-flow.md), referenced from the DPA. The artifact existing is the Phase 1 deliverable.
D-11: a GDPR / UK-GDPR / DPDP (India) / LGPD (Brazil)-grade DPA accepted via clickwrap at signup (the /legal/dpa page is the clickwrap target per UI-SPEC), downloadable as PDF; data-subject-rights: on-demand full data export; account deletion → 30-day soft delete → permanent purge (XC-04). XC-01 stated in product UI + ToS + DPA, backed by the vendor inventory. XC-02 — the reusable founder-approval Dialog built Phase 1 per UI-SPEC (done in Plan 02); no autonomous sends.
The /legal/dpa, /legal/privacy, /legal/terms, /legal/security PAGES are built in Plan 08 (marketing site) — the DPA PDF this plan produces is what /legal/dpa links to for download; the page renders `src/lib/compliance/dpa-content.tsx` (this plan's shared source) and references the vendor inventory and the no-training claim.
Plan 03 deps: `src/db/schema/legal.ts` (legal_acceptances), `src/db/schema/tenancy.ts` (accounts.dpa_accepted_at/dpa_version, accounts.deleted_at), `src/db/client.ts` (getServiceClient), `src/server/trpc.ts` (protectedProcedure), `src/server/routers/index.ts` (this plan adds `compliance: complianceRouter`).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The vendor data-flow inventory (docs/vendor-data-flow.md) — every data-touching vendor, training posture, retention, contract status (XC-01)</name>
  <files>docs/vendor-data-flow.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 — XC-01 stated in product UI + ToS + DPA, backed by the vendor data-flow inventory: Anthropic no-training/7-day retention; OpenAI/Codex fallback + Claude-Code build tooling explicitly covered)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Compliance plumbing" — the artifact's columns; Anthropic = no-training, 7-day retention confirmed; the codex bridge has no DB credentials; §"Open Question Q3" RESOLVED — a living markdown artifact, referenced from the DPA; §"Environment Availability" — the full vendor list: Vercel, Supabase, Stripe, Anthropic, OpenAI, Inngest, Sentry, Amplitude, Langfuse, Resend, Upstash; §"Security Domain" — the customer-data-trains-a-model threat row)
    - C:\Users\ejehe\CLAUDE.md (global: "Customer data: NEVER enters a training pipeline. NEVER passes through unaudited 3rd-party tooling.")
    - tasks/constraints.md ("No customer data used for model training — product UI + ToS + DPA + contractual with vendors; vendor data-flow inventory current")
    - .planning/intel/Trochia_AI_Build_Stack_v2.md (the build tooling list — Claude Code itself is build tooling that touches the repo but not customer data; record it)
  </read_first>
  <action>
    Create `docs/vendor-data-flow.md`: a living markdown artifact with a header (purpose: the evidence base for XC-01 "no customer data used for model training"; update whenever a vendor is added/changed; referenced from `/legal/dpa`) and a table with columns: **Vendor** · **What data touches it** · **Trains on inputs?** · **Retention** · **DPA / contract status** · **Notes**. Rows (one per vendor — fill from current vendor docs; mark `[VERIFY]` on anything you can't confirm and note it):
    - **Anthropic API** — prompts/responses (Phase 2+ includes deck text, Business Memory, transcripts) — *No (API tier is no-training)* — 7-day retention then deleted — Commercial terms / DPA available — the `claude-api` skill mandates prompt caching; calls only via `ai/client.ts`.
    - **OpenAI API (fallback only)** — same as Anthropic, ONLY when the `AI_FALLBACK_ENABLED` flag is on (off by default) — *No (API tier is no-training / ZDR posture)* — `[VERIFY current retention]` — DPA available — config-flagged off; `ai/fallback.ts` has NO DB credentials; never the default production path.
    - **Claude Code / Cursor (build tooling)** — repo source code, NOT customer data — N/A (no customer data flows here) — N/A — N/A — `tasks/lessons.md` rule: never paste real customer data into build tooling; synthetic fixtures only.
    - **Supabase** — all stored customer data (accounts, businesses Phase 2+, decks Phase 3+, etc.) — *No* — until deleted (30-day soft-delete then purge for accounts) — DPA available; SOC 2; EU region added Phase 8 — RLS default-deny; encryption at rest; the system of record.
    - **Stripe** — billing data (customer id, subscription, card-on-file token — NOT the PAN, Trochia never sees it) — *No* — per Stripe's retention — DPA available; PCI DSS — `entitlements()` reads persisted status, never calls Stripe inline.
    - **Resend** — transactional email recipient + body (system mail to the founder only) — *No* — per Resend's retention `[VERIFY]` — DPA available — Trochia→founder system mail only; founder investor-emails go via the founder's own Gmail, never Resend.
    - **Sentry** — error contexts (PII/financials scrubbed by `beforeSend` reusing the redacting logger's `SENSITIVE_FIELDS` set) — *No* — per Sentry's retention — DPA available; Team plan — IDs and event types only.
    - **Amplitude** — product events (ids + enums only — never deck text/financials/transcript bodies) — *No* — per Amplitude's retention — DPA available — the onboarding funnel; revenue events server-side.
    - **Langfuse** — LLM traces (prompts/responses + token counts) — *No* — per Langfuse Cloud's retention / N/A if self-hosted — `[VERIFY]` / N/A — cache-hit metrics; in Phase 1 only the trivial health-check prompt.
    - **Inngest** — job event payloads (ids + the minimum needed; never financials/transcript bodies in `jobs.payload`) — *No* — per Inngest's retention — DPA available — the slow-work lane; signing-key-verified invocations.
    - **Vercel** — request logs, build artifacts, deployed code (no customer data in code) — *No* — per Vercel's retention — DPA available; Pro plan — hosting.
    - **Upstash Redis (if used)** — Stripe-webhook dedupe ids / rate-limit counters (no customer content) — *No* — short TTL — DPA available — dedupe/rate-limit only.
    Add a closing section: "**XC-01 traceability**: this inventory + the DPA (`/legal/dpa`, `public/legal/dpa.pdf`) + the ToS (`/legal/terms`) + the product copy on the relevant screens together state and back the 'no customer data used for model training' commitment. Any new vendor MUST be added here before it touches customer data."
  </action>
  <verify>
    <automated>node scripts/check-banned-strings.mjs && test -f docs/vendor-data-flow.md && grep -qi "training" docs/vendor-data-flow.md && grep -qi "Anthropic" docs/vendor-data-flow.md && grep -qi "OpenAI" docs/vendor-data-flow.md && grep -qi "Claude Code" docs/vendor-data-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/vendor-data-flow.md` exists with a table covering Anthropic, OpenAI (fallback), Claude Code build tooling, Supabase, Stripe, Resend, Sentry, Amplitude, Langfuse, Inngest, Vercel (+ Upstash if used)
    - each row records the "trains on inputs?" answer and the retention
    - the Anthropic row records "No (no-training)" + "7-day retention"; the OpenAI row records it's a config-flagged fallback with no DB credentials; the Claude Code row records "no customer data flows here" + the synthetic-fixtures-only rule
    - the doc has the "XC-01 traceability" closing section and the "add new vendors here before they touch customer data" rule
    - the doc passes `node scripts/check-banned-strings.mjs` (it's in `docs/` not `src/`, but run it anyway — and ensure no banned compliance strings)
  </acceptance_criteria>
  <done>The vendor data-flow inventory artifact exists and covers every data-touching vendor with its training posture, retention, and contract status — XC-01 has a traceable evidence base; the OpenAI fallback and Claude-Code build tooling are explicitly covered.</done>
</task>

<task type="auto">
  <name>Task 2: The clickwrap DPA — DPA_VERSION + recordDpaAcceptance, the shared DPA content module, the downloadable DPA PDF, the acceptDpa tRPC procedure</name>
  <files>src/lib/compliance/dpa.ts, src/lib/compliance/dpa-content.tsx, public/legal/dpa.pdf, src/server/routers/compliance.ts, src/server/routers/index.ts, tests/compliance/dpa.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 — a GDPR / UK-GDPR / DPDP / LGPD-grade DPA accepted via clickwrap at signup, the /legal/dpa page is the clickwrap target, downloadable as PDF; clickwrap acceptance recorded with timestamp + version)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Compliance plumbing" — clickwrap acceptance recorded on accounts or legal_acceptances (Plan 03 made both: accounts.dpa_accepted_at/dpa_version + legal_acceptances); §"Don't Hand-Roll" — @react-pdf/renderer for the DPA PDF, or a static pre-rendered PDF; Assumption A9)
    - .planning/phases/01-foundation/01-UI-SPEC.md (the sign-up clickwrap line: "By signing up you agree to our Data Processing Addendum." linking to /legal/dpa; this plan ships the backend — Plan 07 ships the sign-up screen line; Plan 08 ships the /legal/dpa page)
    - src/db/schema/legal.ts, src/db/schema/tenancy.ts (Plan 03 — legal_acceptances + accounts.dpa_accepted_at/dpa_version)
    - src/server/trpc.ts (Plan 03 — protectedProcedure), src/server/routers/index.ts (Plan 03 — this task adds `compliance`)
    - tasks/banned-strings.txt + scripts/check-banned-strings.mjs (Plan 01 — the DPA text passes the banned-string check via the exported `scanText` core; "data processing addendum" / "not legal advice" are fine; never bare "legal advice")
  </read_first>
  <action>
    Create `src/lib/compliance/dpa.ts`: `export const DPA_VERSION = '2026-05'` (bump on each material revision); `export async function recordDpaAcceptance(accountId: string, db): Promise<void>` — inserts a `legal_acceptances` row `{ account_id: accountId, document: 'dpa', version: DPA_VERSION }` AND updates the `accounts` row `dpa_accepted_at = now()`, `dpa_version = DPA_VERSION` (idempotent — if already accepted at the current version, no-op); also a `recordTosAcceptance` / `recordPrivacyAcceptance` companion for the "Terms and Privacy" clickwrap line. Create `src/lib/compliance/dpa-content.tsx` — the DPA text/content as a SINGLE SOURCE (a structured TSX component or an exported string-tree), Trochia-authored, operator-toned, GDPR / UK-GDPR / DPDP (India) / LGPD (Brazil)-grade — covers: parties, subject-matter & duration, nature & purpose of processing, categories of data subjects & personal data, sub-processors (referencing `docs/vendor-data-flow.md`), data-subject rights & assistance, security measures, breach notification, international transfers (and the multi-region seam — US/UK contractual, India region, EU at V2), no-training commitment (XC-01), deletion (30-day soft-delete then purge), audit rights — a strong real draft (a legal review happens later; the founder's "ship + manage risk in parallel" style applies). Both the PDF generator (`scripts/generate-dpa-pdf.mjs`) and the later `/legal/dpa` page (Plan 08) consume `dpa-content.tsx` — one source of truth. Generate `public/legal/dpa.pdf` from `dpa-content.tsx` via `@react-pdf/renderer` (a small build script `scripts/generate-dpa-pdf.mjs` that renders a `<Document>` to the PDF file, run as part of `npm run build` via a `prebuild` step, OR pre-render it once and commit the PDF — either is fine; if you commit it, also commit the source). Create `src/server/routers/compliance.ts`: `complianceRouter` with `acceptDpa` (a `protectedProcedure.mutation` calling `recordDpaAcceptance(ctx.tenantId, ctx.db)` — used by the sign-up clickwrap flow in Plan 07 and re-acceptance prompts) and a query `dpaStatus` (returns `{ accepted: boolean, version: string | null, currentVersion: DPA_VERSION }`). Add `compliance: complianceRouter` to `appRouter` in `src/server/routers/index.ts`. Write `tests/compliance/dpa.test.ts`: assert `recordDpaAcceptance` inserts a `legal_acceptances` row and sets `accounts.dpa_accepted_at`/`dpa_version` (mock the db); assert it's idempotent at the same version; assert `DPA_VERSION` is a non-empty string; assert the DPA content (rendered from `dpa-content.tsx`) contains the no-training commitment and references the sub-processor inventory; assert the rendered DPA HTML passes the banned-string check (reuse the `scanText` core from `scripts/check-banned-strings.mjs` on the rendered output — it must contain no bare "legal advice" / "investment advice" / "rolling fund" etc.); assert `public/legal/dpa.pdf` exists after the build step.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run check:banned && test -f public/legal/dpa.pdf && npx vitest run tests/compliance/dpa.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `public/legal/dpa.pdf` exists (generated by the build step or committed)
    - `src/lib/compliance/dpa.ts` exports `DPA_VERSION` (non-empty) and `recordDpaAcceptance(accountId, db)` which inserts a `legal_acceptances` `document:'dpa'` row + sets `accounts.dpa_accepted_at`/`dpa_version`, idempotent at the same version
    - `src/lib/compliance/dpa-content.tsx` is the single source of the DPA text — consumed by the PDF generator AND (later) the `/legal/dpa` page (Plan 08)
    - the DPA content contains the no-training commitment (XC-01) and references the sub-processor inventory (`docs/vendor-data-flow.md`)
    - `complianceRouter.acceptDpa` is a `protectedProcedure.mutation`; `complianceRouter.dpaStatus` returns `{ accepted, version, currentVersion }`; `appRouter` includes `compliance`
    - `npm run check:banned` passes (the DPA text uses no banned compliance strings)
    - `tests/compliance/dpa.test.ts` passes
  </acceptance_criteria>
  <done>The clickwrap DPA is wired: a versioned acceptance recorded on `legal_acceptances` + `accounts`, a downloadable `public/legal/dpa.pdf`, the DPA content in a single `dpa-content.tsx` module shared between the PDF and the (Plan-08) `/legal/dpa` page, and the `acceptDpa`/`dpaStatus` tRPC procedures — Plan 07's sign-up clickwrap line calls `acceptDpa`. Banned-string clean; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Data-subject-rights — data export (JSON dump + signed download + email), account soft-delete + restore; the tRPC procedures</name>
  <files>src/modules/data-rights/export.ts, src/modules/data-rights/delete-account.ts, src/server/routers/compliance.ts, tests/compliance/data-rights.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 — data-subject-rights plumbing: on-demand full data export; account deletion → 30-day soft delete → permanent purge — XC-04)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Compliance plumbing" — a data-export endpoint (JSON dump of the tenant's rows); accounts.deleted_at soft-delete column + the Inngest cron that purges rows >30 days deleted (Plan 04's purge-soft-deleted); §"Validation Architecture" — the XC-04 row: `tests/compliance/data-rights.test.ts` covers "Data export returns the tenant's rows; account deletion sets deleted_at; purge cron removes rows >30d deleted")
    - .planning/phases/01-foundation/01-UI-SPEC.md (the destructive-actions table: "Delete account" Dialog with typed `DELETE` confirm + "Export my data first" action link + "Keep my account" dismiss — lives in /app/settings; the Dialog itself is the Plan-02 primitive; the screen is Plan 09; this plan ships the backend)
    - src/db/schema/* (Plan 03 — the tenant-scoped tables to dump; accounts.deleted_at), src/db/client.ts (getServiceClient for the soft-delete write), src/server/trpc.ts (protectedProcedure)
    - src/lib/email/templates/data-export-ready.tsx, src/lib/email/client.ts (Plan 05 — the export-ready email)
    - src/inngest/functions/purge-soft-deleted.ts (Plan 04 — the cron that scans deleted_at and finishes the purge)
  </read_first>
  <action>
    Create `src/modules/data-rights/export.ts`: `export async function exportAccountData(accountId: string): Promise<{ downloadUrl: string; expiresAt: Date }>` — using a tenant-scoped read (or `getServiceClient()` scoped to the one account — document the choice; the tenant-scoped client is cleaner and RLS-safe), SELECT every row across every tenant-scoped table for that account (`accounts` row, `subscriptions`, `legal_acceptances`, `jobs` where `account_id = accountId`, `sessions` if Trochia owns them, and a `_note` that future-phase tables — businesses/decks/investors/pipeline — will be added here as those phases ship), assemble a JSON object `{ exportedAt, dpaVersion, account: {...}, subscriptions: [...], legalAcceptances: [...], jobs: [...], ... }` (omitting internal-only columns; never include the Stripe secret or any `*_secret`/`*_key` column), upload it to a tenant-isolated Supabase Storage bucket (`exports/{accountId}/{timestamp}.json`, private, with a signed URL that expires in e.g. 48h), and send the `data-export-ready` email (Plan 05's template) with the signed URL + expiry. A header comment: this is the on-demand full export (XC-04); a deletion-time auto-export option is offered in the UI ("Export my data first"). Create `src/modules/data-rights/delete-account.ts`: `export async function softDeleteAccount(accountId: string): Promise<void>` — sets `accounts.deleted_at = now()` (via `getServiceClient()` — one of the audited service-role call sites; the user's own session can't write `deleted_at` on a row that then locks them out, so this goes through the service client triggered by an authenticated tRPC mutation), revokes the user's Supabase sessions (so they're signed out), and emits an Inngest event (`account/soft-deleted`) for any async cleanup; the actual permanent purge happens in the `purge-soft-deleted` cron (Plan 04) after 30 days. Also `export async function restoreAccount(accountId: string): Promise<void>` — clears `deleted_at` if within the 30-day window, refuses after. A header comment: 30-day soft delete then permanent purge (XC-04); the founder can change their mind within 30 days. Add to `src/server/routers/compliance.ts`: `requestDataExport` (a `protectedProcedure.mutation` calling `exportAccountData(ctx.tenantId)` — returns the download URL + expiry; could also be async via Inngest if the dump is large — for Phase 1, synchronous is fine since there's almost no data), `requestAccountDeletion` (a `protectedProcedure.mutation` calling `softDeleteAccount(ctx.tenantId)` — the UI's typed-`DELETE`-confirm gate is in the Plan-02 destructive Dialog + the Plan-09 screen; the mutation just executes the soft-delete), `restoreAccount` (clears `deleted_at` if within 30 days). Write `tests/compliance/data-rights.test.ts`: mock the db + Storage + email; assert `exportAccountData` SELECTs every tenant-scoped table for the given account, assembles a JSON object containing those rows, uploads it with an expiring signed URL, and sends the `data-export-ready` email with that URL; assert `softDeleteAccount` sets `accounts.deleted_at` and revokes sessions; assert `restoreAccount` clears `deleted_at` within 30 days and refuses after; assert the export JSON never contains a key matching the `SENSITIVE_FIELDS` set or any `*_secret`/`*_key` column.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npx vitest run tests/compliance/data-rights.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, `npm run typecheck` exit 0
    - `tests/compliance/data-rights.test.ts` passes: `exportAccountData` dumps every tenant-scoped table for the account into a JSON object, uploads it with an expiring signed URL, and sends the `data-export-ready` email; the export JSON contains no `SENSITIVE_FIELDS` key and no `*_secret`/`*_key` column
    - `softDeleteAccount` sets `accounts.deleted_at` and revokes the user's Supabase sessions; `restoreAccount` clears `deleted_at` within 30 days and refuses after
    - `complianceRouter` includes `requestDataExport`, `requestAccountDeletion`, `restoreAccount` (all `protectedProcedure` mutations)
    - the soft-delete write goes through `getServiceClient()` (an audited service-role call site, documented in the header)
    - the `purge-soft-deleted` cron (Plan 04) scans `accounts.deleted_at` — confirmed by grep that `delete-account.ts` and `purge-soft-deleted.ts` agree on the `deleted_at` column name
  </acceptance_criteria>
  <done>Data-subject-rights plumbing is wired: on-demand full data export (JSON dump → tenant-isolated Storage → signed URL → email), account soft-delete (sets `deleted_at`, revokes sessions, the Plan-04 cron finishes the 30-day purge), restore-within-30-days, and the `requestDataExport`/`requestAccountDeletion`/`restoreAccount` tRPC procedures — XC-04 satisfied. Plan 09's Settings screen wires the UI to these.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder → data-export request | the export must include ONLY the requesting tenant's rows — a cross-tenant leak here is catastrophic |
| data export → Supabase Storage signed URL | the export JSON sits in storage with an expiring URL — anyone with the URL can read it until it expires |
| founder → account-deletion request | a deletion request must affect only the requesting tenant; the soft-delete must not be reversible by an attacker after the window |
| vendor inventory ↔ reality | the inventory is the XC-01 evidence base — a stale entry undermines the no-training claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-31 | Information Disclosure | the data export includes another tenant's rows (a forgotten `where account_id=...` or a service-client query without the filter) | mitigate | `exportAccountData` uses a tenant-scoped (RLS-enforced) read OR a service-client query explicitly filtered to the one `accountId`; the test asserts only the given account's rows appear; RLS is the backstop |
| T-1-32 | Information Disclosure | the export JSON leaks secrets / financial figures (a Stripe secret, a `*_key` column, a cap-table figure) | mitigate | `exportAccountData` whitelists which columns are dumped (no `*_secret`/`*_key`); the test asserts no `SENSITIVE_FIELDS` key appears; the export is the founder's own data, not internal config |
| T-1-33 | Information Disclosure | the export's signed download URL is long-lived / shared and leaks the founder's data | mitigate | the signed URL expires (e.g. 48h); the file lives in a private, tenant-isolated bucket path (`exports/{accountId}/...`); a new export = a new URL |
| T-1-34 | Elevation of Privilege / Repudiation | an attacker triggers `softDeleteAccount` on a victim's tenant, or undoes a legitimate deletion | mitigate | `requestAccountDeletion` is a `protectedProcedure` (the session's own tenant only); the UI gates it behind the typed-`DELETE`-confirm Dialog (Plan 02/09); `restoreAccount` is also `protectedProcedure` and window-limited; after 30 days the purge cron makes it irreversible (by design) |
| T-1-35 | Tampering | the DPA version isn't recorded, so it's unclear which terms a founder accepted | mitigate | `recordDpaAcceptance` writes the `DPA_VERSION` to both `legal_acceptances` (an append-only log) and `accounts`; bumping `DPA_VERSION` on a material change triggers a re-acceptance prompt |
| T-1-36 | Repudiation | the "no customer data in training" claim has no evidence trail | mitigate | `docs/vendor-data-flow.md` is the living evidence base, referenced from the DPA; any new vendor must be added before it touches customer data; the Anthropic no-training/7-day-retention and the OpenAI-fallback-no-DB-credentials facts are recorded |
| T-1-37 | Information Disclosure | banned compliance string in the DPA or the vendor doc | mitigate | `npm run check:banned` runs in CI and is a verify step here; the DPA uses the allowlisted "not legal advice" form where applicable |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run typecheck && npm run check:banned` exit 0
- `docs/vendor-data-flow.md` exists and covers Anthropic/OpenAI/Claude-Code/Supabase/Stripe/Resend/Sentry/Amplitude/Langfuse/Inngest/Vercel
- `public/legal/dpa.pdf` exists; `src/lib/compliance/dpa-content.tsx` is the single source (consumed by the PDF generator and Plan 08's `/legal/dpa` page)
- `npx vitest run tests/compliance/` passes
- `complianceRouter` (acceptDpa, dpaStatus, requestDataExport, requestAccountDeletion, restoreAccount) is in `appRouter`
- the soft-delete `deleted_at` column name matches between `delete-account.ts` and `purge-soft-deleted.ts`
</verification>

<success_criteria>
The compliance/privacy plumbing is complete: the clickwrap DPA (versioned acceptance on `legal_acceptances`+`accounts`, downloadable `public/legal/dpa.pdf`, the DPA content in a single `dpa-content.tsx` shared with Plan 08's `/legal/dpa` page) with `acceptDpa`/`dpaStatus`, the data-subject-rights plumbing (on-demand full data export → tenant-isolated Storage → expiring signed URL → email; account soft-delete → `deleted_at` → the Plan-04 purge cron; restore-within-30-days) with `requestDataExport`/`requestAccountDeletion`/`restoreAccount`, and the vendor data-flow inventory (`docs/vendor-data-flow.md` — every data-touching vendor, training posture, retention, contract status; OpenAI fallback + Claude-Code build tooling covered) backing XC-01 — XC-01 and XC-04 satisfied. (XC-05 banned-string CI and XC-03 redacting logger were Plan 01; XC-02 founder-approval Dialog was Plan 02.) The sign-up clickwrap line (Plan 07) and the Settings/Billing screens (Plan 09) call this plan's tRPC procedures; Plan 08 renders the `/legal/dpa` page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md` documenting: the vendor inventory's contents + the `[VERIFY]` items needing follow-up, the `DPA_VERSION` + the `dpa-content.tsx` module's location (the single source Plan 08 consumes), the data-export JSON shape + the Storage bucket path + the signed-URL expiry, the soft-delete/restore window, the `complianceRouter` procedure list, and the `getServiceClient` call sites added (account soft-delete).
</output>
</output>
