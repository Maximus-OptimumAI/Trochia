---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [1, 3]
files_modified:
  - src/ai/client.ts
  - src/ai/router.ts
  - src/ai/fallback.ts
  - src/ai/untrusted.ts
  - src/ai/health-check.ts
  - src/ai/schemas/index.ts
  - src/lib/langfuse.ts
  - src/lib/env.ts
  - src/inngest/client.ts
  - src/inngest/functions/ai-health-check.ts
  - src/inngest/functions/reconcile-stripe.ts
  - src/inngest/functions/purge-soft-deleted.ts
  - src/inngest/functions/stubs.ts
  - src/inngest/functions/index.ts
  - src/app/api/inngest/route.ts
  - tests/ai/client.test.ts
  - tests/ai/health-check.test.ts
  - tests/ai/untrusted.test.ts
  - tests/inngest/serve.test.ts
autonomous: true
requirements: [FND-09, FND-11, XC-06, XC-07, XC-01]
must_haves:
  truths:
    - "All Anthropic calls go through src/ai/client.ts — @anthropic-ai/sdk is imported nowhere else (lint-enforced from Plan 01)"
    - "ai/client.ts places cache_control:{type:'ephemeral'} breakpoints on the stable prefix BEFORE the volatile suffix and logs cache_creation_input_tokens vs cache_read_input_tokens to Langfuse"
    - "ai/router.ts routes by task class: Haiku (classify/poll/cheap-QA), Sonnet 4.6 (draft), Opus 4.7 (reason)"
    - "ai/client.ts validates structured output against a Zod schema via forced tool use; on parse failure one repair retry, then the OpenAI fallback behind a config flag"
    - "Inngest serves all functions from one serve() endpoint at /api/inngest; a deploy-time Haiku health-check job exercises ai/client.ts so Langfuse captures a real trace"
    - "ai/untrusted.ts provides a delimit + prompt-injection-screen helper (the pattern; real enforcement is Phase 2/3)"
    - "src/lib/langfuse.ts is created here as a STUB (isLangfuseConfigured() → false, getLangfuseClient() → null) and consumed by ai/client.ts; Plan 05 fills it with the real account/keys logic — ai/client.ts never edits change in Plan 05"
  artifacts:
    - path: "src/ai/client.ts"
      provides: "the single Anthropic chokepoint — runAgent<T>(): prompt caching, model routing, Zod structured output, repair retry, OpenAI fallback flag, Langfuse trace (via getLangfuseClient() from src/lib/langfuse.ts)"
    - path: "src/lib/langfuse.ts"
      provides: "STUB now — isLangfuseConfigured() (false until Plan 05) + getLangfuseClient() (null until Plan 05); ai/client.ts imports both so Plan 05 only fills this file, never touches ai/client.ts"
    - path: "src/ai/router.ts"
      provides: "pickModel(taskClass) → Haiku/Sonnet 4.6/Opus 4.7"
    - path: "src/ai/untrusted.ts"
      provides: "delimitUntrusted() + screenForInjection() helpers (pattern; XC-07)"
    - path: "src/inngest/functions/ai-health-check.ts"
      provides: "deploy-time ~10-token Haiku ping through ai/client.ts → first real Langfuse trace"
    - path: "src/app/api/inngest/route.ts"
      provides: "Inngest serve() handler registering ai-health-check + reconcile-stripe + purge-soft-deleted + the stub functions (deck-parse/embed/transcribe/brief-enrich/esign-webhook/reminders)"
  key_links:
    - from: "src/ai/client.ts"
      to: "src/lib/langfuse.ts"
      via: "imports isLangfuseConfigured() + getLangfuseClient()"
      pattern: "getLangfuseClient|isLangfuseConfigured"
    - from: "src/inngest/functions/ai-health-check.ts"
      to: "src/ai/client.ts"
      via: "calls runAgent (or a thin healthCheck wrapper)"
      pattern: "ai/client|runAgent|healthCheck"
    - from: "src/app/api/inngest/route.ts"
      to: "src/inngest/functions/index.ts"
      via: "serve({ functions: [...] })"
      pattern: "serve\\(\\{"
    - from: "src/ai/client.ts"
      to: "Langfuse"
      via: "trace.update with cache metrics"
      pattern: "cache_creation_input_tokens|cache_read_input_tokens|cacheWrite|cacheRead"
---

<objective>
Ship the `ai/client.ts` chokepoint REAL (D-09: not dormant) — prompt caching on the stable prefix (XC-06), model routing by task class, Zod-typed structured outputs via forced tool use, the OpenAI/Codex fallback behind a config flag, the `ai/untrusted.ts` delimit + injection-screen pattern (XC-07), and a deploy-time Haiku health-check that produces the first real Langfuse trace — and wire Inngest as the slow-work lane (FND-11): one `serve()` endpoint at `/api/inngest`, the `jobs`-table-writing function pattern, per-key concurrency limits + 4 default retries, real functions (ai-health-check on deploy, reconcile-stripe cron, purge-soft-deleted cron) plus stub functions for the eventual workloads (deck parsing, embedding, transcription, brief enrichment, e-sign webhooks, scheduled reminders) so the wiring is proven. The ESLint import-boundary rule from Plan 01 (no `@anthropic-ai/sdk` / `openai` outside `ai/**`) now actually guards a real `ai/` directory. **Create `src/lib/langfuse.ts` as a STUB** (`isLangfuseConfigured()` → false, `getLangfuseClient()` → null) and have `ai/client.ts` import from it — Plan 05 fills the stub with the real account/keys logic, so `ai/client.ts` is never edited again in Plan 05. Flip the AI env vars (`ANTHROPIC_API_KEY` required, `OPENAI_API_KEY`/`AI_FALLBACK_ENABLED`/`LANGFUSE_*` still optional in Phase 1) in `src/lib/env.ts`.

Purpose: Phase 2 builds the memory spine ON this chokepoint; the prompt-caching + Langfuse-instrumentation pattern must be proven before then ("mandatory in the docs ≠ automatically done").
Output: the `ai/` module, the `src/lib/langfuse.ts` stub, the Inngest module, the `/api/inngest` endpoint, the env-var tightening, and the AI/Inngest tests. The Langfuse account/keys are provisioned in Plan 05 (the stub returns null until then — `ai/client.ts`'s trace calls are no-op-safe). The `reconcile-stripe` function references billing state that Plan 07 finalizes — stub its body to a TODO that re-pulls from Stripe once Plan 07's billing module exists. The `purge-soft-deleted` cron uses `accounts.deleted_at` (exists from Plan 03's schema — that's why this plan `depends_on: [1, 3]`) — implement it fully. The Inngest functions import `getServiceClient` from `src/db/client.ts` and the `jobs`/`tenancy` schema from `src/db/schema/*` (Plan 03) — hence the dependency on Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@tasks/constraints.md
@CLAUDE.md

<interfaces>
<!-- RESEARCH.md §"Code Examples" — the ai/client.ts skeleton (runAgent<T>({ taskClass, stablePrefix:{system,toolDefs,corpus?,businessMemory?}, variableSuffix, schema })): place cache_control:{type:'ephemeral'} breakpoints on the stable-prefix blocks IN ORDER (system → toolDefs → corpus → businessMemory) BEFORE the volatile suffix; call anthropic.messages.create with forced tool use for structured output; on parse failure one repair retry then fallbackToOpenAI(opts) if the config flag is on; trace.update({ metadata:{ cacheWrite: res.usage.cache_creation_input_tokens, cacheRead: res.usage.cache_read_input_tokens, inputTokens, outputTokens, model } }). Models: Haiku 4.5 / Sonnet 4.6 / Opus 4.7 (Claude). Use zod-to-json-schema for the tool schema; re-parse with Zod.
Langfuse seam: `src/lib/langfuse.ts` exports `isLangfuseConfigured(): boolean` (true iff `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`/`LANGFUSE_HOST` are set) and `getLangfuseClient(): Langfuse | null` (a memoized singleton, null when unconfigured). In THIS plan it's a STUB — `isLangfuseConfigured()` returns false, `getLangfuseClient()` returns null — with a `// TODO(Plan 05): real implementation once the Langfuse account/keys exist`. `ai/client.ts` does `const langfuse = getLangfuseClient()` and `langfuse?.trace(...)` so it works no-op-safe now and "just works" once Plan 05 fills the stub. Plan 05 does NOT touch `ai/client.ts`.
RESEARCH.md §"Code Examples" — the Inngest serve() endpoint (Next 16 App Router): app/api/inngest/route.ts → export const maxDuration = 300 (up to 800 on Vercel Pro); export const { GET, POST, PUT } = serve({ client: inngest, functions: [aiHealthCheck, reconcileStripe, purgeSoftDeleted, ...Object.values(stubs)] }); per-key concurrency limits + 4 default retries on each function definition.
RESEARCH.md §"Component Responsibilities" — ai/client.ts, ai/router.ts, ai/fallback.ts (config-flagged, NO DB credentials), ai/untrusted.ts (delimit + injection screen), ai/health-check.ts (~10-token Haiku ping invoked by an Inngest job on deploy); inngest/functions/{ai-health-check (on deploy), reconcile-stripe (cron), purge-soft-deleted (cron), stubs}.
RESEARCH.md §"Pitfall 2" — prompt caching mandatory but never wired: bake cache_control breakpoints in NOW even though the prefix is thin in Phase 1; log the cache metrics; the deploy-time Haiku health-check produces the first real trace so caching is confirmed before Phase 2.
RESEARCH.md §"Open Question" — the codex bridge has no DB credentials (architectural boundary, PITFALLS §5); the OpenAI fallback is config-flagged OFF by default; do NOT route production traffic through it.
Inngest "on deploy" trigger: Inngest doesn't have a literal "on deploy" event — use a `vercel.deployment.succeeded` webhook → Inngest event, or simpler: a function triggered by an event the Vercel build step sends post-deploy (a `npm run postbuild` or a Vercel Deploy Hook), OR a scheduled function that runs shortly after deploy + a manual `inngest.send({name:'ai/health-check.requested'})`. Pick the simplest that produces a real Langfuse trace per deploy; document the choice.
Plan 03 deps: `getServiceClient` from `src/db/client.ts`; the `jobs` table + `accounts.deleted_at` from `src/db/schema/{jobs,tenancy}.ts`. env.ts (Plan 01) already declares `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `AI_FALLBACK_ENABLED`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`, `INNGEST_SIGNING_KEY`, `INNGEST_EVENT_KEY` as `.optional()` with "Plan 04 flips this" comments — flip `ANTHROPIC_API_KEY` to prod-required here; leave the rest optional (Plan 05 tightens `LANGFUSE_*`; the Vercel↔Inngest integration auto-injects the Inngest keys in prod).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ai/client.ts chokepoint — prompt caching, model routing, Zod structured output, OpenAI fallback flag, Langfuse trace (via the src/lib/langfuse.ts stub); ai/untrusted.ts pattern; ai/health-check.ts; the langfuse stub; flip ANTHROPIC_API_KEY</name>
  <files>src/ai/client.ts, src/ai/router.ts, src/ai/fallback.ts, src/ai/untrusted.ts, src/ai/health-check.ts, src/ai/schemas/index.ts, src/lib/langfuse.ts, src/lib/env.ts, tests/ai/client.test.ts, tests/ai/untrusted.test.ts</files>
  <behavior>
    - runAgent<T>({ taskClass:'classify', stablePrefix, variableSuffix, schema }) calls Anthropic with the Haiku model; taskClass:'draft' → Sonnet; taskClass:'reason' → Opus (asserted by inspecting the mocked anthropic.messages.create call args)
    - the messages/system passed to anthropic.messages.create carry cache_control:{type:'ephemeral'} on the stable-prefix blocks, positioned before the volatile suffix (asserted on the mocked call args)
    - when the mocked Anthropic response's tool-use args match the Zod schema, runAgent returns the parsed object
    - when the mocked response fails the Zod schema once then succeeds, runAgent retries once and returns the parsed object
    - when parsing keeps failing AND the OpenAI-fallback config flag is on, runAgent calls the OpenAI fallback; when the flag is off, it throws
    - runAgent emits a Langfuse trace whose metadata includes cacheWrite/cacheRead/inputTokens/outputTokens/model when getLangfuseClient() returns a client; when it returns null (the stub state in Phase 1), no crash — a no-op trace path
    - delimitUntrusted(text) wraps text in clearly-delimited fences; screenForInjection(text) flags obvious injection markers ("ignore previous instructions", "system:", etc.) and returns {flagged:boolean, matches:string[]}
  </behavior>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 — ai/client.ts ships real: prompt caching on the stable prefix, model routing Opus/Sonnet/Haiku, Zod-typed outputs, OpenAI/Codex fallback behind a config flag, deploy-time Haiku health-check producing a real Langfuse trace; the lints enforce this is the only path to Anthropic; the claude-api skill's prompt-caching mandate applies — XC-06; XC-07 untrusted-input pattern)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Code Examples" the ai/client.ts skeleton; §"Pattern 3" AI chokepoint with enforced prompt caching + model routing; §"Pitfall 2" caching mandatory-but-unwired; §"Component Responsibilities" ai/* rows; §"Open Question" codex bridge has no DB credentials; §"Standard Stack" — @anthropic-ai/sdk Claude Opus 4.7/Sonnet 4.6/Haiku 4.5, openai fallback-only behind a flag, langfuse JS SDK v3 inside ai/client.ts, zod-to-json-schema)
    - tasks/constraints.md ("Architecture guardrails" — all Anthropic via ai/client.ts lint-enforced; prompt caching active on every production call, instrumented in Langfuse; safe-engine/cap-table-engine no ai/ import)
    - eslint.config.mjs (from Plan 01 — the import-boundary rule now guards this real ai/ dir)
    - src/lib/env.ts (from Plan 01 — `ANTHROPIC_API_KEY` etc. declared `.optional()` with a "Plan 04 flips this" comment)
  </read_first>
  <action>
    Create `src/lib/langfuse.ts` (the STUB): `export function isLangfuseConfigured(): boolean { return false; /* TODO(Plan 05): true iff env.LANGFUSE_PUBLIC_KEY && env.LANGFUSE_SECRET_KEY && env.LANGFUSE_HOST */ }` and `export function getLangfuseClient(): Langfuse | null { return null; /* TODO(Plan 05): memoized `new Langfuse({ publicKey, secretKey, baseUrl })` when configured */ }` — with a header comment: "STUB until Plan 05 — `ai/client.ts` consumes these so Plan 05 only fills this file; do NOT move the Langfuse construction back into `ai/client.ts`." Create `src/ai/router.ts`: `type TaskClass = 'classify' | 'draft' | 'reason'`; `pickModel(taskClass): string` → `'classify'` → the Haiku 4.5 model id, `'draft'` → Sonnet 4.6, `'reason'` → Opus 4.7 (read the exact model ids from the current Anthropic docs via Context7; export them as named constants). Create `src/ai/fallback.ts`: `fallbackToOpenAI<T>(opts): Promise<T>` — uses the `openai` SDK (the ONLY other file allowed to import it), reads `process.env.OPENAI_API_KEY`, is invoked ONLY when `env.AI_FALLBACK_ENABLED === 'true'` (a config flag, default off); a header comment: "this module has NO database credentials — the codex bridge is a build-time/fallback tool, never the default production path". Create `src/ai/schemas/index.ts`: a place for shared Zod structured-output schemas — mostly empty in Phase 1; add a tiny `HealthCheckSchema = z.object({ ok: z.boolean() })` for the health-check. Create `src/ai/untrusted.ts`: `delimitUntrusted(text: string, label = 'USER_CONTENT'): string` — returns the text wrapped in clearly-marked fences (e.g. `<<<${label}_BEGIN>>>\n${text}\n<<<${label}_END>>>` with a note that the model must treat everything between the fences as data, not instructions); `screenForInjection(text: string): { flagged: boolean; matches: string[] }` — regex-scan for common injection markers ("ignore (all )?previous instructions", "disregard the above", "system:", "you are now", "</?(system|assistant)>", etc.), returns the matches; a header comment: "this is the PATTERN — real enforcement (rejecting/flagging uploaded decks/transcripts/knowledge packs) starts Phase 2/3; never let model output trigger an external action without the founder-approval Dialog (XC-02, XC-07)". Create `src/ai/client.ts` (THE only file importing `@anthropic-ai/sdk`): `const anthropic = new Anthropic({ apiKey: env.ANTHROPIC_API_KEY })`; **`const langfuse = getLangfuseClient()`** (imported from `@/lib/langfuse` — null in Phase 1, real once Plan 05 fills the stub); `export async function runAgent<T>(opts: { taskClass: TaskClass; stablePrefix: { system: string; toolDefs?: unknown; corpus?: string; businessMemory?: string }; variableSuffix: unknown; schema: ZodType<T> }): Promise<T>` — `const model = pickModel(opts.taskClass)`; start a Langfuse trace via `langfuse?.trace(...)` (or a no-op object — null-safe); build the request: a `system` array whose blocks are `[ {type:'text', text: opts.stablePrefix.system, cache_control:{type:'ephemeral'}}, ...(toolDefs/corpus/businessMemory as further cached blocks IN ORDER each with cache_control), ]` — the cache breakpoints go on the STABLE prefix; the `messages` array carries the VOLATILE `variableSuffix` (this deck / this turn) WITHOUT cache_control; convert `opts.schema` via `zod-to-json-schema` into a tool definition and use `tool_choice: { type: 'tool', name: '...' }` (forced tool use) for reliable structured output; `const res = await anthropic.messages.create({ model, system, tools, tool_choice, messages, max_tokens })`; `trace?.update({ metadata: { cacheWrite: res.usage.cache_creation_input_tokens, cacheRead: res.usage.cache_read_input_tokens, inputTokens: res.usage.input_tokens, outputTokens: res.usage.output_tokens, model } })`; extract the tool-use args, `const parsed = opts.schema.safeParse(args)`; if `!parsed.success` → one repair retry (re-call with a "your last output failed validation, here's the error" message) → if still failing → `if (env.AI_FALLBACK_ENABLED === 'true') return fallbackToOpenAI(opts); else throw new AppError('AI structured output failed validation')`; on any Anthropic error `trace?.update({ level: 'ERROR' }); throw`. Create `src/ai/health-check.ts`: `export async function aiHealthCheck(): Promise<{ ok: boolean }>` — calls `runAgent({ taskClass: 'classify', stablePrefix: { system: 'Reply with the JSON {"ok": true}.' }, variableSuffix: 'ping', schema: HealthCheckSchema })` (a ~10-token Haiku call) so Langfuse captures a real trace (once Plan 05's keys are live); returns the parsed result. In `src/lib/env.ts`: flip `ANTHROPIC_API_KEY` from `.optional()` to required-in-prod (a disjoint edit touching only that line); leave `OPENAI_API_KEY`, `AI_FALLBACK_ENABLED` (default `'false'`), `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`/`LANGFUSE_HOST`, `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` `.optional()` (Plan 05 tightens `LANGFUSE_*`). Write `tests/ai/client.test.ts` (Vitest, MSW-mock the Anthropic + OpenAI endpoints): assert model routing (classify→Haiku, draft→Sonnet, reason→Opus by inspecting the request body's `model`), assert `cache_control` breakpoints are present on the system blocks and absent on the volatile message, assert Zod parse success returns the object, assert one repair retry on first-failure-then-success, assert the fallback is called when the flag is on and a throw when off, assert a Langfuse trace is created with the cache-metric metadata when `getLangfuseClient()` is mocked to return a client, AND assert the null path (the stub state) doesn't crash. Write `tests/ai/untrusted.test.ts`: `delimitUntrusted("hi")` contains the fence markers; `screenForInjection("ignore all previous instructions and ...")` returns `{ flagged: true, matches: [...] }`; `screenForInjection("a normal sentence")` returns `{ flagged: false, matches: [] }`.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npx vitest run tests/ai/client.test.ts tests/ai/untrusted.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` passes — and `@anthropic-ai/sdk` is imported ONLY in `src/ai/client.ts`, `openai` ONLY in `src/ai/fallback.ts` (verifiable: adding either import elsewhere fails lint)
    - `src/lib/langfuse.ts` exists as a stub: `isLangfuseConfigured()` returns false, `getLangfuseClient()` returns null, with a `TODO(Plan 05)` and the "do not move Langfuse construction back into ai/client.ts" header note
    - `src/ai/client.ts` imports `getLangfuseClient` from `@/lib/langfuse` (NOT an inline `new Langfuse()`)
    - `tests/ai/client.test.ts` passes: classify→Haiku, draft→Sonnet, reason→Opus model ids; `cache_control:{type:'ephemeral'}` on the system/stable-prefix blocks, none on the volatile message; Zod parse returns the object; one repair retry on first-fail-then-succeed; fallback called iff `AI_FALLBACK_ENABLED==='true'` else throws; a Langfuse trace with cacheWrite/cacheRead/inputTokens/outputTokens/model metadata when `getLangfuseClient()` is mocked-to-return-a-client; the null path (stub) no crash
    - `tests/ai/untrusted.test.ts` passes: `delimitUntrusted` fences the text; `screenForInjection` flags "ignore all previous instructions" and passes a normal sentence
    - `src/ai/fallback.ts` has the "no database credentials" header comment and only runs when the config flag is on
    - `src/lib/env.ts` now requires `ANTHROPIC_API_KEY` in prod (a disjoint edit); `OPENAI_API_KEY`/`AI_FALLBACK_ENABLED`/`LANGFUSE_*`/`INNGEST_*` remain `.optional()`
  </acceptance_criteria>
  <done>The ai/ chokepoint is real: prompt-caching breakpoints baked in, model routing live, Zod structured output via forced tool use with a repair retry and a config-flagged OpenAI fallback, the untrusted-input delimit/screen pattern, the Haiku health-check, the `src/lib/langfuse.ts` stub it consumes (Plan 05 fills it), and `ANTHROPIC_API_KEY` flipped prod-required — all behind the lint boundary; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Inngest — client, the serve() endpoint, real functions (ai-health-check on deploy, reconcile-stripe cron stub, purge-soft-deleted cron), stub functions, the jobs-table-writing pattern</name>
  <files>src/inngest/client.ts, src/inngest/functions/ai-health-check.ts, src/inngest/functions/reconcile-stripe.ts, src/inngest/functions/purge-soft-deleted.ts, src/inngest/functions/stubs.ts, src/inngest/functions/index.ts, src/app/api/inngest/route.ts, tests/inngest/serve.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 — Inngest single app, single serve() endpoint at /api/inngest, 4 retries default, per-key concurrency limits; Phase 1 registers the endpoint, the jobs table (exists from Plan 03), Supabase Realtime status polling, real-but-mostly-no-op job functions for the eventual workloads (deck parsing, embedding, transcription, brief enrichment, e-sign webhooks, scheduled reminders), plus the one real job that matters now: the AI chokepoint health-check ping)
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Code Examples" the Inngest serve() endpoint for Next 16 App Router; §"Component Responsibilities" inngest/functions rows — ai-health-check on deploy, reconcile-stripe cron (the safety net for billing tier — PITFALLS §18), purge-soft-deleted cron (accounts.deleted_at > 30 days → permanent delete — XC-04); §"Standard Stack" — inngest SDK v4, Vercel↔Inngest integration auto-wires env+endpoint on deploy, maxDuration 300–800s; §"Don't Hand-Roll" — Inngest is the slow-work lane)
    - src/db/schema/jobs.ts, src/db/schema/tenancy.ts, src/db/client.ts (from Plan 03 — the jobs table + accounts.deleted_at + getServiceClient; THIS is why this plan depends on Plan 03)
    - src/ai/health-check.ts (from task 1 — the function ai-health-check calls)
  </read_first>
  <action>
    Create `src/inngest/client.ts`: `export const inngest = new Inngest({ id: 'trochia', signingKey: env.INNGEST_SIGNING_KEY, eventKey: env.INNGEST_EVENT_KEY })` (or per the current Inngest v4 init API — Context7 `inngest`). Create `src/inngest/functions/ai-health-check.ts`: `inngest.createFunction({ id: 'ai-health-check', retries: 4, concurrency: { limit: 1 } }, { event: 'ai/health-check.requested' }, async ({ step }) => { const r = await step.run('haiku-ping', () => aiHealthCheck()); return r })` — and decide the deploy trigger: implement BOTH a `npm run postbuild` (or a Vercel Deploy Hook) that does `inngest.send({ name: 'ai/health-check.requested' })` AND keep the function event-triggerable manually; document the chosen mechanism. Have it write a `jobs` row (via `getServiceClient()` — a `jobs.type='ai-health-check'`, `account_id=null` system job, status transitions queued→running→done/failed) so the jobs-table-writing pattern is demonstrated. Create `src/inngest/functions/reconcile-stripe.ts`: `inngest.createFunction({ id: 'reconcile-stripe', retries: 4 }, { cron: '0 */6 * * *' }, async ({ step }) => { /* TODO(Plan 07): for each account with a stripe_customer_id, re-pull the subscription from the Stripe API and reconcile accounts.subscription_status/tier/current_period_end — webhooks are the optimization, this poller is the safety net (PITFALLS §18). Until Plan 07 wires the billing module, this is a no-op that logs 'reconcile-stripe: billing module not yet wired'. */ })`. Create `src/inngest/functions/purge-soft-deleted.ts`: `inngest.createFunction({ id: 'purge-soft-deleted', retries: 4 }, { cron: '0 3 * * *' }, async ({ step }) => { /* find accounts where deleted_at < now() - interval '30 days'; via getServiceClient(), permanently delete those accounts and their cascading rows (subscriptions, legal_acceptances, jobs, sessions) — XC-04. Implement this FULLY (the schema exists from Plan 03). Log the count purged. */ })`. Create `src/inngest/functions/stubs.ts`: real `inngest.createFunction` definitions (so the wiring is proven) but no-op bodies, for: `deck-parse` (event `deck/uploaded`), `embed` (event `embedding/requested`), `transcribe` (event `transcript/uploaded`), `brief-enrich` (event `brief/enrich.requested`), `esign-webhook` (event `esign/event.received`), `reminders` (cron `0 9 * * *`) — each with `retries: 4` and a sensible `concurrency` limit, each body just `logger.info('stub: <name> — implemented in Phase N')`. Create `src/inngest/functions/index.ts`: re-export `[aiHealthCheck, reconcileStripe, purgeSoftDeleted, ...Object.values(stubs)]` as `allFunctions`. Create `src/app/api/inngest/route.ts`: `export const maxDuration = 300; export const { GET, POST, PUT } = serve({ client: inngest, functions: allFunctions });` (per the RESEARCH.md example; `maxDuration` can go to 800 on Vercel Pro — 300 is fine for Phase 1). Add a `postbuild` npm script if you chose the postbuild trigger. (Env: `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` were declared in `src/lib/env.ts` by Plan 01 and stay `.optional()` in Phase 1 dev — the Vercel↔Inngest integration auto-injects them in prod; do NOT re-edit env.ts here.) Write `tests/inngest/serve.test.ts`: import `allFunctions` and assert it includes functions with ids `ai-health-check`, `reconcile-stripe`, `purge-soft-deleted`, `deck-parse`, `embed`, `transcribe`, `brief-enrich`, `esign-webhook`, `reminders`; assert each has `retries: 4`; import the route module and assert `GET`/`POST`/`PUT` are exported; (optionally) invoke the `purge-soft-deleted` function's handler with a mocked service client containing an account with `deleted_at` 31 days ago and assert it's deleted, and one 5 days ago and assert it's kept.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npx vitest run tests/inngest/serve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `src/app/api/inngest/route.ts` exports `GET`, `POST`, `PUT` from `serve({ client: inngest, functions: allFunctions })` and `export const maxDuration` is set
    - `tests/inngest/serve.test.ts` passes: `allFunctions` includes ids `ai-health-check`, `reconcile-stripe`, `purge-soft-deleted`, `deck-parse`, `embed`, `transcribe`, `brief-enrich`, `esign-webhook`, `reminders`; each has `retries: 4`
    - `purge-soft-deleted` deletes accounts with `deleted_at` older than 30 days and keeps newer ones (asserted by the test with a mocked service client) — implemented fully, not stubbed
    - `reconcile-stripe` body is a documented no-op with a `TODO(Plan 07)` (not yet wired to Stripe)
    - `ai-health-check` calls `aiHealthCheck()` from `src/ai/health-check.ts` (via `step.run`) and writes a `jobs` row; the deploy-trigger mechanism is documented in the SUMMARY
  </acceptance_criteria>
  <done>Inngest is the slow-work lane: one serve() endpoint at /api/inngest registering the AI health-check (real, calls ai/client.ts → Langfuse trace once Plan 05's keys land), reconcile-stripe (cron, stubbed for Plan 07), purge-soft-deleted (cron, fully implemented — XC-04), and the stub functions for the future workloads; per-key concurrency + 4 retries; the jobs-table-writing pattern demonstrated; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: ai/health-check integration test — the deploy-time Haiku ping produces a Langfuse trace with cache metrics</name>
  <files>tests/ai/health-check.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§"Validation Architecture" — the FND-09/XC-06 row: "Cache hit rate instrumented in Langfuse (deploy-time Haiku health-check produces a trace)" → `tests/ai/health-check.test.ts` asserts the health-check emits a Langfuse trace with cache-metric metadata; §"Pitfall 2")
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 — the deploy-time health-check exercises the path so Langfuse captures a real trace (cache hit rate, tokens, latency, cost) before Phase 2 builds on it)
    - src/ai/health-check.ts, src/ai/client.ts, src/lib/langfuse.ts (from task 1)
  </read_first>
  <action>
    Write `tests/ai/health-check.test.ts` (Vitest): mock the Anthropic endpoint (MSW) to return a tool-use response `{ ok: true }` with a realistic `usage` block including `cache_creation_input_tokens` and `cache_read_input_tokens`; mock `getLangfuseClient()` (from `@/lib/langfuse`) to return a spy client (so no network); call `aiHealthCheck()`; assert (1) it returns `{ ok: true }`; (2) `runAgent` was called with `taskClass: 'classify'` (Haiku); (3) the spy Langfuse client created a trace and `.update()` was called with metadata containing `cacheWrite`, `cacheRead`, `inputTokens`, `outputTokens`, `model`; (4) when `getLangfuseClient()` is mocked to return `null` (the stub state), `aiHealthCheck()` still returns `{ ok: true }` without crashing (the no-op trace path). Add a note: post-deploy (after Plan 05's keys are live), the real trace is verified manually in the Langfuse dashboard (this is the one Manual-Only verification for FND-09/XC-06 — record it in VALIDATION.md's Manual-Only table).
  </action>
  <verify>
    <automated>npx vitest run tests/ai/health-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ai/health-check.test.ts` passes: `aiHealthCheck()` returns `{ ok: true }`; `runAgent` called with `taskClass:'classify'`; with `getLangfuseClient()` mocked to a spy, a Langfuse trace `.update()` carries `cacheWrite`/`cacheRead`/`inputTokens`/`outputTokens`/`model` metadata; with `getLangfuseClient()` mocked to `null`, returns `{ ok: true }` without crashing
  </acceptance_criteria>
  <done>The deploy-time Haiku health-check is proven by test to exercise ai/client.ts and emit a Langfuse trace with the cache metrics (when configured) — XC-06's "cache hit rate is instrumented, not assumed" has an automated guard (plus a manual post-deploy dashboard check recorded in VALIDATION.md, once Plan 05's keys land).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → ai/ chokepoint | the ONLY sanctioned path to Anthropic; bypassing it skips prompt caching, cost controls, Langfuse tracing, and the untrusted-input pattern |
| ai/ → Anthropic API | requests carry the cached stable prefix + the volatile suffix; the volatile suffix will (Phase 2/3) contain untrusted uploaded content |
| ai/fallback.ts → OpenAI | a config-flagged fallback with NO database credentials — the codex bridge is never the default and never touches tenant data |
| Inngest cloud → /api/inngest | Inngest invokes functions with a signing key; an unverified invocation could trigger jobs |
| Inngest job → service-role DB client | jobs run with `getServiceClient()` (RLS-bypassing) and must re-assert the tenantId they carry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-18 | Elevation of Privilege / Tampering | code calls Anthropic/OpenAI directly, bypassing the chokepoint (no caching, no cost cap, no tracing) | mitigate | ESLint import-boundary rule (Plan 01) bans `@anthropic-ai/sdk` outside `src/ai/client.ts` and `openai` outside `src/ai/fallback.ts`; CI fails on violation; `runAgent` is the single entry point |
| T-1-19 | Tampering / Information Disclosure | prompt injection via the volatile suffix (uploaded decks/transcripts/knowledge packs — Phase 2/3) makes the model emit attacker-controlled output or exfiltrate the cached prefix | mitigate (pattern now, enforced Phase 2/3) | `ai/untrusted.ts` delimits untrusted content and screens for injection markers; structured output is Zod-validated before use; model output never triggers an external action without the founder-approval Dialog (XC-02, XC-07) |
| T-1-20 | Repudiation / Information Disclosure | customer data trains a model via a default API setting; the OpenAI fallback leaks tenant data | mitigate | Anthropic API = no-training, 7-day retention (confirmed); the OpenAI fallback is config-flagged OFF and has no DB credentials; the vendor data-flow inventory (Plan 06) records both; XC-01 |
| T-1-21 | Spoofing / Tampering | a forged request to `/api/inngest` triggers functions (e.g. `purge-soft-deleted`) | mitigate | Inngest's `serve()` handler verifies the signing key (`INNGEST_SIGNING_KEY`); only Inngest cloud (with the key) can invoke functions |
| T-1-22 | Denial of Service (economic) | a runaway caller floods `ai/client.ts` → unbounded Anthropic cost | mitigate (pattern now) | bounded repair retries (one); Inngest per-key concurrency caps on AI-backed functions; per-tenant cost tracking in Langfuse — full rate-limiting on tRPC AI routes lands when those routes exist (Phase 2+, PITFALLS §19); for Phase 1 the only AI traffic is the deploy-time Haiku ping |
| T-1-23 | Information Disclosure | the AI health-check's prompt/response or token counts logged with sensitive content | accept | the health-check prompt is "Reply with {\"ok\":true}" and the response is `{ok:true}` — no sensitive content; Langfuse stores token counts + the trivial prompt, not customer data |
| T-1-24 | Information Disclosure | the `jobs.payload`/`result` jsonb columns leak data cross-tenant | mitigate | the `jobs` table has RLS (Plan 03); system jobs (`account_id IS NULL`) are service-client-only; tenant jobs are isolated by the `account_id` policy; never put financial figures / transcript bodies in `jobs.payload` (logging-scrub discipline, XC-03) |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run typecheck` exit 0
- `@anthropic-ai/sdk` imported only in `src/ai/client.ts`; `openai` only in `src/ai/fallback.ts` (lint enforces)
- `src/lib/langfuse.ts` exists as a stub; `src/ai/client.ts` imports `getLangfuseClient` from it (no inline `new Langfuse()`)
- `npx vitest run tests/ai/ tests/inngest/` passes
- `/api/inngest` route exports GET/POST/PUT with `maxDuration` set
- `purge-soft-deleted` fully implemented (deletes >30-day soft-deleted accounts); `reconcile-stripe` a documented stub for Plan 07
- `src/lib/env.ts` requires `ANTHROPIC_API_KEY` in prod
</verification>

<success_criteria>
The `ai/client.ts` chokepoint is REAL — prompt caching on the stable prefix (XC-06), model routing (Haiku/Sonnet/Opus), Zod structured output via forced tool use with a repair retry, the config-flagged OpenAI fallback (no DB creds), the `ai/untrusted.ts` delimit/screen pattern (XC-07), the deploy-time Haiku health-check, and the `src/lib/langfuse.ts` stub `ai/client.ts` consumes (Plan 05 fills it — `ai/client.ts` untouched in Plan 05) — all behind the lint boundary (FND-09). Inngest is the slow-work lane: one `serve()` endpoint at `/api/inngest` (FND-11) with the AI health-check (real), reconcile-stripe (cron, stubbed for Plan 07), purge-soft-deleted (cron, full — XC-04), and stub functions for the future workloads, per-key concurrency + 4 retries, the jobs-table pattern. `ANTHROPIC_API_KEY` flipped prod-required. Tests for ai/ + inngest pass. Langfuse/Sentry/Amplitude SDK init + keys are Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` documenting: the exact Anthropic model ids chosen, the `runAgent` signature, the cache-breakpoint placement, the `src/lib/langfuse.ts` stub API (and that Plan 05 fills it, never touching `ai/client.ts`), the OpenAI-fallback config flag name, the Inngest deploy-trigger mechanism chosen, the function ids + their triggers, the `reconcile-stripe` TODO for Plan 07, the `purge-soft-deleted` retention logic, and the env var(s) flipped. Record the post-deploy "verify the Langfuse trace appeared" manual check for VALIDATION.md.
</output>
</output>
